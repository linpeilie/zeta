# 04 · 实现记录

**任务**：接入 Claude Code CLI 作为第三个 Agent Provider  
**对应计划**：[`03-实现计划.md`](./03-实现计划.md)

约定：每行一条偏离 / 发现 / 临时决策；无则该任务段不写。

---

## T01 · 修正设计文档四处偏差

- 额外同步了设计文档 §9 测试策略（明确 storm 等共享层测试一行不改、CC fixture 只放 Provider 自有目录、补 identity 单测条目），超出完成判据字面范围，但与 §1 / §8 M2 同一处偏差修正一致。
- `flutter test` 全量 **2 红**，与 T01 无关：`agent_usage_panel_test` 仍期望文案「已用 x% · 剩余 y%」，而 `bb302eb6 refactor(usage): 优化展示内容` 已改展示却未同步测试。`dart format` / `flutter analyze` 通过。

## T02 · CLI 取样 + 脱敏 fixture

- `result_error.jsonl` 的 `error_max_turns` / `error_during_execution` **未 live 打出**，按设计文档 subtype 名 + live `result.success` 字段形状合成；已在 `02-契约缺口.md` §5 登记。
- 每 turn 会再发一次 `system.init`（同 session_id）——设计文档未写；记入 02 §1 Q3 / §2，后续 mapper 须幂等，未在本步改设计正文。
- live 另见 `rate_limit_event`、`system/thinking_tokens`；按既有「未识别丢弃」处理，未扩事件表。
- `ExitPlanMode.input` 取样为空 `{}`；T22 实现前 plan 正文来源未钉死（02 §5）。
- fixture 路径用 `/tmp/zeta-cc-fixture` 占位，非真实 Windows 路径。

## T03 · 架构守卫

- 计划写「6 个 G1 文件」：取 5 个共享 application 文件 + `agent_provider_bundle.dart`（禁止 kind 分支）。未把 acp_* 共享 mapper 列入本守卫（与 T18 五文件列表不同，但覆盖 plan 字面三条断言）。
- `AgentProviderKind` 基线计数 = 0（与当前 bundle 源码一致）。

## T04–T06 · domain / capability / 空壳

- `AgentProviderKind.claudeCode` 与 icon kind 分支、factory throw 分支在仓库中**已预先存在**；T04 只补 id 常量与 `defaultClaudeCode`，T06 把 factory throw 换成空壳实例。
- `defaultsFor(claudeCode)` 仅 `canPrompt`/`canCancelTurn`（按 plan），`canCreateSession` 仍 false——新建 thread 若依赖该位可能暂时不可走通，M1（T14）前属预期。

## T07 · 管理侧

- 计划写改 `lib/src/app/app.dart`，实际 repository map 在 `lib/src/ui/features/ide/views/ide_home.dart`（真正的 composition 点）；按实际代码接线，未硬造 app.dart 改动。
- 最小 repository 固定 `notInstalled`，完整 detect 留给 T29。

## T08 · 图标与详情文案

- `AgentProviderIcon` 的 `claudeCode` kind 分支与 icon 单测已存在；本步主要是管理页 `_AgentLogo` 传入 kind，以及 Claude Code 详情三段接入指引卡。

## T09–T10 · 传输与启动参数

- `StreamJsonPeer` 用自定义行缓冲做 max-line 阈值（非 `LineSplitter`），以支持超长行拒收。
- `ProcessStarter` 复用 transport 层 typedef；`which` 解析失败不抛，回落 config.command（符合「starter 不预判可用性」）。

## T11 · StreamIdentity 骨架

- 完成判据要求 §3.B 第 2 行（tool_use 打断），因此骨架额外带了 `resolveTool`（T15 仍负责 `resolveReasoning` 与完整 11 行）。
- entryId 用 session/turn/source 的 **hash 片段** 而非 source id 原文，避免 snapshot/diagnostics 泄漏（G8）；与 Grok 把 `Uri.encodeComponent(sourceId)` 嵌进 entryId 的做法有意不同。
- snapshot 字段用 `sourceMessageEntryCount` / `seenToolCallCount`，不暴露 source id map。

## T12 · EventMapper 最小集

- 设计文档写 `AgentSession(workingDirectory/model)`，实际 domain 的 `AgentSession` 只有 `id/providerId/title/raw`——cwd/model 写入 `raw`，未改 domain。
- 设计写 `AgentThreadStatusChangedEvent(running=false)`，实际 API 用 `AgentThreadRuntimeStatus.idle`。
- T12 故意忽略 `thinking`/`tool_use` content block（留给 T16/T17）；hello_turn 中 thinking 帧不产出事件。

## T13 · control_request fail-closed stub

- Handler 纯同步，返回 `responseFrame` 由调用方 `StreamJsonPeer.send`（T14 接线）；不持有 peer，便于单测。
- 缺 `request_id` 时仍产出 deny 帧（合成 id），避免 CLI hang；计入 `malformedCount`。

## T14 · Provider 接线

- 除 plan 写的 `supportsUsage` 外，额外打开 `canCreateSession`（否则 startSession 语义不诚实）。
- `initialize` 不拉起进程（无 cwd）；`startSession` 才按 projectPath 启动 peer + `--session-id` + `--permission-mode default`。
- 单测用 fake Process 覆盖 hello turn / control deny；真实 CLI 手工冒烟未在本机全量重跑（T02 已证多回合常驻）。
- `grep 尚未接入` 在 `datasources/claude_code/` 下无输出。

## T15 · identity 扩展（§3.B 全 11 行）

- 新增 `resolveReasoning`、公开 `closeVisiblePhases`；`resolveTool` / 有界 `recentRawEventKeys` 在 T11 已有，本步补测。
- 额外单测：B2 partial/whole 同 entryId、`maxRecentRawEvents` 窗口裁剪。
- 跨 session 丢弃依赖显式 `invalidateSession`（beginTurn 只失效同 session 的旧 active）。

## T16 · mapper thinking

- `thinking` → `AgentReasoningDeltaEvent(kind: text)`，phase entryId 经 `resolveReasoning`；text 经 `resolveMessage` 自动关 phase。
- `thinking_interleaved.jsonl` 断言两个不同 reasoning entryId + 独立 message entryId。
- tool_use 仍跳过（T17）。

## T17 · mapper 工具生命周期

- `tool_use` → `AgentToolCallEvent(inProgress)`；`user.tool_result` → completed / failed（`is_error == true` 才 failed，缺省 completed）。
- 成功 tool_result 常省略 `is_error`（与 T02 取样一致）。
- `ExitPlanMode` 本阶段按普通工具卡（`AgentToolKind.other`），不授予权限。
- user 非 tool_result 静默丢弃。

## T18 · 共享层零改动验收

- purity 测试追加 G1 五文件 `lineCount + byteLength + FNV-1a` 内容基线。
- `git diff origin/dev`（及 working tree）对五文件 **输出为空**。
- storm / timeline_store / coalescing_buffer / pipeline 回归全绿；对应测试文件本身未改。

## T19 · 权限模式 codec

- 新增 `ClaudeCodePermissionModeCodec`：`:ask`↔`default`、`:accept-edits`↔`acceptEdits`、`:plan`↔`plan`、`:bypass`↔`bypassPermissions`。
- 未知/空值 fail-closed 到 `:ask`，单测锁定绝不落到 `:bypass`。
- `catalog()` 静态 4 项、`defaultOptionId=':ask'`（T21 adapter 将调用）。

## T20 · control_request 真实审批

- 删除 T13 对 `can_use_tool` 的自动 deny；改为 pending registry + `AgentPermissionRequestedEvent`。
- sessionId/turnId 由 Provider 从 running turn 注入（control 帧自身无作用域）。
- 四种决策 wire：`allow_once`/`allow_always` → `behavior:allow` + `updatedInput`；`deny_once`/`deny_always` → `behavior:deny` + `message`。
- `allow_always`/`deny_always` 的 session 缓存留给 T21；本步只编码 wire 并暴露 `outcome`/`toolName`。
- 未知 control type / 缺 request_id 仍 fail-closed 立即 deny。
- `AgentPermissionRequest.raw` 仅 `tool_name` + source 标记，不落 input（G8）；command 走专用字段。
- `supportsPermissionRequests` 仍 false（T24 打开）；handler/provider 路径已可测。

## T21 · 权限 adapter 接线（阻塞已解除并完成）

- 触发止损条件「方案和实际代码对不上」：计划与设计文档要求
  `applyPermissionSelection` 返回 `nextSession` 后触发 Provider/Registry 重启；实际
  `AgentConversationPermissionSelectionController` 对 `nextSession` 只提交待生效状态并持久化
  optionId，没有 runtime 失效或重启回调。
- 当前唯一的 Provider 全量重启入口是
  `AgentProviderSettingsController.updateProviderConfig(restartProvider: true)`；权限持久化路径
  `persistPermissionOptionIdForProvider` 不触发重启。Registry 的配置差异判断也只比较 kind、
  command、arguments、environment，不比较 permission optionId。
- data adapter 不能自行调用 Registry；若在 `ClaudeCodeAgentProvider` 内直接重拉 peer，现阶段又
  没有已实现的 resume 通路（T28 尚未开始），无法保证保持当前 session。继续需要先确认是扩展
  application 编排入口/回调，还是调整 T21 为“仅下次新 session 生效”。本轮未改运行时代码，
  未执行 format/analyze/test。
- 用户确认采用「切换后重启，但当前会话必须没有执行中的 turn」后解除阻塞。这里的“重启”限定为
  `ClaudeCodeAgentProvider` 内部 CLI peer 重启，不重建 Registry/Provider/Binding；旧 peer 关闭后
  使用同一 session id 的 `--resume` 与新 `--permission-mode` 拉起，因此无需提前实现公开的 T28
  resume 能力，也没有修改共享适配层或 Provider 端口。
- UI 在 turn running 时禁用权限选择器，ViewModel 返回明确提示；Provider 以 running turn、turn
  admission、pending control_request 三重检查作为权威门禁。权限切换先占用互斥 operation；并发
  `sendMessage` 会等待切换完成，避免旧参数启动新 turn。
- 当前没有 peer 时只更新内存 mode，scope 为 `nextSession`；空闲的已启动 session 成功重启后 scope
  为 `currentSession`。重启失败会恢复旧 mode，并 best-effort 以同 session 恢复旧 peer后抛出原错。
- 新增版本化 session 决策文件存储，生产文件由 `lib/src/app` 组合层注入到
  `~/.zeta/state/claude_code/session_<id>.json`；JSON 每项只含 `toolName` 和 `decision`，损坏/未知版本
  宽容解码为空。`allow_always` / `deny_always` 后，相同工具后续请求直接按缓存回写，不再发权限事件；
  当前请求 input 仅用于 live `updatedInput`，从不落盘。
- 验证：`dart format .` 无额外改动；`flutter analyze` 通过；相关 5 个测试文件共 31 项全绿（adapter、
  Provider、Composer、默认 factory、共享层纯度）。真实 Claude CLI 手工切换尚未执行，自动化 fake
  Process 已逐字段断言第二次启动包含同 session 的 `--resume` 与新 permission mode。
- 完整回归：`flutter test` 通过（1550 passed，1 skipped）；共享适配层五文件 working tree diff 为空。

## T22 · Plan 审批（阻塞）

- 触发止损条件「方案和实际代码对不上」：设计文档要求由
  `ExitPlanMode` 的 `tool_use` 直接生成 Plan 审批，并在用户决定后通过
  `control_response` 回写；但冻结的真实 fixture 只有 `tool_use.id`，没有可用于
  `control_response` 的 `control_request.request_id`，且 `input` 为 `{}`。现有证据无法证明
  `tool_use.id` 与 `request_id` 等价，不能自行猜测关联键。
- 官方 Claude Agent SDK 当前实现显示 `can_use_tool` 的 control request 以
  `request.subtype` 区分，另带 `tool_use_id` 关联工具调用；成功响应则使用嵌套的
  `response.subtype/request_id/response` 形状。当前设计文档、T20 handler 与测试使用
  `request.type` 和扁平 `control_response`，协议形状存在实质差异；T02 又未采到
  `control_request`，因此不能确认本机基线 CLI 2.1.224 实际采用哪一版 wire。
- T22 需要先补一次脱敏定点取样，至少冻结 `ExitPlanMode` 对应
  `control_request` 的 `request_id`、`tool_use_id`、subtype 字段及 response 外层形状；随后先修正
  设计文档/实现计划，并判断 T20 是否需要兼容修复。当前未改运行时代码，未执行
  format/analyze/test，T22 保持「待做」。
- 用户授权后在空临时目录对本机 Claude Code CLI 2.1.224 做了最小、脱敏、非破坏性复采；使用
  Plan 模式、`haiku`、`--no-session-persistence`，没有保留 prompt、回复正文、完整路径、原始
  payload 或真实 id，临时目录已删除。有效样本确认：收到 `system.init`；
  `ExitPlanMode.input.plan` 本次非空；随后收到与该 tool id 匹配的 `tool_result(is_error=true)` 和
  `result.success`；整个回合**没有任何 `control_request`**。
- 进一步对照官方 Claude Agent SDK 当前源码确认：配置 `can_use_tool` callback 时，SDK 会自动给
  CLI 增加 `--permission-prompt-tool stdio`，并以 `request.subtype + tool_use_id` 接收
  `can_use_tool`，再用嵌套的 `control_response.response` 封包回写。当前
  `ClaudeCodeProcessStarter` 没有该启动参数，T20 handler 又使用 `request.type` 和扁平响应，说明
  已完成的 T20 目前只被 fake wire 测试证明，真实 CLI 路径不会进入 handler。
- 因此新增止损点：不能直接进入 T22。需要用户确认是否先插入 T20 修复步骤，补
  `--permission-prompt-tool stdio`、修正/兼容 control wire，并再做一次最终脱敏取样冻结本机
  2.1.224 的实际 `request_id ↔ tool_use_id` 关联。确认前不改运行时代码。

## T20.1 · 真实 CLI wire 修复（完成）

- 用户确认先修复 T20 真实 wire，再继续 T22；未修改共享适配层或任何 Provider 端口。
- 启动参数默认加入 `--permission-prompt-tool stdio`；stream-json user 帧补齐
  `session_id` 与 `parent_tool_use_id: null`。
- `can_use_tool` 改按 `request.subtype` 解码并强制校验 `tool_use_id`、`tool_name`、
  `input`；四种决策与 fail-closed 错误统一使用 SDK 的嵌套
  `control_response.response{subtype,request_id,response}` 形状。
- 在空临时目录对本机 Claude Code CLI 2.1.224 做最终脱敏复采：`system.init` →
  `ExitPlanMode` tool_use（`input` 键为 `plan` / `planFilePath`，plan 非空）→
  `can_use_tool` control request；其 `tool_use_id` 与 tool id 相等，且 tool_use 先到。
  嵌套 deny 被 CLI 接受，随后出现匹配的 `tool_result(is_error=true)` 与
  `result.success`。无 JSON 解析错误、无 stderr、无超时；临时目录已删除，未保留
  prompt、回复正文、完整路径、原始 payload 或真实 id。
- 同步修正 Provider 设计文档与契约缺口记录，T22 明确以 `tool_use_id` 配对、以独立
  `request_id` 回写，不再猜测两者等价。
- 验证：`dart format .`（469 files，0 changed）；`flutter analyze`（No issues）；
  相关 7 个测试文件共 52 项全绿（starter、handler、Provider、permission adapter、
  default factory、共享层纯度、permission runtime 架构契约）。

## T22 · Plan 审批（完成）

- 新增 Claude Code 自有 `ClaudeCodePlanApprovalAdapter`，复用现有中立
  `AgentPlanApprovalProvider` / `AgentPlanApprovalRequest`，没有修改共享层或 Provider
  端口。Plan 与普通工具权限各自持有独立 pending registry。
- mapper 遇到 `ExitPlanMode` 时仍通过 Provider identity 建立工具边界，但不输出
  `AgentToolCallEvent`；登记 tool id 后，同 id 的 `tool_result` 也被抑制，时间线不会出现
  无头完成卡。
- adapter 按 live 已冻结顺序把后续 `can_use_tool.request.tool_use_id` 与 tool id 精确
  配对；领域 request id 使用 tool id，回写只使用独立 control request id。未匹配、坏形状和
  冲突重复请求都在 Plan 路径 fail-closed，不会降级成权限卡。
- plan 正文优先取 control/tool input 的非空 `plan`；兼容早期真实 `{}` 样本，仅在当前
  turn 内回退到最近 assistant 文本，再无内容则为空。plan、planFilePath 和 control raw
  不写日志或持久化 metadata。
- accepted → 嵌套 allow + 原样 `updatedInput`；rejected → 嵌套 deny + 修改理由；
  cancelled → deny 后 interrupt 当前 turn。Provider 现已实现既有
  `AgentPlanApprovalProvider`，capability 仍按计划留给 T24 打开。
- 更新脱敏 fixture 为 tool_use → 同 tool id control_request → tool_result，并覆盖：只产生
  一个 Plan 审批、零工具卡、两种 id 不混用、allow/deny wire、取消语义，以及普通权限路由
  不受影响。
- 验证：`dart format .`（471 files，0 changed）；`flutter analyze`（No issues）；
  直接相关 3 个测试文件 21 项全绿；完整 `flutter test` 1559 passed、1 skipped。共享适配层
  working tree diff 为空。

## T23 · Plan 执行交接（阻塞已解除，拆为 T23.1–T23.4）

- 触发止损条件「方案和实际代码对不上」与「改动量明显超出预估」，且计划正文与 G5
  硬门禁冲突。本步已停止，未修改运行时代码，未执行本步 format/analyze/test。
- 计划要求新回合默认权限为 `:accept-edits`；但权威规则要求 Plan 执行交接保持保守默认，
  不得自动授权修改。Claude Code codec 明确定义该选项会自动允许编辑类工具，因此不能按
  当前 T23 文案实现。
- 现有 `AgentPlanExecutionHandoffController` 只创建/清除本地 pending request，不负责发送
  回合或冻结权限。真正的 `startPlanExecution` 位于
  `AgentConversationViewModel`：它选择 Default 后调用普通 `sendMessage`；后者始终从当前
  permission state 取快照，没有本地交接专用的一次性 override。故仅改计划列出的
  controller/test 无法满足验收条件。
- `AgentPermissionOption.id` 对 application/presentation 明确是不透明的；共享层不能硬编码或
  解析 `:accept-edits`。若保留“默认自动选 Accept edits”，必须扩展中立权限语义或端口，已超出
  原 T23 范围，并可能触发用户要求的 Provider 端口止损条件。
- Claude 当前把 `--permission-mode plan` 暴露为权限选项，`supportsModeSelection` 仍为 false；
  现有本地执行交接只会在中立 Plan conversation mode 的成功终态触发。因此 T22 的
  Provider Plan 审批与通用终态交接之间目前没有连接，不能靠补 controller 单测宣称跑通。
- 建议安全修订：保持 Provider Plan 审批与本地执行确认两次动作；新执行回合使用权限目录
  声明的保守 default（Claude 当前解析为 `:ask`），而不是自动选择 Accept edits；允许用户在
  执行前显式改选。实现范围需扩到 ViewModel/权限快照编排、Claude 的 Plan 状态投影与对应
  集成测试，但不修改共享适配层或 Provider 端口。等待用户确认后先改 T23 计划再实施。
- 用户确认采用进一步修订：执行权限默认恢复“进入 Plan 前的权限”，把 Plan 视为临时覆盖；
  只有同 Binding/thread/runtime generation 且选项仍存在、allowed 时才沿用。上下文失效时回落
  到权限目录声明的保守默认；目录不可用时禁止静默执行，要求用户显式选择。
- 修订不解析不透明 optionId，不硬编码 `:ask` / `:accept-edits`，不新增 Provider 端口，也不修改
  共享适配层。Provider Plan 审批与本地执行继续保持两次动作：先通过现有 planApproval 端口让
  当前 Plan turn 正常收尾；仅在成功终态后显示本地执行卡；第二次点击才新建 Default turn。
- 为避免改变 Grok“审批后由 Provider 自行继续”的现有语义，Plan 审批请求将增加中立的本地交接
  语义声明；application 只按该语义分流，不按 Provider id/kind/显示名分支。T23 已拆为权限冻结、
  执行卡重校验、Claude turn 快照应用、G5 隔离收口四个逐步验收任务。

## T23.1 · Plan 前权限冻结 + 两段式交接候选（完成）

- `AgentConversationPermissionSelectionController` 记录最近一次成功用户选择的中立前后值及
  Binding thread/runtime identity；只有当前值、thread 和 generation 全部仍匹配时才暴露前值。
  optionId 始终不透明，apply 失败、重新绑定或跨 thread 查询均不会泄漏陈旧选择。
- `AgentPlanApprovalRequest` 新增默认 `providerManaged` 的中立 continuation；Claude adapter 明确
  投影为 `localExecutionHandoff`，Grok 和其他 Provider 不改代码即保持原有自行继续语义。
- accepted 的本地续接只暂存计划、Plan 前权限和 runtime 门闩，不立即发消息；同 turn 成功终态
  才创建 `AgentPlanExecutionRequest`。失败终态、拒绝、取消、审批端口缺失或响应异常都会丢弃
  暂存；本步没有调用权限审批端口、没有 steer，也没有启动新的执行 turn。
- 验证：`dart format .`（471 files，7 changed）；`flutter analyze`（No issues）；权限 controller、
  handoff controller、ViewModel、Claude Plan adapter 四个相关测试文件共 126 项全绿；共享适配层
  working tree diff 为空。

## T23.2 · 执行卡权限重校验 + 显式覆盖（完成）

- `AgentPlanExecutionRequest` 增加非持久化的一次性权限 choice，并区分 Plan 前、catalog 保守默认、
  用户仅本次覆盖和 Provider fallback 四种来源；生成的 turn 快照使用
  `localWorkflowOverride`，不会冒充 thread effective，也不会回写默认偏好。
- handoff controller 在卡片创建、权限目录变化和点击执行三个时点校验 option 是否仍存在且
  `allowed`。Plan 前种子仅在同 thread/runtime generation 有效；失效后只信任 adapter 声明的
  catalog default。Provider 有权限端口但目录无可用默认时保留卡片并禁用执行；没有权限端口的
  Provider 沿用自身 fallback，避免伪造不存在的选择能力。
- 执行卡新增独立权限行，明确显示“Plan 前 / 保守默认 / 仅本次 / Provider 默认”；用户改选只
  更新本地 handoff，不调用 `applyPermissionSelection`。执行前再次重校验，通过后才把一次性快照
  交给新回合；卡片继续声明不会预授权具体命令、文件或网络。
- 验证：最终一轮 `dart format .`（471 files，1 changed）；`flutter analyze`（No issues）；handoff、
  ViewModel、Plan 卡、Composer toolbar、UI state 五个相关测试文件共 128 项全绿。新增 widget 测试
  覆盖可见权限来源、仅本次改选零 apply、无可用目录禁用执行；共享适配层 diff 为空。

## T23.3 · Claude 按 turn 权限快照恢复同 session（完成）

- ViewModel 的本地“执行”路径把卡片已重校验的 choice 转为 `localWorkflowOverride` 快照，选择
  Default 后调用普通 `sendMessage` 新建回合；没有调用 steer、权限审批或 Provider planApproval。
- `ClaudeCodeAgentProvider.sendMessage` 在 turn admission 内先校验 session、无 running turn 和非空
  prompt，再读取 `configuration.permissionSnapshot`。目标 mode 与当前 Plan 不同时，复用 T21 的
  peer 切换事务：关闭旧 peer、以同 session `--resume` + 目标 `--permission-mode` 启动新 peer，
  最后才登记 turn 并把 user frame 发给新 peer。
- turn admission 与外部权限切换继续共用互斥门闩；陈旧 session、已有 running turn、pending 审批
  或重启失败均 fail-closed。重启失败仍按原逻辑 best-effort 恢复旧 mode，不会先发送执行 prompt。
- 验证：`dart format .`（471 files，1 changed）；`flutter analyze`（No issues）；Claude Provider、
  permission adapter、ViewModel、Plan 卡四个相关测试文件共 110 项全绿。新增 fake process 测试逐字段
  断言 Plan peer 零 user frame、同 session resume 到 `default` 后新 peer 才收到 prompt，并覆盖
  running/stale session 拒绝；共享适配层 diff 为空。

## T23.4 · G5 隔离回归 + 文档收口（完成）

- 补强四类交互的隔离断言：权限请求只接受权限 decision，Provider Plan 请求只接受 Plan
  decision；本地执行卡既不写权限/Plan decision registry，也不调用权限 apply 或 turn steer。
  三条路径继续使用各自的 request/decision 与回写入口，互相不可见。
- 同步权威规则、架构总览、工程规范、开发者指南、中英文术语表、中英文贡献指南、Claude
  Provider 提案及 changelog。现行文档统一表述为：优先恢复同 Binding/thread/runtime generation
  下仍有效的 Plan 前权限；失效时回落到 Provider 权限目录声明的保守默认；无可用选项时要求用户
  显式选择；本地覆盖只作用于这一个新 Default turn，不持久化、不提前 apply，也不预授权具体
  命令、文件或网络操作。
- 验证：`dart format .`（471 files，2 changed）；`flutter analyze`（No issues）；权限快照、handoff、
  Claude Provider、ViewModel、Plan 卡、pending interaction 与架构守卫等 13 个相关测试文件共
  184 项全绿；完整 `flutter test` 1571 passed、1 skipped。共享适配层 working tree diff 为空，
  `git diff --check` 无错误，现行文档中旧的强制 `:accept-edits` 表述扫描为空。

## T24 · capability 与 bundle 端口一致性（阻塞已解除并完成）

- 触发止损条件「方案和实际代码对不上」：T24 要求 T03/T18 架构守卫继续通过，但当前
  `claude_code_shared_layer_purity_test.dart` 冻结的
  `agent_conversation_timeline_store.dart` 基线为 68,946 bytes，当前 Git blob 与工作区文件均为
  66,936 bytes；相关测试在 byteLength 断言失败。本步没有修改该共享层文件，`git diff` 为空。
- 该基线由提交 `0426f57` 引入；同一提交中的 timeline store blob 已经是 66,936 bytes，说明冻结值
  从写入 Git 起就与仓库内容不一致。差值恰好为 2,010 bytes，等于冻结的 2,010 行，疑似基线按
  CRLF 工作区内容计算、Git blob 后续按 LF 规范化，属于架构守卫本身的跨行尾问题。
- 已完成但尚未标记完成的 T24 代码：Claude Code 静态 capability 打开
  `supportsPermissionRequests` / `supportsPlanApproval`，并新增 Claude fake provider bundle 测试，
  断言 `interactions` / `permissionPolicy` / `planApproval` 自动挂载；未修改
  `agent_provider_bundle.dart`，也未新增 Provider 端口。
- 验证结果：`dart format .` 完成（470 files，1 changed）；`flutter analyze` 通过；capability 与
  bundle 测试通过；架构守卫前三条通过，T18 内容基线在 timeline store byteLength 处失败。因此
  T24 保持「待做」，等待确认是把基线修正为当前 LF 内容，还是把守卫改成先统一行尾再计算基线。
- 用户确认采用建议方案后，架构守卫改为先把 CRLF / CR 统一成 LF，再计算规范化 byteLength 与
  FNV-1a 指纹；补充三种行尾输入得到同一字节序列的回归测试，并把 timeline store 基线修正为
  Git blob 的 LF 内容。该修复只改测试尺子，没有修改任何共享适配层源码。
- 最终验证：`dart format .`（470 files，1 changed）；`flutter analyze`（No issues）；capability、
  bundle、共享层纯度 3 个相关测试文件共 18 项全绿。`agent_provider_bundle.dart` 未新增 kind 分支，
  G1 五个共享 application 文件 working tree diff 为空，T24 标记完成。

## T25 · 本地历史列表 reader（阻塞）

- 触发止损条件「方案和实际代码对不上」：T25 计划与 Provider 设计文档 §4.8 都要求 reader 输出
  `AgentThreadDescriptor` 列表，但当前 domain 中不存在 `AgentThreadDescriptor`；现行 thread 目录
  端口 `AgentProvider.listThreads` / `AgentThreadCatalogPort.listThreads` 的返回类型是
  `AgentThreadPage`，其中条目类型为 `AgentThreadSummary`。
- 现有 Grok 本地历史 reader 也直接输出 `AgentThreadPage<AgentThreadSummary>` 语义。若 Claude reader
  沿用现有中立契约，可只新增 Provider 自有 data reader 与测试，不需要修改共享层或 Provider 端口；
  若坚持新增 `AgentThreadDescriptor`，则需要扩展 domain / Provider 端口，触发另一条明确止损条件。
- 本轮只完成了只读核对，尚未修改 T25 运行时代码，也未执行 T25 的 format / analyze / 测试；T25
  保持「待做」，等待确认是否把计划与设计文档中的 `AgentThreadDescriptor` 更正为
  `AgentThreadSummary`（由 `AgentThreadPage` 承载）。
- 用户确认沿用现有中立契约后，计划与 Provider 设计文档已同步修正为
  `AgentThreadPage` / `AgentThreadSummary`；没有新增 domain 类型或修改 Provider 端口。
- 新增只读 `ClaudeCodeSessionHistoryReader`：按冻结规则把 `/`、`\\`、`:` 替换为 `-`，只定位
  当前项目目录；每个 JSONL 仅采样有限头尾字节与行数，坏 JSON 行计数后跳过。摘要只保留 session、
  标题、时间、模型、采样消息数等白名单字段，不把 prompt / 回复写入 raw。
- reader 支持 recency 排序、offset cursor 分页和标题搜索；归档查询保守返回空。目录遍历关闭符号
  链接跟随，整个读取路径没有 create / write / delete 操作。
- 验证：最终 `dart format .`（472 files，0 changed）；`flutter analyze`（No issues）；reader 与
  共享层纯度 2 个相关测试文件共 9 项全绿。测试冻结 Unix/Windows 编码、损坏行继续解析且计数、
  目录 mtime 不变、跨项目隔离、分页顺序与大文件头尾窗口；T25 标记完成。

## T26 · history/live 事件对齐（完成）

- 对本机 Claude Code 历史做了只读结构复核，只输出字段名、类型和计数，没有输出正文、路径、
  session/turn id 或原始 payload。确认磁盘帧以 camelCase `sessionId` 为主，assistant 的
  `message.stop_reason` 表达 `tool_use` / `end_turn`，没有 live stream 的独立 `result` 帧；工具块与
  usage 字段仍可正规化到现有 mapper。
- `ClaudeCodeSessionHistoryReader` 的完整读取路径每次新建独立
  `ClaudeCodeStreamIdentity`、`ClaudeCodeEventMapper` 和纯同步 history reducer。磁盘帧先在
  Claude 自有 data 层正规化，再交给现有 mapper；`end_turn` 在 Provider 自有路径合成中立终态，
  不修改共享 pipeline / TimelineStore。
- user 正文从历史帧重建为中立 user message 事件，但不进入 Provider identity 的 message segment
  计数，保持 assistant/reasoning/tool entryId 与 live 路径一致。assistant 多次调用的 usage 按 turn
  累加，继续标记 `isSessionCumulative: false`。
- 完整历史只接受当前 Claude home、project 编码目录与 `<threadId>.jsonl` 精确匹配的文件；外部
  `sessionPath` fail-closed，不跟随到用户工作区其他文件。输出 snapshot 的 raw 仅含来源白名单。
- 新增 parity 测试：同一 `hello_turn.jsonl` 经 live 与 history 路径后，过滤本地重建的 user 事件，
  其事件类型、entryId/turnId、status/role/kind 逐位置完全一致；两个 identity 实例不同，history
  读取前后 live snapshot 不变，history 自身 snapshot 与 live 的规范化计数一致。
- 另覆盖真实磁盘形状的 tool_use → tool_result → end_turn：无 result 帧时仍得到 completed turn，
  工具原地收尾、时间/模型/cwd 正确，本回合 input/output/cache usage 正确聚合。
- 验证：最终 `dart format .`（473 files，0 changed）；`flutter analyze`（No issues）；reader、parity、
  mapper、identity、共享层纯度 5 个相关测试文件共 32 项全绿；`git diff --check` 通过，共享层五文件
  diff 为空。T26 标记完成。

## T27 · hidden list 持久化（完成）

- 新增 Claude 自有 hidden thread store：文件实现只接收 app 组合层注入的
  `~/.zeta/state/claude_code/hidden_threads.json` 具体路径，使用原子文本文件写入；版本 1 仅持久化
  `version` 与 `hiddenThreadKeys` 白名单，不保存 prompt、回复、session 路径或 Provider raw。
- 解码对缺文件、缺字段、损坏 JSON、未知版本和损坏条目均宽容回退为空；版本 0 的
  `hiddenThreads` 可迁移读取。内存实现用于测试和未注入文件存储的场景。
- `ClaudeCodeSessionHistoryReader` 在当前项目列表完成后按 project + thread 稳定键过滤隐藏项；
  `removeThreadFromList` 串行更新 Zeta hidden store。Claude Provider 复用现有
  `AgentLocalThreadListProvider` / bundle 端口，没有新增或修改 Provider 端口。
- app 组合层统一注入 hidden store；Claude capability 仅先开放
  `canRemoveThreadFromList`，列表/历史能力仍按计划留给 T28，避免入口早于完整接线出现。
- 会话移除确认文案改为 Provider 无关说明，明确只移除 Zeta 本地索引、Provider 历史文件仍保留；
  组件测试直接注入“可本地移除、不可远端删除”的中立能力，未提前打开 T28 的 Claude 列表入口。
- 只读保护测试记录 Claude JSONL 内容、文件 mtime 与目录 mtime，执行隐藏后逐项断言完全不变，
  并确认 Claude project 目录没有新增文件；唯一新增文件位于独立 Zeta state 目录。
- 验证：最终 `dart format .`（475 files，1 changed）；`flutter analyze`（No issues）；hidden store、
  reader、factory、capability、共享层纯度 5 个测试文件共 22 项全绿；项目线程 Widget 测试 13 项
  全绿。共享适配层未修改，T27 标记完成。

## T28 · 历史列表、读取与 resume 接线（完成）

- `--resume <session-id>` 的稳定参数拼装已由 T10 实现，并在 T21/T23.3 用于权限切换；本步复用
  既有实现，没有重复改写 starter。process starter 的 8 项测试继续逐字段覆盖 resume 优先于
  session-id、模型和 permission mode 的顺序。
- `ClaudeCodeAgentProvider.resumeSession` 现在校验非空 session/projectPath，应用请求级权限快照，
  绑定 session decision store，并以同 session 的 `--resume` 在项目 cwd 启动 peer；列表与完整历史
  分别委托 T25/T26 的 reader，Provider raw 仍只保留 cwd 白名单。
- mapper 按 `AgentRuntimeScope`（含 connection epoch）记录首个 `system.init` 握手。CLI 返回的
  session id 与启动预期不符时只产出 `AgentErrorEvent(code=claudeCodeSessionMismatch)`，不产出
  `AgentSessionStartedEvent`，错误文案不包含实际/预期 id；握手拒绝后同 runtime 的迟到 init 不会
  被重新接受。
- 同一 peer 每个 turn 重发的同 session `system.init` 现在幂等丢弃，不再制造重复会话边界；同一
  runtime 后续突然切换 session 则继续 fail-closed 为协议错误。peer 重启使用新 connection epoch，
  不与旧握手状态串味。
- 打开 Claude 的 `canListThreads` / `canReadHistory` / `canResumeSession`，bundle 自动挂载既有
  thread catalog；没有修改 `AgentProviderBundle` 或任何 Provider 端口，也没有修改共享事件层。
- 验证：`dart format .`（475 files，3 changed）；`flutter analyze`（No issues）；process starter、
  mapper、Provider、reader/parity、factory、capability/bundle、共享层纯度 9 个测试文件共 57 项
  全绿；项目线程 controller/widget 与 ViewModel 共 139 项全绿；完整 `flutter test` 为
  1588 passed、1 skipped。真实 macOS GUI + Claude CLI 的“重启后可见并点开恢复”本轮未手工执行，
  自动化 fake Process/Widget 测试不替代平台验收。

## T29 · Agent 管理三阶段检测（阻塞已解除，完成）

- 触发止损条件「方案和实际代码对不上」，已停止，未修改 T29 运行时代码，也未执行 T29 的
  format / analyze / 测试。
- 计划要求 detect 的第四阶段执行短生命周期握手：发送 ping user message，并等待
  `system.init + result`；但 T02 的真实 CLI 取样已确认，`system.init` 是每个 turn 开头产生的帧，
  `result` 是模型回合终态。也就是说，要满足这项握手就必须发送真实用户回合，Claude CLI 必然
  访问网络/模型，并可能产生套餐用量。
- 同一 T29 完成判据又明确要求“detect 阶段不发起任何网络请求”。在当前 stream-json 协议下，
  自动 detect 无法同时满足“拿到 init + result”和“零网络/零模型调用”；这不是 fake Process
  可以证明成立的语义。
- 建议修订：自动 detect 只做 `--version` 可调用性、凭证文件 stat/mtime、版本解析和日志定位，
  runtime 显示 `notRunning`；只有用户显式点击“测试连接”时，`testConnection` 才以 20s 上限发送
  最小无破坏 prompt，并在 UI 明示会连接 Claude 服务。这样 detect 保持零网络，连接测试仍能覆盖
  真实 `system.init + result`。
- T29 保持「待做」，等待用户确认是否按上述拆分修改计划后实施。
- 用户确认采用建议拆分：自动 detect 改为安装/账号/版本与日志定位，不启动 stream-json peer、
  不发送 prompt；短生命周期 `system.init + result` 握手只由用户显式点击“测试连接”触发，20s
  超时。阻塞解除，计划已同步修订，开始实施。
- `ClaudeCodeAgentManagementRepository.detect` 已收敛为三阶段本地检测：只执行
  `claude --version`、对凭证/配置做 stat + mtime，并枚举日志路径。文件系统窄接口没有
  内容读取方法，detect 也不会调用 connection probe。
- 未安装、已安装但未登录、已登录三态已有独立用例；fake 文件系统逐次记录
  stat/list 操作，测试断言无 read，detect 期间 connection probe 调用数为 0。
- 用户显式测试连接时，UI 先说明会连接 Claude 服务并可能产生少量模型用量；
  取消不调用 repository，确认后才发送固定最小消息。握手上限 20s，且只有同 session
  的 `system.init` 之后收到成功 `result` 才视为通过。
- 本步没有修改共享适配层，也没有改动 Provider 端口签名；只把旧的“无计费连接测试”
  注释更正为 Provider 中立的“显式连接测试”。
- 验证：`dart format .`（476 files，0 changed）；`flutter analyze`（No issues）；
  Claude 管理 repository 5 项、管理页 5 项和相邻管理/transport 回归全绿；完整
  `flutter test` 为 1594 passed、1 skipped。为避免未经确认的模型用量，本步未自动
  运行真实 Claude 连接测试；自动化 fake 测试不替代真实账号验收。

## T30 · 静态模型目录（阻塞已解除，完成）

- 触发止损条件“方案和实际代码对不上”，已停止，未修改 T30 运行时代码。
- 计划的“涉及文件”只要求新增 `claude_code_model_catalog.dart` 及其测试，完成判据却同时
  要求“切换后 `--model` 参数正确”。实际 `ClaudeCodeAgentProvider.listModels()` 仍固定抛
  `UnsupportedError`，`updateModelSelection()` 是空实现，`_ensurePeer()` 只读不可变的
  `config.selectedModel ?? config.defaultModel`。
- 因此若只按计划新增目录并打开 `supportsModelSelection`，UI 会显示模型入口，但用户
  切换的模型不会进入 Claude CLI 启动参数，违反 G4 的“不得伪造能力”。
- 建议扩大 T30：同步修改 `claude_code_agent_provider.dart` 及其测试，保存中立
  `AgentModelSelection`；首次建 peer 时传入所选 model，已有 session 则在下一回合开始前按原
  session 安全重启/恢复 peer，再将新 model 传入 `--model`；运行中回合的切换只影响
  下一回合。这会超出原“只新增目录”的文件范围，需用户确认后再继续。
- 用户已确认按建议扩大 T30；阻塞解除。计划已补充 Provider 内存选择、下一回合
  安全恢复 peer 与 `--model` 参数的测试范围，开始实施。
- 新增无网络的 `ClaudeCodeModelCatalog`，固定包含 Opus 4.7、Sonnet 4.6、Haiku 4.5
  三个明确版本和 `opus` / `sonnet` / `haiku` 三个 CLI 短别名；只有稳定的
  `sonnet` 别名标记为默认。该目录不读取 Claude CLI 缓存，不发起网络请求。
- `ClaudeCodeAgentProvider.listModels()` 现在返回完整静态目录；
  `supportsModelSelection=true` 后 bundle 自动暴露既有中立 `modelCatalog` 端口，没有修改
  `AgentProvider` / `AgentProviderBundle` 端口签名。
- Provider 在内存中保留 `AgentModelSelection`：首次启动直接传给 `--model`；已有
  session 在下一回合准入前关闭旧 peer，以同 session `--resume` 并携带新 `--model`
  恢复。运行中切换只更新下一回合快照，不中断当前 turn；新 peer 启动失败时尝试
  恢复上一模型的 peer，不静默吞掉失败。
- 模型目录、Provider、process starter、capability/bundle、app 级目录缓存、模型偏好持久化和
  Composer 交互共 76 项相关测试全绿。其中 fake 进程逐项断言首次 `--model`、
  同 session `--resume --model`，以及运行中切换不重启当前进程。
- 验证：`dart format .`（478 files，0 changed）；`flutter analyze`（No issues）；完整
  `flutter test` 为 1600 passed、1 skipped。共享适配层和 Provider 端口无 diff。本轮
  未运行真实 Claude CLI 模型切换；自动化 fake Process 测试不替代真实账号验收。

## T31 · `/compact` 会话压缩（阻塞已解除，完成）

- `ClaudeCodeAgentProvider.compactThread()` 现在只接受当前 Provider 已绑定的
  session；空 id 或其他 session 会在写帧前拒绝，避免把压缩命令发到错误对话。
- Claude CLI 没有独立 compact 控制帧，因此 Provider 复用正常回合通道，精确写出
  `{"role":"user","content":[{"type":"text","text":"/compact"}]}`。这条路径继续执行
  同 session 单回合、权限、模型选择和 peer 恢复检查，没有旁路。
- 打开 `canCompactThread` 后，既有 bundle 自动暴露 `threadMutations`；没有修改
  `AgentProvider` / `AgentProviderBundle` 端口，也没有修改共享事件层。
- 验证：`dart format .`（478 files，1 changed）；`flutter analyze`（No issues）；
  Claude Provider、capability/bundle 与 ViewModel 4 个相关测试文件共 122 项全绿。
  真实 Claude CLI 的“压缩后继续对话”本轮未手工执行；fake Process 测试不替代平台验收。
- 进入 T32 核对用户可感知变化时，发现现有 presentation / ViewModel 没有任何
  `compactThread` 调用点，Composer 的斜线命令菜单也只有 Plan。手工输入 `/compact`
  在 T31 之前已会通过普通 `sendMessage` 发送，因此不是本步新增的可达能力。
- 这触发止损条件“方案和实际代码对不上”：仅实现 Provider 方法并打开
  `canCompactThread` 无法完成计划中的手工验收，也不应写成用户可感知的 CHANGELOG。
  已停止 T32，等待确认是扩大 T31 补 capability-gated 入口/交互链路，还是仅保留
  Provider 内部契约并修改验收口径。
- 用户已确认扩大 T31：Composer 斜线菜单新增受 `canCompactThread` 控制的 Compact
  入口，ViewModel 必须通过当前 Binding/session runtime 的中立 `threadMutations`
  调用当前 thread，不在 UI 按 Provider kind 分支，不修改共享适配层或 Provider 端口。
- 已补齐用户可达链路：Composer 输入 `/compact` 时显示 `Compact context`，仅在当前
  会话可写、空闲且 capability 开启时出现；选择后消费命令文本并关闭菜单。
- ViewModel 通过当前 `AgentConversationBinding` 惰性取得 session runtime，恢复并校验
  当前 thread 后调用中立 `threadMutations.compactThread`。Provider 会等本次压缩回合
  终态后再释放 Binding 活动租约，随后普通回合可继续复用原 session。
- 扩大范围后的验证：`dart format .`（478 files，1 changed）；`flutter analyze`
  （No issues）；Provider、ViewModel、Widget 等相关测试共 140 项全绿；完整
  `flutter test` 为 1605 passed、1 skipped。`git diff --check` 通过，共享适配层和
  Provider 端口无 diff。真实 Claude CLI 的压缩回合本轮未手工执行，fake Process
  测试不替代真实账号验收。

## T32 · 文档收口（阻塞已解除，完成）

- 已按计划内范围新增当前 stream-json 协议基线，并开始同步 `AGENTS.md`、架构总览、
  设计文档、贡献指南、文档索引、历史提案状态与 CHANGELOG；尚未执行本步
  format / analyze / 相关测试，也未把 T32 标记完成。
- 收口扫描发现计划文件清单之外仍有与实际代码冲突的现行文档：
  `docs/guides/developer_guide.md` 仍把 Agent 管理和 CLI 私有数据范围写成
  Codex/Grok（或 Codex/Grok/Cursor），`docs/product/troubleshooting.md` 与
  `troubleshooting.en.md` 仍声称 Cursor 退役后的可用回退只包含 Codex/Grok。
- 实际 catalog、factory 与管理侧已经包含 Claude Code，因此严格按 T32 原文件清单会留下
  自相矛盾的当前文档。这触发止损条件“方案和实际代码对不上”；已暂停，等待确认是否
  扩大 T32，把开发者指南和中英文故障排查一并同步。
- 用户已确认扩大 T32；计划已补入开发者指南和中英文故障排查，阻塞解除，继续实施。
- 扩大后的全局扫描又发现 `docs/architecture/engineering_standards.md` 仍以硬规则写着
  “当前活跃 Provider 只有 Codex 与 Grok”；`docs/history/project_memory.md` 也把同一内容
  声明为应长期维护的当前事实。`docs/history/cursor_agent_guide.md` 是历史材料，但其中
  “当前活跃”措辞会被误读为今天的状态。
- 工程规范是现行权威正文，不能与 `AGENTS.md` 和实际 catalog 冲突；但它和两份 history
  文件不在第一次确认扩大的文件清单内。这再次触发“方案和实际代码对不上”，已暂停，
  等待确认是否把工程规范、项目记忆同步为三 Provider，并把 Cursor 历史说明改成
  “退役当时”的历史口径。
- 用户已二次确认扩大 T32；计划已补入工程规范、项目记忆和 Cursor 历史说明，阻塞解除。
- 已完成当前协议基线、历史提案归档标识、架构/工程规范、开发者与贡献指南、故障排查、
  项目记忆、文档索引和 CHANGELOG 的一致性收口；Cursor 历史说明保留“退役完成时”的
  历史快照口径。
- 验证：`dart format .`（478 files，0 changed）；`flutter analyze`（No issues）；
  capability、bundle、factory、Cursor 回退、管理 controller 与 Claude 管理 repository
  相关测试共 27 项全绿；完成判据 grep 无残留，`git diff --check` 通过。

## T33 · 真实 CLI 冒烟（脚本完成，真机执行阻塞）

- 新增 `tool/smoke_claude_code_stream_json.py`：使用空临时只读 workspace、
  `--no-session-persistence`、Haiku 与 `--permission-mode default`；任何
  `control_request` 都回写 deny。脚本只输出 OS、架构、CLI 版本和结果四项，不输出
  prompt、回复、文件内容、session/turn id、原始帧、stderr 或可执行路径。
- 验证：`dart format .`（478 files，0 changed）；`flutter analyze`（No issues）；
  `--help` 参数/语法检查通过。
- 真实执行命令使用冻结基线 `--expected-version 2.1.224 --timeout 90`，当前环境返回：
  OS `Darwin`、架构 `x86_64`、CLI `unavailable`、结果 `BLOCKED (cli-not-found)`。
  未发起模型回合、未产生凭据访问或模型用量。
- 按计划“无设备或凭据时标记待执行/阻塞，不得推断通过”，T33 不标记完成；需在安装并
  登录 Claude Code CLI 的设备上重新执行后补录真实结果。
- 用户在 Darwin x86_64 真机执行冻结版本门禁：检测到 Claude Code CLI `2.1.227`，
  因参数要求 `--expected-version 2.1.224` 返回
  `FAIL (version-mismatch; expected=2.1.224)`。脚本在版本不匹配时会在模型调用前退出，
  因此这不是 stream-json 兼容性失败，也不能作为真实协议冒烟通过；需省略
  `--expected-version` 再跑一次兼容性诊断并记录实际结果。
- 用户随后省略版本门禁在同一 Darwin x86_64 / Claude Code CLI `2.1.227` 环境执行，
  返回 `FAIL (result-failed)`。按脚本状态机，这表示同 session 的 `system.init` 已匹配且
  已收到 terminal `result`，但 `subtype != success` 或 `is_error == true`；不是超时、进程
  未启动或 session mismatch。
- 当前四行隐私输出故意不包含 result 正文、原始错误或 stderr，尚无法区分账号/模型拒绝
  与 2.1.227 wire 行为变化。这触发止损条件“方案和实际代码对不上”；已停止 T34，等待
  确认是否扩大 T33：仅追加白名单结构诊断（result subtype、`is_error`、stop reason/error
  category，不含正文）并用 CLI 默认模型与 `haiku` 各跑一次隔离模型别名问题。
- 用户已确认扩大 T33，并确认本机已安装 Claude Code，可直接测试。允许最多执行两个
  最小模型回合；诊断仍须保持四行输出和白名单字段边界。
- 已按确认范围扩展四行输出：`Result` 仅追加白名单枚举/布尔/计数，CLI 默认模型通过
  “省略 `--model`”表达，不再把 `haiku` 当脚本隐式默认。`dart format .`
  （478 files，0 changed）、`flutter analyze`（No issues）和 `--help` 检查通过。
- Darwin x86_64 / CLI `2.1.227` 的两次真实结果完全相同：CLI 默认模型与显式
  `--model haiku` 均为 `subtype=success`、`is_error=true`、
  `stop_reason=stop_sequence`、`assistant_text=true`、`denied_controls=0`。
- 这表明 session init 和 result wire 均可解析，也排除了 `haiku` 别名拒绝；但 result
  仍明确标记错误。已用完用户确认的两个最小模型回合，未继续调用。当前无法在不分类
  result 正文的前提下区分账号/额度/网络等服务错误与 2.1.227 终态语义变化，T33 继续
  阻塞，T34 未开始。
- 用户随后在同一 Darwin x86_64 / Claude Code CLI `2.1.227` 环境提供最终真机结果：
  `PASS (init+assistant+result)`。该次运行满足四行脱敏输出和实际主链路完成判据；不包含
  prompt、回复、路径、session/turn id、stderr 或原始 payload。此前两次
  `is_error=true` 作为瞬时失败证据保留，不推断未提供的根因。
- 最终收口验证：`dart format .`（478 files，0 changed）；`flutter analyze`
  （No issues）；脚本 `--help` 通过；源文件只有 OS、架构、CLI 版本、Result 四处
  `print`；`git diff --check` 通过。T33 阻塞解除并完成。

## T34 · OAuth 凭据只读边界

- 新增 `ClaudeCodeOAuthCredentialsReader`：只读
  `~/.claude/.credentials.json` 的 `claudeAiOauth` 白名单字段，每次调用都重新
  读文件，不缓存凭据。文件缺失、空文件、JSON 损坏、字段不完整、过期
  或读取异常均返回 `null`，不向上抛出。
- 文件边界只暴露 `read`，读取器没有写文件、HTTP 或 token 刷新能力；
  `ClaudeCodeOAuthCredentials.toString()` 只输出 `hasCredentials` 布尔值，不含
  token 全文或前缀。凭据模型保持单向解码，不提供可写回的 `toJson`。
- 验证：`dart format .`（480 files，2 changed）；`flutter analyze`
  （No issues）；`claude_code_oauth_credentials_reader_test.dart` 8 项全绿；生产文件
  无 `POST` / HTTP / 写文件调用。

## T35 · Anthropic 只读 API 客户端

- 新增 `ClaudeCodeAnthropicApiClient`：模型目录与套餐用量都只发 GET，固定
  10 秒超时，每次调用在 `finally` 中强制关闭短生命周期 `HttpClient`。
  订阅 OAuth 使用 `Authorization: Bearer` 与 oauth beta 头；API Key 使用
  `x-api-key`，不会携带 oauth beta 头。
- 401、429、其他非 200、超时、网络异常、非法 JSON 或非 object JSON
  均返回 `null`，不重试。非 200 诊断回调的参数类型只有 status code
  整数，响应体和 token 没有进入日志的通路。
- 首次验证发现测试 fake 的可空字段断言未显式解包，仅修正测试
  空安全后重跑全部门禁。最终验证：`dart format .`（482 files，
  0 changed）；`flutter analyze`（No issues）；
  `claude_code_anthropic_api_client_test.dart` 6 项全绿。

## T36 · 动态模型目录（阻塞已解除，完成）

- 计划的文件范围只包含新 mapper、`claude_code_model_catalog.dart` 及其测试，
  但实际 `ClaudeCodeAgentProvider` 当前未实现
  `AgentRefreshableModelCatalogProvider`。应用级 `forceRefresh` 仅在 Provider 实例
  实现该现有可选接口时才会调用 `refreshModels`；只改计划列出的文件无法让
  动态刷新在真实应用路径生效。
- 这触发止损条件“方案和实际代码对不上”。已在 T36 动手前停止，未修改
  共享适配层或 Provider 端口。继续所需的最小扩大范围为：额外修改
  `claude_code_agent_provider.dart` 及其测试，让 Provider 实现已有的
  `AgentRefreshableModelCatalogProvider` 并将 `refreshModels` 转发给 Claude Code 自有目录。
  等待用户确认该文件范围扩大。
- 用户已确认按上述最小范围扩大；计划同步补入 Provider 与测试后继续实施。
- 新增纯函数 `claude_code_model_catalog_mapper.dart`：按服务端顺序映射 `id`、
  `display_name`、`max_input_tokens`，只在 `raw` 中保留声明过的完整
  `capabilities` 节点；损坏条目跳过，首个 Sonnet 模型标记为默认。
- `ClaudeCodeModelCatalog` 现在动态优先并维护 Provider 实例内缓存；普通读取命中缓存，
  `refreshModels` 绕过缓存。凭据缺失、远端返回空/损坏或 loader 抛错都回退原有 6 项
  静态目录且不向上抛异常。
- `claudeCode.accountDataEnrichment=false` 时，目录在读取凭据之前直接回退静态列表；
  fake 断言凭据读取和远端 API 调用均为 0。默认缺省值保持开启。
- `ClaudeCodeAgentProvider` 实现既有 `AgentRefreshableModelCatalogProvider` 并把强制刷新
  转发到自有 catalog；没有新增或修改 Provider 端口，也没有修改共享事件适配层。
- 验证：`dart format .`（485 files，0 changed）；`flutter analyze`（No issues）；
  mapper/catalog、OAuth reader、Anthropic API client、Claude Provider、默认 factory、
  app 模型目录仓储、bundle 与共享层纯度守卫 9 个测试文件共 65 项全绿；
  完整 `flutter test` 退出码 0；`git diff --check` 通过。

## T37 · 套餐用量与账号数据增强开关（阻塞已解除，完成）

- 动手前核对触发止损条件“方案和实际代码对不上”，已停止，未修改 T37 运行时代码，
  也未执行 T37 的 format / analyze / 相关测试。
- T37 计划与历史设计 §4.3/§4.11 明确写着 `AgentUsageQuotaProvider`“不经 bundle，
  UI 直接对 Provider 做 `is` 判断”；但实际
  `lib/src/features/agent/domain/agent_provider_bundle.dart` 已有中立 `usageQuota` 端口，
  并在 Provider 实现 `AgentUsageQuotaProvider` 时自动挂载。
- 实际 `ProviderAgentUsagePanelRepository` 也接收并调用该 `usageQuota` 端口；因此继续按计划
  实现会绕开现有架构，而按实际代码实现又会违反当前计划文字。`AGENTS.md` G4 的 bundle
  端口清单同时漏写了实际存在的 `usageQuota`，与代码及 glossary 不一致。
- 建议采用实际已有的 Provider 中立路径：Claude Code 只实现现有
  `AgentUsageQuotaProvider`，由 bundle 自动挂载，**不修改 bundle 或 Provider 端口**；同步
  修正 T37 计划、历史设计和 `AGENTS.md` G4 端口清单后再实施 mapper、60 秒节流与详情页开关。
  等待用户确认该架构口径。
- 用户已确认采用上述中立 bundle 路由；T37 解除阻塞后继续实施。计划、历史设计与
  `AGENTS.md` 端口清单同步修正，`agent_provider_bundle.dart` 和 Provider 端口均未修改。
- 新增纯函数 `claude_code_usage_quota_mapper.dart`：映射五小时/周窗口、重置时间、
  `subscriptionType` 与 extra usage；损坏字段不补零，未知余额单位不推算币种或余额。
- 新增 `ClaudeCodeUsageQuotaAdapter`：账号增强关闭、API Key 模式、未登录、401/429
  折叠结果与 loader 异常均返回 `null`；成功和失败都按 60 秒内存节流，并发调用复用
  同一请求。适配器不刷新、不写回、不持久化 OAuth 凭据。
- `ClaudeCodeAgentProvider` 实现既有 `AgentUsageQuotaProvider`，由 bundle 自动挂载
  `usageQuota`。API Key 模式同时识别脱敏 `hasApiKey` 标志、Provider 环境与宿主环境，
  只判断值是否存在，不记录或持久化原文。
- Claude Code 详情页新增默认开启的「账号数据增强」开关，明确说明会瞬时读取本机
  `claude login` OAuth 凭据发起模型目录/套餐用量只读查询，不刷新、写回或持久化凭据。
  关闭时只持久化布尔 `false`；重新开启删除显式覆盖，恢复缺省开启语义。
- 开关参与模型目录安全指纹；变更会同时失效模型缓存并只重建 global runtime，已有
  session runtime 不终止。单测固定了 global 被替换、session 仍为同一实例的边界。
- 首轮相关测试只有 Widget 文案 finder 过宽；收窄后又发现测试把 fake-async 保存操作
  放入真实异步轮询造成自锁。改为通过控制器的可等待入口驱动更新后，开关默认值、
  回调存在、配置持久化与 UI 回显均通过，生产保存路径无死锁。
- 最终验证：`dart format .`（489 files，0 changed）；`flutter analyze`（No issues）；
  mapper、adapter、OAuth/API
  client、模型目录、Claude Provider、bundle、模型缓存、设置控制器、用量仓储、详情页
  Widget 与共享层纯度守卫共 12 个测试文件、94 项全绿；完整 `flutter test`
  退出码 0（109.9 秒）。

## 统一用量查询设计评审 · G7 边界冲突（阻塞，未实施）

- 在 T37 后续的统一套餐/Token 查询设计评审中发现，当前
  `GrokUsageStatisticsRepository` 会从配置或宿主环境解析 `GROK_HOME`，并默认回退到
  `~/.grok`，只读扫描 `sessions/**/updates.jsonl`；`docs/guides/developer_guide.md`
  的“使用统计开发约束”也明确要求这条路径。
- 但权威规则 `AGENTS.md` G7 同时规定不得读取 `~/.grok`，并声明唯一例外仅为 Codex
  使用统计只读扫描。现有代码、开发者文档与权威门禁互相矛盾，原拟“保持 Grok 行为并把
  scanner 搬入 Provider 自有 token source”的方案会原样继承该冲突。
- 已按“方案和实际代码对不上”止损：没有开始统一用量查询的业务代码、测试或生产装配
  修改。待用户确认二选一：
  1. 将 G7 扩展为用量/历史专用的精确只读白名单，并同步权威规则与长期文档；
  2. 保持 G7 现状，停止 Grok 私有历史扫描，另行设计只统计 Zeta 自有观测记录的方案，
     接受历史覆盖范围变化。
- 用户已于 2026-08-12 确认总体方案，并选择第三种、更宽的规则调整：删除原 G7
  私有数据读取禁令，允许 Provider data adapter 按功能读取其他 CLI 私有数据；原
  G8/G9 顺延为新 G7/G8。该授权只解决读取边界，不自动授权迁移、改写或删除。
- 统一用量任务已新增 T00 先同步权威规则、长期文档、提示词和编号引用；T00 全绿前
  不进入业务实现。
