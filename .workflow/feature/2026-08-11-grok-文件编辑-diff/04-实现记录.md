# 04 · 实现记录

任务：三 Provider 文件变更证据通用化（由 Grok 文件编辑展示缺口触发）

日期：2026-08-12

状态：R0 与步骤 1—15 已完成

## 2026-08-12 · 步骤 1 前置核验

### 核验结果

- 当前工作树无未提交改动；当前 `dev` 比 `origin/dev` 超前 7 个既有提交。
- 仓库 Codex app-server pin 为 `0.144.5`，本机 `codex --version` 为
  `codex-cli 0.144.1`，不能用于步骤 1 的 schema diff、真实证据或 smoke。
- 本机唯一发现的 Grok 可执行文件为 `~/.grok/bin/grok.exe`，其
  `grok --version` 输出为 `grok 1.0.0 (3cd0d0cbce)`，不能替代步骤 1
  指定的 Grok `0.2.119` 证据环境。
- 在任务目录、Codex 协议文档及已检查的本机常见缓存位置中，未发现可用的
  Codex `0.144.5` / Grok `0.2.119` 可执行环境。
- 在任务目录与相关协议文档中未发现覆盖“ACP target、Store 机械合并、
  Policy turn case 退役”的 G1 Issue 最终批准记录或链接。

### 停线决定

`03-任务拆解.md` 的步骤 1 和硬停线要求以精确版本实证固化 fixture，并在
取得 G1 最终批准后继续。当前缺少两个精确 CLI 环境及批准凭据，若使用现有
CLI、仓库 schema 或推测生成 fixture，会违反任务明确禁止的版本替代规则。

因此立即停线：步骤 1 在 `03-任务拆解.md` 中保持“待做”，未新增 fixture、
测试或生产代码，也未开始步骤 2。由于没有完成任何步骤且没有编辑 Dart
文件，本次未运行 `dart format .`、`flutter analyze` 或 `flutter test`。

### 恢复条件

1. 提供可调用的 Codex CLI `0.144.5` 环境或其明确可执行文件路径。
2. 提供可调用的 Grok CLI `0.2.119` 环境或其明确可执行文件路径。
3. 提供 G1 Issue 链接/批准渠道，以便在双 Provider 实证后取得并记录最终批准。

## 2026-08-12 · 用户授权恢复步骤 1

用户明确授权跳过 Codex CLI 与 Grok CLI 的精确版本限制。本次将使用本机
Codex `0.144.1` 与 Grok `1.0.0 (3cd0d0cbce)` 继续步骤 1，并遵守以下边界：

- fixture、测试与实现记录如实写实际版本，不沿用会造成误解的版本标签；
- 仓库 pin `0.144.5` schema 仍可作为仓库静态契约证据，但不会被描述成本机
  `0.144.1` 的真实运行证据；
- G1 Issue 最终批准仍是步骤 1 完成和步骤 4—6 开始前的独立硬门；
- 其余停线条件不变。

Codex 文档技能缓存已在线同步：本机匹配 schema 为 `0.144.1`，缓存可用；
官方最新 release 为 `0.147.0`，未自动升级。

## 2026-08-12 · 步骤 1 再次停线：实际 Codex 行为与方案不符

### 已完成的只读/临时环境核验

- `python tool/smoke_grok_acp.py --timeout 120`：4 项通过，0 失败；实际版本
  为 Grok `1.0.0 (3cd0d0cbce)`。
- `python tool/smoke_codex_app_server.py --timeout 120`：17 项通过，0 失败；
  实际版本为 Codex `0.144.1`。
- Codex 文件编辑采样使用临时 Git workspace、合成单行文件、
  `workspace-write` 沙箱和 ephemeral thread；采样只保留方法、item 类型、
  状态与计数，未保留 prompt、回复、命令、真实路径、正文、id 或 stderr。
- 最终受控采样中，临时文件确实完成预期替换；期间收到 3 次仅针对该合成
  文件的 `item/commandExecution/requestApproval` 并逐次批准。
- 同一次成功编辑中，`fileChange` 的 `item/started`、
  `item/fileChange/patchUpdated`、`item/completed`、`turn/diff/updated` 均为
  0 条；紧接着的 `thread/read` 中 `fileChange` item 也是 0 条。
- 采样产生的临时目录已全部清理。

### 与方案的冲突

`02-方案设计.md` 和步骤 1 把“Codex tool-scoped `fileChange` 是完整且可由
`thread/read` 恢复的唯一详情真源”作为后续 target、Store 机械合并及退役
turn aggregate 的前提。当前实际环境虽然成功编辑文件，却只观察到
`commandExecution`，无法证明该前提，也无法按计划生成真实
`item/started → patchUpdated → completed → turn/diff/updated` fixture。

这命中用户指定的“方案和实际代码对不上”停线条件，也命中步骤 1 自身的
“tool item 不是完整可恢复 snapshot 时停止并回修 01/02”判据。因此：

- 步骤 1 标记为“阻塞”，未生成或提交合成冒充真实的 Codex fileChange fixture；
- 未继续 Grok 文件编辑采样，未开始步骤 2 或任何生产代码改动；
- 因没有完成步骤且没有 Dart 改动，未运行 `dart format .`、
  `flutter analyze` 或 `flutter test`；仅在记录完成后运行 `git diff --check`。

### 需要用户拍板

需要在以下方向中明确选择后才能恢复：保留原方案并提供一份确实产生
tool-scoped `fileChange` 且可由 `thread/read` 恢复的 Codex 运行证据；或回修
`01/02/03`，重新定义 Codex 文件编辑的详情真源与迁移路径。不能由实现阶段
自行把 `commandExecution`、turn aggregate 或 pinned schema 合成 fixture 当作
真实 tool snapshot。

## 2026-08-12 · 用户要求改用当前真实环境并加入 Claude Code

用户明确要求：

- 使用当前 Codex 环境验证真实数据；
- 不再沿用与实际行为冲突的旧方案；
- 将 Claude Code 纳入当前目标；
- 针对当前目标重新设计方案。

本轮据此只进行临时环境采样、代码/文档核对和 workflow 方案重设。旧步骤 1 不再继续，
未修改生产 Dart 代码，也未把 schema fixture 冒充真实运行数据。

### 当前环境实证

#### Grok `1.0.0`

- 在临时 workspace 中对合成文件完成一次真实 Edit，文件内容确实变化，turn 以
  `end_turn` 结束。
- 同一 tool 的 `tool_call_update` 明确给出 `kind: edit`，以及
  `content[type=diff] {path, oldText, newText}`；完成 update 再次携带同一 diff。
- tool input 形状包含 `file_path/old_string/new_string`，后续 update 可出现
  `replace_all/variant`。
- 本次没有发生权限选择；该事实不用于推断一般情况下无需权限。
- 采样输出只保留字段名、类型、状态与脱敏占位，临时目录已清理。

结论：Grok 当前能提供 Provider 一手的替换前/后文本，适合映射为
`textReplacement`，但不能把它未经协议保证地称为完整文件快照。

#### Claude Code `2.1.227`

- `python tool/smoke_claude_code_stream_json.py --timeout 90` 通过
  `init + assistant + result` 基线。
- 真实 Edit：`assistant.tool_use.input` 含
  `file_path/old_string/new_string/replace_all`；成功 `user.tool_result` 只有普通字符串，
  未观察到结构化 diff，且可省略 `is_error`。
- 真实 Write：`assistant.tool_use.input` 含 `file_path/content`；成功
  `user.tool_result` 同样只有普通字符串。临时文件成功创建且非空。
- 两次采样均使用 `--no-session-persistence`、临时 workspace 和合成内容；只输出工具名、
  字段名、字符串长度、结果状态，不保留 prompt、回复、正文、真实路径、id 或 stderr。

结论：Claude 文件证据来自 tool_use，终态来自 tool_result。必须在 Claude data mapper 的
Provider-local tracker 中把证据带到终态；不能依赖共享 Store 从旧 rawInput 猜回。

#### Codex `0.144.1`

- 重新执行结构化事实采样，临时文件完成预期替换。
- 实际仍只有 `commandExecution` item；观察到 completed 与 failed 命令项，source 为
  `agent`，`commandActions` 为 `{command, type}`，本次 `type` 为 `unknown`。
- `fileChange` started/completed、`item/fileChange/patchUpdated` 和
  `turn/diff/updated` 均为 0。
- 审批请求带有 command、commandActions、cwd 等字段，但这些都不能可靠还原最终文件
  变化。本次只批准指向临时合成文件的安全操作。
- ephemeral thread 没有可用于证明历史可恢复性的 turn；不从该结果推断 thread/read
  能或不能恢复其他结构化路径。
- 临时目录已清理，记录未包含 prompt、回复、命令、路径、正文、id 或 stderr 原文。

结论：当前真实 Codex command-only 路径只能显示命令卡。把命令、审批参数或执行后的
工作区状态转换成文件 diff 都属于猜测。仓库 schema 的 fileChange/turn diff 只作为独立
兼容路径，不能再写成当前运行时必然行为。

### Codex 文档与 schema 核对

按 `codex-app-server-docs` skill 要求先同步了本地文档缓存：

- 本机安装版本：`0.144.1`；
- 本机 schema 与该版本匹配；
- 当前官方 release：`0.147.0`；
- 未升级 CLI；用户已授权跳过精确版本限制。

仓库 pin `0.144.5` schema 仍定义：

- `FileChangePatchUpdatedNotification {threadId, turnId, itemId, changes[]}`；
- `FileUpdateChange {path, kind, diff}`；
- update kind 可带 `move_path`；
- `TurnDiffUpdatedNotification {threadId, turnId, diff}`。

这些是静态兼容证据，不是上述 `0.144.1` command-only 采样中实际出现的事件。

## 2026-08-12 · R0 方案重设与再次停线

### 重新设计结果

已重写：

- `01-需求分析.md`：目标改为三 Provider 的中立“文件变更证据”契约；
- `02-方案设计.md`：加入 `textReplacement`、`writtenContent`、`unifiedPatch` 三种
  evidence，以及无证据降级；
- `03-任务拆解.md`：按 Grok → Claude → Codex producer、合同、UI 和清理重新排序；
- 本文件：保留旧停线经过，并记录当前真实证据和新的恢复条件。

新方案不修改 Provider port/capability，也不从工作区或命令补算 diff。Codex
`turn/diff/updated` 不再按旧方案直接退役，而是计划迁移为 tool evidence 缺失时的 typed
live-only fallback；当前 commandExecution 路径明确不产文件变更卡。

### 命中用户硬停线条件

重新核对现有代码后确认，最终要让 presentation 删除
`rawOutput['changes']/unified_diff` 等解析，并让 Claude tool_result/Grok status update 不丢
证据，至少需要：

1. 扩展中立 `AgentToolCall`，承载 `AgentFileChangeSnapshot`；
2. 修改共享 `AgentConversationTimelineStore`，机械携带该 typed 字段；
3. 把旧 `AgentTurnDiffEvent/TimelineEntry` 的字符串 payload 改为 typed turn snapshot。

这三项不需要 Provider 分支、命令解析、raw 推断或 Provider port 改动，但第 2—3 项仍属于
用户点名的“共享适配层”范围。因此按要求立即停线：

- R0 文档重设可以完成并验证；
- `03-任务拆解.md` 步骤 1 标为“阻塞：等待用户批准共享层范围”；
- 在用户明确批准前，不开始 Dart 生产代码，不创建共享层 patch；
- 若实现中发现还需修改 `acp_*`、CoalescingPolicy 语义、Pipeline/Dispatcher 或任何
  Provider port，将再次立即停线，不能把本次批准外推。

### 恢复条件

用户明确批准以下窄范围：

> 允许扩展 `AgentToolCall` 的中立 typed 文件变更字段，并让
> `AgentConversationTimelineStore` 与 typed turn entry 只做机械透传/原位替换；共享层不得
> 出现 Provider 分支、raw/命令解析、路径去重或 partial snapshot 推断；不得修改 Provider
> port。

批准后从 `03-任务拆解.md` 步骤 1 开始；每步完成后均执行 format、analyze、相关测试并
更新状态。

## 2026-08-12 · R0 验证结果

- `dart format .`：通过，检查 509 个文件，0 个文件发生格式变化。
- `flutter analyze`：通过，0 个问题。
- 相关测试：通过，共 145 项，0 失败。执行范围：
  - `test/src/features/agent/data/mappers/grok_session_update_mapper_test.dart`
  - `test/src/features/agent/data/datasources/claude_code/claude_code_event_mapper_test.dart`
  - `test/src/features/agent/data/datasources/app_server/codex_app_server_provider_test.dart`
- 当前环境 smoke/临时采样：Grok ACP 基线 4 项通过、Codex app-server 基线 17 项通过、
  Claude Code stream-json 基线通过；Grok Edit、Claude Edit/Write、Codex command-only
  实际结构均已按上文脱敏记录。
- `git diff --check`：通过，仅有 Git 提示工作区 Markdown 下次写入会按仓库配置转换为
  CRLF；无空白错误。

R0 状态已更新为“完成”。由于只修改 workflow Markdown，本步没有生产 Dart 改动，也没有
新增 fixture。下一步仍是“阻塞：等待用户批准 G1 共享层机械透传范围”。

## 2026-08-12 · 步骤 1 获得 G1 窄范围批准

用户明确确认开始执行，并再次要求每步完成后运行 format、analyze、相关测试和更新状态。
本次批准严格限定为：

- 可扩展 `AgentToolCall` 的 Provider-neutral typed 文件变更字段；
- 可让 `AgentConversationTimelineStore` 与 typed turn entry 机械透传/原位替换；
- 共享层不得出现 Provider 分支、raw/命令解析、路径去重或 partial snapshot 推断；
- 不得修改任何 Provider port；
- 若实现发现必须扩大上述共享范围、方案与实际代码不符，或单步明显超出预估，立即停线。

步骤 1 已从“阻塞”更新为“进行中”。本步只固化合成 sentinel fixture、来源/隐私合同测试
和批准记录，不修改生产逻辑。

## 2026-08-12 · 步骤 1 完成

### 实际改动

- 新增 `test/fixtures/agent_file_change_evidence/manifest.json`，固定“版本仅用于证据来源、
  不作为运行时硬门”，并声明真实形状与 schema 构造不得混标。
- 新增四份独立 fixture：
  - Grok `1.0.0` Edit real-shape；
  - Claude Code `2.1.227` Edit/Write real-shape；
  - Codex `0.144.1` command-only real-shape；
  - Codex `0.144.5` structured fileChange schema-constructed shape。
- 新增 `provider_file_change_fixture_contract_test.dart`，固定来源、关键字段、Codex
  command-only 零 fileChange 事实、schema-only 标签和隐私 sentinel 契约。

新增范围为 5 个 fixture 文件 + 1 个测试文件，符合步骤预估；未修改生产代码、共享层或
Provider port。

### 本步验证

- `dart format .`：通过，510 个文件中 1 个新测试文件被格式化。
- `flutter analyze`：通过，0 个问题。
- `flutter test test/src/features/agent/data/mappers/provider_file_change_fixture_contract_test.dart`：
  6 项通过，0 失败。
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。

步骤 1 状态已更新为“完成”。

## 2026-08-12 · 步骤 2 开始

核对现有日志链后确认：`AgentIgnoredMessageLogger.record()` 会把 `rawPayload ?? payload`
完整 JSON 编码到 trace 日志，`details` 也会接受任意字符串；文件 diff/old/new 一旦进入
ignored 路径，就可能经全局 logger 写入本地日志。

实际修复可保持在步骤预估内：

- 仅修改共享 data logging 工具，不改 Pipeline/Store/Provider port；
- 保留 `rawPayload` named 参数以兼容现有调用，但 logger 明确忽略它；
- 日志只输出安全 method/reason、payload 结构 presence/type/count、出现次数；
- `details` 只允许现有 `errorKind`、`updateKind` 两个键，值必须是安全协议标签；
- 用直接文件日志测试和 Grok provider 回归固定敏感 sentinel 不进入内存日志或磁盘。

未发现方案与实际代码冲突，步骤 2 已更新为“进行中”。

## 2026-08-12 · 步骤 2 完成

### 实现结果

- `AgentIgnoredMessageLogger` 不再序列化或输出 `rawPayload` / `payload` 正文；保留
  `rawPayload` named 参数只为兼容现有调用。
- ignored 诊断只输出经过约束的 method、reason、出现次数，以及 payload 的
  presence/type/count 结构摘要；`details` 只接受 `errorKind`、`updateKind`。
- method、reason、type/detail 中含控制字符或超长/非法格式时统一输出
  `<invalid>`，避免换行注入或正文混入协议标签。
- Grok 与 Codex 旧回归从“原始 JSON 可见”改为“安全摘要可见、正文不可见”；新增
  logger 直测同时验证内存日志与实际磁盘日志不含路径、patch、raw payload、任意
  detail、thread/item id 等 sentinel。

实际修改 1 个 production data/logging 文件、1 个新增测试文件，并调整 Grok、Codex
各 1 个已有回归文件。测试文件数比表格预估上限多 1 个，是因为 Codex 已有测试也
明确断言原始正文；仅翻转该测试的 5 条断言，没有新增生产范围，未构成明显超量。
未修改 Pipeline、TimelineStore、共享 ACP mapper 或任何 Provider port。

### 验证

- `dart format .`：通过，511 个文件，最终 0 个待格式化改动。
- `flutter analyze`：通过，0 个问题。
- `flutter test -r compact test/src/features/agent/data/agent_ignored_message_logger_test.dart test/src/features/agent/data/datasources/acp/grok_acp_provider_test.dart test/src/features/agent/data/datasources/app_server/codex_app_server_provider_test.dart test/src/core/logging/app_logging_test.dart`：
  175 项通过，0 失败。
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。

步骤 2 状态已更新为“完成”。

## 2026-08-12 · 步骤 3 开始

CodeGraph 核对确认 `AgentToolCall` 定义及 `copyWith` 都集中在
`agent_tool_models.dart`，现有构造点全部使用 named 参数；新增可选字段不会要求修改调用方。
方案中的模型形状与实际 domain 分层一致，可保持在 2 个 domain 文件和 1 个单测文件内：

- 新建纯中立 `agent_file_change_models.dart`，包含动作、可恢复性、三种 evidence、change
  与防御性不可变 snapshot；
- `agent_tool_models.dart` 只新增 `fileChanges` 字段及 preserve/replace/clear 复制语义，并
  转出新模型；
- 本步不接 Provider mapper、TimelineStore、事件管线或 UI，也不修改 Provider port。

未发现方案与实际代码冲突，步骤 3 已更新为“进行中”。

## 2026-08-12 · 步骤 3 完成

### 实现结果

- 新增纯 domain `agent_file_change_models.dart`：
  - `AgentFileChangeKind`：created/modified/deleted/moved/unknown；
  - `AgentFileChangeReplayability`：replayable/liveOnly；
  - `AgentTextReplacementEvidence`、`AgentWrittenContentEvidence`、
    `AgentUnifiedPatchEvidence` 三种 sealed evidence；
  - `AgentFileChange` 与防御性复制、不可修改的 `AgentFileChangeSnapshot`。
- 空 old/new/content/patch 保留为合法显式值；`evidence == null` 只表示结构化摘要；
  moved 目标路径与 unknown 动作均不靠正文推断。
- `AgentToolCall.fileChanges` 区分 `null`（无 snapshot）与空 snapshot（权威清空）；
  `copyWith` 支持默认保留、显式替换与 `clearFileChanges` 清除，清除优先。
- 新模型经 `agent_tool_models.dart` 转出，既有 `agent_models.dart` 统一入口无需增加第三个
  domain 修改文件。

实际范围为 2 个 domain 文件和 1 个单测文件，符合预估；未接 Provider mapper、Store、
事件管线、持久化或 UI，未修改任何 Provider port。

### 验证

- `dart format .`：通过，513 个文件中 2 个文件被格式化。
- `flutter analyze`：通过，0 个问题。
- `flutter test -r compact test/src/features/agent/domain/agent_file_change_models_test.dart test/src/features/agent/domain/agent_tool_call_title_test.dart`：
  16 项通过，0 失败。
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。

步骤 3 状态已更新为“完成”。

## 2026-08-12 · 步骤 4 开始

CodeGraph 核对了 `AgentTurnDiffEvent → CoalescingPolicy → Reducer → Mutation →
EventProcessor → TimelineStore → AgentTurnDiffTimelineEntry → renderer` 全链，以及 Store 内
所有 `AgentToolCall` 重建点。实际代码与拆解一致，但必须遵守既定迁移顺序：Codex mapper
到步骤 7 才切 typed producer，旧字符串 presentation 链到步骤 12 才原子删除。

因此本步采用计划内的兼容桥，不提前扩 Provider 范围：

- 新增 `AgentTurnFileChangesEvent/Mutation/Entry` typed 通道；legacy turn diff 通道暂留，
  供尚未迁移的 Codex producer 与现有 renderer 回归；
- typed Entry 只从中立 `AgentUnifiedPatchEvidence` 提供临时 renderer 兼容文本，身份、路径、
  动作和清空仍以 snapshot 为准；该兼容 getter 在步骤 12 随旧解析链删除；
- TimelineStore 对 tool snapshot 始终采用 incoming 值，`null` 清除、非空/空 snapshot
  原样替换，不保存 last-valid；
- typed turn snapshot 按既有 turn entry id 原位替换，空 `changes` 机械移除；
- Coalescing key、barrier 与 latest-wins 不变，不改 Pipeline 逻辑。

预计修改 6 个 domain/application 文件和 4 个既有测试文件，仍在步骤预估内；不修改
Provider mapper、共享 ACP mapper、presentation 生产文件或任何 Provider port。步骤 4 已更新
为“进行中”。

## 2026-08-12 · 步骤 4 完成

### 实现结果

- 新增独立 `AgentTurnFileChangesEvent`、
  `AgentUpsertTurnFileChangesTimelineMutation` 与
  `AgentTurnFileChangesTimelineEntry`；reducer 保持纯同步，仍只发布 liveTurn + immediate +
  auto-scroll。
- typed turn event 复用旧 turn diff 的 `sessionId + turnId + <turn>` coalescing key，仍是
  latest-wins、非 barrier；Pipeline 和 Buffer 机制没有改动。
- TimelineStore 对 tool `fileChanges` 始终机械采用 incoming：新建/替换原样携带，incoming
  `null` 会清除旧 snapshot，不为任何 Provider 保存 last-valid。
- typed turn snapshot 按既有 `turn-diff-$turnId` entry id 原位 upsert；空 `changes` 是
  权威清空并机械移除 entry，Store 不读 path/evidence/raw。
- 迁移期 typed turn entry 继承旧 entry，仅把中立 `AgentUnifiedPatchEvidence` 拼成临时
  renderer 输入；不从 patch 推断身份/动作。Codex mapper 仍留待步骤 7，旧 presentation
  解析链仍留待步骤 12 原子删除。
- 更新 G1 T18 冻结指纹前，先核对守卫文字与步骤 1 的用户批准记录；只更新本次获批的
  TimelineStore 与 CoalescingPolicy 基线，其他三个冻结共享文件保持不变。

实际修改 6 个 domain/application 文件和 5 个测试文件，共约 409 行净 diff，符合
5—8 个生产文件、3—5 个测试及约 450—700 行的估算量级。未修改 Provider mapper、共享
ACP mapper、Provider port 或 presentation 生产文件。

### 验证

- `dart format .`：通过，513 个文件中最终 1 个测试文件被格式化。
- `flutter analyze`：通过，0 个问题。
- `flutter test -r compact test/src/features/agent/application/agent_conversation_timeline_store_test.dart test/src/features/agent/application/agent_conversation_reducer_test.dart test/src/features/agent/application/coalescing_event_buffer_test.dart test/src/features/agent/application/agent_event_pipeline_test.dart test/src/features/agent/application/agent_event_storm_fixture_test.dart test/src/features/agent/architecture/claude_code_shared_layer_purity_test.dart test/src/features/agent/presentation/agent_timeline_grouping_test.dart`：
  125 项通过，0 失败。
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。
- G1 Provider 名称检查：本步新增共享代码无 Provider 名称、import、kind 或 raw 分支；
  TimelineStore 命中项均为既有注释基线。

步骤 4 状态已更新为“完成”。

## 2026-08-12 · 步骤 5 完成

### 实现结果

- 新增 Grok-owned `GrokToolContentProjection` / `GrokFileChangeTracker`，只在 Grok data
  层读取 ACP tool content：普通 content block 继续作为工具正文，diff block 映射为
  `AgentTextReplacementEvidence`，不再产生 `diff: <path>` 占位。
- tracker 按 runtime/session/turn/tool 隔离并输出完整累计 snapshot：重复 diff 复用同一
  change id/revision，status-only 与损坏 diff 携带 last-valid，证据或顺序变化才递增 revision；
  空 old/new 保持合法，非 bool `replace_all` 不被猜测。
- 动作只由明确 Grok tool kind 决定：已确认 edit 映射为 `modified`，缺失或未知语义保持
  `unknown`；change id 由 tool id、Provider 顺序和规范化路径 key 稳定生成。
- `GrokSessionUpdateMapper` 在 identity 接受事件后才调用 tracker，并在新 turn、取消/失败、
  session/runtime 失效与 dispose 时清理；成功终态保留到新 turn，以支持 identity 已允许的
  迟到同-tool terminal update 继续携带完整 snapshot。
- `GrokUpdatesHistoryParser` 每次 parse 仍创建独立 mapper/tracker；history tool merge 机械采用
  incoming 完整 snapshot。新增 real-shape mapper 回归和 live/history 逐字段一致性测试。

实际范围为 3 个 Grok data 文件和 3 个测试文件，约 700 行新增/调整，文件数符合预估且
行数与 400—650 的量级接近；未修改共享 `acp_*`、共享 application 层、presentation、
Provider port 或 capability。

### 验证

- `dart format .`：通过，515 个文件中 1 个测试文件被格式化。
- `flutter analyze`：通过，0 个问题。
- Grok tracker、mapper、provider、history parser、live/history canonical、history reader
  共 6 个测试文件：128 项通过，0 失败。
- `python tool/smoke_grok_acp.py --timeout 120`：当前 Grok `1.0.0`、Windows 11 环境下
  4 项通过，0 失败；按用户授权未设置精确版本门槛。
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。

步骤 5 状态已更新为“完成”。

## 2026-08-12 · 步骤 6 完成

### 实现结果

- 新增 Claude Code-owned `ClaudeCodeFileChangeTracker`，按 runtime/session/turn/tool use id
  隔离并保存完整工具投影；`Edit` 映射为 modified +
  `AgentTextReplacementEvidence`，`Write` 映射为 unknown +
  `AgentWrittenContentEvidence`，空 old/new/content 保持合法。
- `NotebookEdit` / `MultiEdit` 只输出路径级 unknown 摘要，不伪造证据；其他工具不产生
  file changes。相同证据复用 change id/revision，内容变化才递增 revision。
- `ClaudeCodeEventMapper` 在 `tool_use` 建立 snapshot，在关联的 `tool_result` 中带回同一
  title/kind/location/rawInput/fileChanges；成功、失败、取消、runtime 失效与 dispose 均通过
  mapper-owned 生命周期清理 tracker。
- `ClaudeCodeAgentProvider` 改为调用 mapper 的完成/失效入口，未把协议字段泄漏到
  application/domain，也未修改 Provider port 或 capability。
- history 每次读取继续使用独立 mapper/tracker；新增真实 Edit/Write 形状的 live/history
  逐字段一致性回归，并覆盖失败、清理、NotebookEdit/MultiEdit 与非文件工具。

实际范围为 3 个 Claude Code data 文件和 3 个测试文件，约 741 行新增/调整，落在
450—750 行预估内；未修改共享 ACP mapper、共享 application 层、presentation 或任何
Provider port。

### 验证

- `dart format .`：通过，517 个文件，0 个文件需调整。
- `flutter analyze`：通过，0 个问题。
- Claude tracker、event mapper、live/history parity、history reader、provider、stream identity
  共 6 个测试文件：59 项通过，0 失败。
- `python tool/smoke_claude_code_stream_json.py --timeout 90`：当前 Claude Code
  `2.1.227`、Windows AMD64 环境下 init/assistant/result 通过；按用户授权未设置精确版本门槛。
- 脱敏临时采样确认：Edit 输入键为 `file_path/old_string/new_string/replace_all`，Write 输入键为
  `file_path/content`；两者成功 `tool_result.content` 均为字符串且可按 tool use id 关联，文件
  实际写入已在自动删除的临时目录核验。Edit 的真实 CLI 前置条件是同回合先 Read 目标文件。
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。

步骤 6 状态已更新为“完成”。

## 2026-08-12 · 步骤 7 开始

已按 `codex-app-server-docs` 技能同步协议缓存：本机 Codex `0.144.1` 的 stable schema
匹配本机；官方最新 release 为 `0.147.0`。按用户授权不以精确版本差异阻断实现，也未升级
CLI。仓库 `gen_codex_schema.ps1 -Diff` 的默认版本门被跳过后，确认完整 0.144.1/0.144.5
schema 存在大量版本差异；针对本步骤单独核对后，以下关键结构保持一致：

- `FileUpdateChange` 仍为必填 `path/kind/diff`；kind 为 add/delete/update，update 可带
  `move_path`；
- `item/fileChange/patchUpdated` 仍提供 threadId/turnId/itemId/changes；
- `ThreadItem.fileChange` 仍提供 id/status/changes；
- `turn/diff/updated` 仍提供 threadId/turnId/diff，且 diff 是当前回合聚合快照。

CodeGraph 核对确认现有实时入口在 `_CodexNotificationMapper`，ThreadItem 映射集中于
`codex_app_server_helpers.dart`，`thread/read` history 复用同一 helper，provider 已持有 mapper
并具备 turn/runtime 生命周期入口。当前 commandExecution 与 fileChange 分支独立，可保持
command-only 路径无 snapshot。

计划采用 4 个 Codex data 文件和约 5 个定向测试：新增 Codex-local tracker，修改 notification
mapper、ThreadItem helper、thread/read history；provider 只补 tracker 生命周期清理。预计仍在
步骤 7 的文件数与 550—900 行量级内，不修改共享 application/ACP、Provider port、capability、
审批参数或命令解析。未发现方案与实际代码冲突，步骤 7 已更新为“进行中”。

## 2026-08-12 · 步骤 7 停线：改动量明显超出预估

### 停线原因

完成实现和验证后按本步文件核算，实际范围为 5 个 Codex data 文件和 2 个测试文件，文件数
仍在预估内，但净改动约 1,100 行，超过任务拆解中的 550—900 行上限约 22%。新增量主要
来自两部分：

- Codex-local tracker 同时实现 schema `changes[]` 投影、稳定 identity/revision、per-file
  unified diff 拆分、tool 优先/turn aggregate 抑制/empty clear，以及 runtime/thread/turn 清理；
- 定向测试覆盖 structured tool lifecycle、turn fallback、tool 后到清除、current command-only
  负向路径、thread/read 恢复、损坏更新和多文件 patch。

这已命中用户指定的“某步改动量明显超出预估”停线条件，因此步骤 7 未标完成，也未开始
步骤 8。当前没有发现方案与实际代码冲突；没有修改共享 application/ACP 层、Provider port、
capability、审批语义或命令解析。

### 停线前已完成的验证

- `dart format .`：通过，519 个文件，最终 1 个测试文件被格式化；
- `flutter analyze`：通过，0 个问题；
- Codex tracker、app-server provider/thread-read/history、fixture contract 共 3 个测试文件：
  116 项通过，0 失败；
- `python tool/smoke_codex_app_server.py`：当前 Codex `0.144.1` 环境下 17 项通过，0 失败，
  未设置 expected-version；记录不保留 thread/turn id、prompt、回复或 stderr 原文；
- `git diff --check`：通过；只有仓库行尾转换提示，无空白错误。

等待用户决定：接受步骤 7 的实际规模并保留完整实现，或要求压缩实现/测试后再重新验证。

## 2026-08-12 · 步骤 7 用户批准并完成

用户明确批准保留完整实现并接受约 1,100 行的实际规模。停线原因已解除；未压缩 tracker、
per-file turn diff 解析或边界测试。批准后没有新增 Dart 改动，停线前已通过的 format、analyze、
116 项相关测试、17 项当前 Codex app-server smoke 与 `git diff --check` 结果继续有效。

步骤 7 状态已更新为“完成”，下一步为步骤 8。

## 2026-08-12 · 步骤 8 开始

### 实际代码核对

- `AgentEventCoalescingPolicy` 对同 owner 的非终态 tool update 使用稳定 key；普通更新由
  Provider 提供完整 snapshot 后 latest-wins，终态保持 barrier，先发布合并后的进度再发布终态。
- `AgentTurnFileChangesEvent` 沿用 turn scope 的 snapshot key；`AgentTurnCompletedEvent` 是
  barrier，因此最新 turn snapshot 必须先于完成事件发布。
- 方案与实际代码一致，不需要修改共享 Buffer/Pipeline/CoalescingPolicy，也不涉及 Provider
  port；本步按“测试辅助对象 + 合同测试”执行，规模仍在预估内。

### 实现结果

- 新增测试专用 `AgentFileChangeCanonicalEnvelope`，只消费中立 typed event，并把 owner、
  status、revision、replayability、change identity/order/kind 与三种 evidence 投影成稳定签名；
  共享测试没有引入 Provider adapter 或 raw fixture。
- Buffer 合同固定了 tool_use/detail 的 latest-wins、终态 barrier 前先 flush 进度，以及屏障
  前后的 tool event 都保留完整 evidence；turn snapshot 同样固定为 latest-wins 后先于 turn
  completed 发布。
- Pipeline 合同固定了 typed turn snapshot 的最新 revision 与 barrier 顺序，并把测试 runtime
  的 `providerId` 改成中立 sentinel。
- 三 Provider producer 的 canonical envelope 已固定：Grok 为 `pending → completed`，Claude
  Edit/Write 为 `inProgress → completed`，Codex structured schema 路径为
  `inProgress → inProgress → completed`；同一 owner 的各阶段 snapshot 签名完全一致。
- 调试时曾把 Grok detail 阶段误写为 `inProgress`；真实 ACP shape 仍是 `pending` 且已经带
  完整证据，测试预期已按真实数据修正，未改生产逻辑。
- 本步未修改共享生产代码、Provider port、capability 或 Provider reducer/tracker。

### 验证

- `dart format .`：通过，520 个文件中最终 1 个测试文件被格式化。
- `flutter analyze`：通过，0 个问题。
- Buffer、Pipeline、shared purity、fixture normalization 与三 Provider producer 共 7 个测试
  文件：183 项通过，0 失败。

## 2026-08-12 · 步骤 9 开始

### 实际代码核对

- Grok history parser 每次 `parse` 都创建并释放独立 `GrokSessionUpdateMapper`，因此 identity
  与 file-change tracker 不会和 live 或另一次 replay 共用。
- Claude history reader 每次读取都创建独立 identity、`ClaudeCodeEventMapper` 与
  `_ClaudeCodeHistoryEventReducer`；mapper 自带独立 file-change tracker。
- Codex live provider 使用自己的 notification tracker；`threadHistoryFromReadResult` 每次调用
  都创建并释放独立 `CodexFileChangeTracker`。
- 方案与实际代码一致，本步只扩展 canonical 测试辅助和三 Provider parity/replay 测试；不改
  生产代码、共享层或 Provider port，预计规模仍在步骤 9 范围内。

### 实现结果

- canonical envelope 新增 owner + status + 完整 ordered snapshot 签名；签名逐项包含 change
  id、顺序、path、destination、kind、evidence 变体/正文、revision 与 replayability。history
  可合法生成自己的本地 turn identity，因此跨路径签名不把 session/turn scope 当成 owner。
- Grok Edit 使用 live mapper、第一次 history parse 与第二次 replay parse 三个隔离状态重建；
  三者终态签名一致，snapshot 对象互不复用。
- Claude Edit/Write 使用 live identity，以及两次 history read 新建的两个 identity、mapper、
  tracker 和 history reducer 重建；三者签名一致，identity/snapshot 对象均互不复用。
- Codex structured schema fixture 使用独立 live provider 与两次 `thread/read` 重建；tool owner、
  status、ordered change 与 unified patch 签名一致。当前 command-only fixture 同时固定 live、
  history、replay 均为 execute tool 且 `fileChanges == null`。
- Codex turn aggregate 仍只在 live tracker 测试中验证 `liveOnly` fallback/clear，不把它纳入可
  replay 的 tool parity；三 Provider turn/session/runtime/epoch 清理回归一并通过。
- 本步未修改任何生产代码、共享层、Provider port 或 capability。

### 验证

- `dart format .`：通过，520 个文件中 3 个测试文件被格式化。
- `flutter analyze`：通过，0 个问题。
- 三 Provider history/replay canonical、Codex provider 与三 Provider tracker cleanup 共 6 个
  测试文件：123 项通过，0 失败。

## 2026-08-12 · 步骤 10 开始

### 实际代码核对

- 现有 `agent_timeline_grouping.dart` 仍从 tool raw/unified diff header 猜文件项；按迁移顺序本步
  不改它，也不切生产 UI，而是先新增只消费 typed snapshot 的并行 projection/cache。
- 新 projection 的 path、destination、kind、change id 与 owner 全部来自中立 domain；unified
  patch 只允许拆行、标记展示类别和统计 `+/-` 行，不能覆盖 domain identity。
- cache key 固定为 owner + revision + change id，布局宽度不进入 key；同一 snapshot 的 resize
  必须返回同一投影实例。
- 方案与实际代码一致，不涉及共享层或 Provider port；预计仍为 2 个 presentation 文件、2 个
  单测，规模在步骤 10 预估内。

### 实现结果

- 新增 Provider-neutral `AgentFileChangeProjection`：按 snapshot 原顺序输出稳定 owner/change
  identity、path/destination、动作、replayability、行统计与 sealed detail projection。
- 使用穷尽模式匹配分别投影 replacement、written content、unified patch 与 null summary；空
  evidence 字符串仍生成显式详情，所有行集合为不可变列表。
- unified patch 只做 metadata/hunk/context/added/removed 行分类和 `+/-` 统计；测试使用故意
  错误的 patch header，确认展示 path/destination 仍只来自 typed change。
- 新增 `AgentFileChangeProjectionCache`，item key 为 owner + revision + change id；同一 snapshot
  返回同一 projection，revision 变化按 owner 淘汰旧正文，支持 `retainOnly` 与 `clear`。
- 本步没有接入 `agent_timeline_grouping.dart` 或 Widget，现有生产 UI 行为未变化。
- 生产与测试共新增 520 行，处于步骤预估的 350—600 行范围内。

### 验证

- `dart format .`：通过，524 个文件中 3 个新增/修改文件被格式化。
- `flutter analyze`：通过，0 个问题。
- projection、cache 与既有 timeline projection cache 共 3 个测试文件：11 项通过，0 失败。
- 10,000 行 patch 在 639/640/641、1280→1000→1280 的重复 resolve 中只计算 1 次；宽度不
  进入内容 key。

## 2026-08-12 · 步骤 11 开始

### 实际代码核对

- 现有文件编辑卡使用 `IdeCollapsibleCard`，但 `_AgentDiffDetails` 会一次性构建整块文本，
  不适合 10,000 行 evidence；本步新增并行的 typed evidence Widget，不修改旧 grouping。
- 新 Widget 只消费 `AgentFileChangeItemProjection` 和中立 `AgentToolStatus`，不读 Provider
  raw/wire 字段，也不根据路径或 patch header 猜身份。
- 响应式分界复用 `IdeMetrics.stackedRowBreakpoint = 640`：639px 上下堆叠，
  640/641px 左右并排；布局宽度不会进入 projection cache key。
- 折叠、键盘焦点与语义复用现有 `IdeCollapsibleCard`；长内容详情使用有界
  viewport + lazy builder，不使用 post-frame 测高、`GlobalKey` 或 layout 反馈环。
- 方案与实际代码一致；预计 2—3 个 Widget/style 文件 + 3—4 个 widget 测试，仍在
  步骤 11 的文件数和约 600—1,000 行量级内，不涉及共享层或 Provider port。

### 实现结果

- 新增 typed 文件变更卡和三种 evidence Widget：replacement 使用稳定 `Wrap`
  拓扑在 639px 堆叠、640/641px 并排；written content 保留所属 tool 的进行中/失败/
  终态语义；unified patch 使用新增、删除、分块标题和元数据的中立视觉。
- null evidence 只显示路径/动作摘要与“Provider 未提供内容证据”，即使调用方
  传入 `expanded=true` 也不会展开；`liveOnly` 明确标注“本回合实时汇总”和不可历史恢复语义。
- 详情和每行 key 只由 owner/change/role/index 组成，revision 和宽度不参与；跨 640px
  保留面板 Element。每个详情都有 `RepaintBoundary`、Graphite token 和非颜色 semantics。
- 长内容使用最高 280px 的 lazy viewport，按当前文字缩放的真实行高原型计算滚动
  extent；支持方向键、Page Up/Down、Home/End，10,000 行时末行可达且挂载行有界。
- 本步新增 2 个 Widget 文件、1 个 Widget 测试文件（共 4 个测试场景），约 1,008 行；
  与“约 600—1,000 行”的取整边界相当，不构成明显超量。未切换生产 grouping，未修改共享层、
  Provider port 或 capability。

### 验证

- `dart format .`：通过，527 个文件最终 0 个待格式化。
- `flutter analyze`：通过，0 个问题。
- 新 evidence Widget、`IdeCollapsibleCard`、projection 与 cache 共 4 个测试文件：9 项
  通过，0 失败。
- `git diff --check`：通过；只有工作区 CRLF 转换提示，无空白错误。

## 2026-08-12 · 步骤 12 停线：需要再次修改 G1 共享层

### 停线原因

实际代码核对确认，typed `AgentTurnFileChangesTimelineEntry` 仍继承共享
`AgentConversationTimelineStore` 中的旧 `AgentTurnDiffTimelineEntry`，并通过
`_legacyUnifiedPatchForRenderer` 把 typed snapshot 重新压成字符串供旧 UI 解析。要完成步骤 12
要求的“删除旧 turn diff 字符串链”，不能只改 presentation，必须再次修改 G1 清单中的
`lib/src/features/agent/application/agent_conversation_timeline_store.dart`。

同一旧兼容链还包含 `AgentTurnDiffEvent`、旧 mutation/reducer/processor/coalescing case，以及
presentation 中对 `AgentTurnDiffTimelineEntry.diff/raw` 的 grouping、extent、context panel 分支。
其中生产 data Provider 已不再产出旧 event，当前剩余产出者为测试 fixture；但删除这些
共享类型和分支仍是架构边界变更，不在本步已明确的“presentation 原子切换”授权内。

这已命中用户指定的“发现必须改共享适配层”停线条件，因此立即停止：

- 步骤 12 标记为阻塞，未修改任何步骤 12 生产代码或测试；
- 没有发现需要修改 Provider port/capability；
- 当前也未确认步骤 12 会超出 6—10 个生产文件、5—8 个测试和约 700—1,200 行的
  估算；停线原因仅是 G1 共享层授权。

### 需要用户确认的最小范围

是否批准步骤 12 在原子切换中同步完成以下 G1 机械清理：

1. 删除旧 `AgentTurnDiffEvent` 及对应 mutation/reducer/processor/coalescing case；
2. 删除 `upsertTurnDiff`、`AgentTurnDiffTimelineEntry` 和 `_legacyUnifiedPatchForRenderer`；
3. 让 `AgentTurnFileChangesTimelineEntry` 直接继承 `AgentTimelineEntry`，仍只机械携带 typed snapshot；
4. 同步把共享测试 fixture 迁移到 `AgentTurnFileChangesEvent`，不引入 Provider 分支、raw 解析、
   partial 推断或新的 identity 规则。

本步在收到明确批准前不继续。

## 2026-08-12 · 步骤 12 获得 G1 最小清理批准

用户明确批准上述最小范围。步骤 12 恢复为“进行中”，共享层修改限定为：

- 删除旧 turn diff event/mutation/reducer/processor/coalescing 兼容 case；
- 删除 TimelineStore 的旧 diff 方法、旧 entry 和 typed-to-string 桥接；
- typed `AgentTurnFileChangesTimelineEntry` 直接机械携带 snapshot；
- 共享测试 fixture 只迁移到 typed event。

批准不包含 Provider 分支、raw 解析、partial 推断、identity 规则改动、Provider port 或
capability 变更；若实现需要越过这些边界，再次停线。

## 2026-08-12 · 步骤 12 完成

### 实现结果

- 生产时间线已原子切到 typed file-change snapshot：tool 与 turn fallback 都通过同一个
  `AgentFileChangeProjectionCache` 进入步骤 11 的 evidence Widget；owner/change identity、
  revision、顺序、动作与 replayability 均来自 Provider 已归一化的中立模型。
- grouping 删除文件变更 raw output、locations、wire key 和 patch header 推断；有非空 typed
  snapshot 的任意工具进入文件变更组，没有 snapshot 的编辑工具按普通命令展示。turn fallback
  独立成组，不与相邻 tool owner 合并。
- context panel 对文件变更只输出 typed snapshot 的中立 JSON；编辑工具缺少 snapshot 时只展示
  中立元数据，明确不回退 raw。非文件工具仍保留原有 raw 诊断展示。
- 完成已批准的 G1 最小清理：删除旧 `AgentTurnDiffEvent`、mutation/reducer/processor/
  coalescing case、`upsertTurnDiff`、`AgentTurnDiffTimelineEntry` 与 typed-to-string 桥；
  `AgentTurnFileChangesTimelineEntry` 现在直接机械携带 snapshot。
- extent/layout revision 不再哈希整块 diff 正文，只使用 typed snapshot revision 与 change
  metadata；长期 timeline projection cache 同时管理文件 projection 生命周期，resize 不触发
  正文重复计算。
- 生产卡片已替换为 replacement、written content、unified patch 三种 typed evidence；旧
  `_AgentDiffDetails` 和一次性整块高亮路径删除。Grok、Claude Code、Codex 三种证据在同一
  history 可见链路中逐项展开通过。
- 新增 presentation purity guard，固定 grouping 不得读取 raw/wire/locations 或解析 patch
  header，并固定编辑工具 context 必须先经过 typed 路径。没有新增 Provider 分支、identity
  规则、Provider port 或 capability。
- 已批准的 G1 清理增加了原估文件数，但新增范围严格等于用户批准的机械删除与 fixture 迁移；
  没有出现第二次未授权扩围，整体代码改动量仍处于本步约 700—1,200 行量级。

### 验证

- `dart format .`：通过，最终 528 个文件中 0 个待格式化。
- `flutter analyze`：通过，0 个问题。
- reducer、TimelineStore、Pipeline、Buffer、事件风暴、grouping、projection/cache、extent/layout
  与两项 purity guard：14 个测试文件，149 项通过，0 失败。
- evidence Widget、AgentPane history、ViewModel、完整 conversation widget 与滚动基线：
  5 个测试文件，150 项通过，0 失败。
- Grok ACP、Claude Code stream-json、Codex app-server 三 Provider 协议/历史夹具：
  3 个测试文件，191 项通过，0 失败。

## 2026-08-12 · 步骤 13 完成

### 实际范围核对

- 长期文档仍把文件变更写成旧“回合 diff”，并把共享合并规则描述为 `token/diff`；实际代码
  已在步骤 12 删除旧字符串事件和 renderer，必须同步为完整 typed snapshot 语义。
- 本步只修改 AGENTS、CHANGELOG、架构/开发/术语/贡献指南及 Codex、Claude Code 协议事实，
  共 13 份长期文档；未修改生产代码、共享实现、Provider port 或 capability。
- 文档净变更约 193 行新增、34 行删除，属于步骤 13 预期的长期文档同步范围，没有明显超量。

### 同步结果

- 中英文总览、术语表和贡献指南统一定义 `AgentFileChangeSnapshot`、三种 evidence、`null` 与
  empty clear、`replayable` 与 `liveOnly`，并明确 Store 只机械透传、presentation 不读 raw。
- 工程规范、设计文档和开发指南新增 Provider-local tracker 责任与三 Provider 映射：Grok ACP
  diff、Claude Edit/Write/摘要、Codex structured tool + typed turn fallback；command-only 只保留命令卡。
- Codex 协议文档记录 tool 优先、aggregate 抑制/empty clear、schema fixture 与当前运行实证隔离；
  Claude Code 协议文档记录 tool_use 产证据、tool_result 带回快照及独立 history tracker。
- AGENTS G1 自查补入 Claude；G2/G7、任务路由与持久化边界补齐文件证据正文不得进入日志、
  缓存、通知、thread summary 或持久化 JSON 的约束。
- CHANGELOG 增加用户可见的三 Provider 文件证据展示与诚实降级说明。

### 验证

- `dart format .`：通过，528 个文件中 0 个变更。
- `flutter analyze`：通过，0 个问题。
- 13 份步骤 13 文档的相对 Markdown target 检查通过；首次检查把代码示例
  `` `[$name](path)` `` 误当链接，排除 fenced/inline code 后重跑通过，不是文档缺链。
- 文件变更 presentation purity 与 Claude shared-layer purity 共 2 个测试文件：8 项通过，0 失败。
- `git diff --check`：通过；只有工作区 CRLF 转换提示，无空白错误。

## 2026-08-12 · 步骤 14 完成

### 收尾与验收结果

- 新增 `05-验收报告.md`，逐条映射 AC-1—AC-8；八项均通过。当前 Codex CLI 的真实编辑仍只
  产生 commandExecution，structured fileChange 明确保留为“待观察”，没有拿 schema fixture
  冒充真实运行证据。
- 全量测试通过 1,732 项，1 项仅限 macOS 的 WindowFrame 菜单测试按平台条件跳过，0 失败。
- G1/G2/G3 自查通过：共享层 Provider code、TimelineStore 文件 wire 推断、reducer
  async/scheduler 均为 0 命中。G6 本任务涉及的 24 个 Agent production Dart 文件均通过分层检查；
  扩大扫描发现未被本任务修改的 `agent_skill_models.dart` 既有 Flutter import，已按用户批准的
  最小清理范围保留，并在验收报告中如实列为独立后续项。
- G7/G8、文件变更 presentation purity 与 ignored logger 泄漏回归共 4 个测试文件、12 项通过。
- 当前环境 smoke：Grok `1.0.3` 为 4/4，Claude Code `2.1.227` 为
  `PASS (init+assistant+result)`，Codex `0.144.1` 为 17/17；按授权均不设置精确版本硬门。
- 临时真实编辑采样确认 Grok 产生结构化 replacement diff、Claude Code Edit/Write 成功；Codex
  当前仍为 command-only。采样只用临时 workspace 和合成内容，记录已脱敏且临时目录已清理。
- Windows Profile 在 101 个 turn、10,000 行 evidence 和 1280→1000→1280 resize 下，UI p95
  1.041ms、Raster p95 1.553ms、慢帧 2/139（1.44%）、正文投影 1 次；临时入口已删除。
- `dart format .`：Profile 入口存在时 529 个文件 0 变更；删除入口后的最终复核为 528 个文件
  0 变更。`flutter analyze` 两次均为 0 个问题。
- 验收报告 3 个相对链接全部有效；`git diff --check` 通过，仅有 CRLF 转换提示，无空白错误。
- 本步没有新增生产功能，没有再次修改共享适配职责、Provider port/capability 或权限/Plan 语义。

## 2026-08-12 · Codex 本地历史 Apply patch 诊断停线

### 真实数据与代码核对

- 截图对应的当前 Codex `0.144.1` 本地 session JSONL 中，工具外层记录为
  `custom_tool_call(name: exec)`，文件编辑另有结构化 `patch_apply_end`；两者 call id 不相同，
  因此历史解析后分别显示为“Exec”和“Apply patch”。
- 脱敏形状检查确认，当前会话的 `patch_apply_end` 全部带 `changes`，变更值可包含
  `type`、`move_path`、`content` 与 `unified_diff`；该会话没有 App-Server `fileChange`
  ThreadItem，也没有 turn diff。
- `_JsonlHistoryParser._consumePatchApplyEnd` 当前只从 `changes` 提取 locations 和摘要，构造
  `AgentToolCall(kind: edit)` 时没有填入 `fileChanges`。presentation 已按步骤 12 正确禁止从
  raw/locations 反推文件证据，因此最终只能显示工具标题“Apply patch”。
- `CodexFileChangeTracker` 目前只接收 App-Server `fileChange.changes[]` 的 list 形状；
  `thread/read` 路径有 typed snapshot，但本地 JSONL 优先且远端结果只补 turn failure，不能补回
  本地条目遗漏的文件证据。

### 停线原因与建议最小范围

步骤 7、AC-6 和步骤 14 的验收把 Codex 可恢复历史限定在 App-Server `fileChange` fixture，
遗漏了当前环境真实存在且携带 structured diff 的本地 `patch_apply_end` 合法历史路径。这属于
“方案和实际代码/协议数据对不上”，因此停在线上诊断阶段，未修改任何生产代码。

建议后续最小修复只位于 Codex 自有 data 层：

1. 为 `patch_apply_end.changes` 增加宽容的 Codex-local typed 映射，并把 snapshot 挂到对应
   “Apply patch”历史工具；
2. 保持 presentation 不读 raw，共享 TimelineStore/CoalescingPolicy 和 Provider port/capability
   均不修改；
3. 用当前版本脱敏 fixture 固定 update/add/delete/move、缺失 diff、损坏输入，以及从 JSONL
   history 到可见 evidence 的回归。

收到用户明确批准前不继续实现。

## 2026-08-12 · 步骤 15 获得最小修复批准

用户明确批准按诊断建议继续。步骤 15 限定为 Codex 自有 data 层的本地 JSONL
`patch_apply_end.changes` typed 映射、当前 `0.144.1` 脱敏 fixture，以及 mapper/history/可见
evidence 定向回归；不修改共享 TimelineStore/CoalescingPolicy、Provider port/capability、
presentation raw 禁令或权限/Plan 语义。

真实采样只观察到 update/add/delete；move 使用已存在但当前为空的 `move_path` 字段做合成兼容
测试，不能标为真实运行证据。若实现需要越过上述边界或明显超过预计范围，再次停线。

## 2026-08-12 · 步骤 15 完成

### 实现结果

- `CodexFileChangeTracker` 新增 Codex 本地 JSONL `patch_apply_end.changes` 的 Provider-local 映射：
  update 读取 `unified_diff`，add 读取写入后的 `content`，delete 只保留路径/删除动作，update 携带
  非空 `move_path` 时映射为移动；缺失证据仍保留 Provider 明示的摘要，损坏 `changes` 不覆盖
  last-valid snapshot。
- `CodexJsonlHistoryParser` 为每次历史读取使用独立 tracker，把 typed snapshot 挂到对应的
  “Apply patch”工具。外层 `custom_tool_call(name: exec)` 因没有相同 call id，继续作为独立 Exec
  展示，不把命令文本或 raw 输出猜成文件证据。
- 新增当前 Codex `0.144.1` 脱敏真实形状 fixture，并补齐 manifest 隐私合同、tracker 单元测试、
  完整 Provider 历史回归；既有历史 evidence Widget 和两项 purity guard 一并复验。
- 实际生产改动仍为 2 个 Codex data 文件；没有修改共享 TimelineStore/CoalescingPolicy、Provider
  port/capability、presentation 或权限/Plan 语义，范围未超出步骤 15 预估。

### 验证

- `dart format .`：通过，528 个文件中 0 个待格式化。
- `flutter analyze`：通过，0 个问题。
- 6 个相关测试文件共 136 项通过，0 失败，覆盖 tracker、fixture 合同、完整 Provider 历史、
  历史 unified patch 可见性、共享层与 presentation purity。
- 最终完整 `flutter test`：1,736 项通过，1 项仅限 macOS 的 WindowFrame 菜单测试按平台条件
  跳过，0 失败。
- 首次组合运行把外层 `exec` 错误断言为 `AgentToolKind.execute`；修正测试为当前实际语义
  `AgentToolKind.other`，生产代码未迁就错误断言。加入 fixture 合同后又两次被隐私哨兵拦截，
  原因是合成 add/delete 内容未带 `REDACTED` 标记；统一替换为脱敏 sentinel 后从
  format → analyze → 相关测试完整重跑并通过。
