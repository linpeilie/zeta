# 05 · 验收报告

任务：三 Provider 文件变更证据通用化（由 Grok 文件编辑展示缺口触发）

日期：2026-08-12

结论：**AC-1—AC-8 与步骤 15 修复均通过；Codex 当前真实 App-Server structured fileChange
路径仍待观察，本地 JSONL `patch_apply_end` 历史证据已用当前数据形状固定。**

## 1. 验收范围与真实基线

本报告按 [`01-需求分析.md`](./01-需求分析.md) 的 AC-1—AC-8、
[`02-方案设计.md`](./02-方案设计.md) 的真实验收要求和
[`03-任务拆解.md`](./03-任务拆解.md) 步骤 14 与步骤 15 执行。Codex、Grok、Claude Code 的精确
版本限制继续按用户授权跳过；版本只记录 provenance，不作为本次运行硬门。

| Provider / 来源 | 本次实际基线 | 结果 |
| --- | --- | --- |
| Grok | `1.0.3`，Windows 11 / AMD64 | 基础 ACP smoke 4/4；真实 Edit 产生 2 个结构化 diff block |
| Claude Code | `2.1.227`，Windows / AMD64 | 基础 stream-json smoke 通过；真实 Edit 与 Write 均成功 |
| Codex | `0.144.1`，Windows 11 / AMD64 | app-server smoke 17/17；实时 App-Server 编辑仍为 command-only；本地 session JSONL 的 `patch_apply_end` 含结构化 update/add/delete 证据并已恢复 |
| Codex schema fixture | 仓库 pin `0.144.5` | structured fileChange / patchUpdated / turn fallback 合同通过 |

所有真实编辑都使用临时 workspace 与合成 sentinel 文件。验收记录不包含 prompt、回复、命令、
真实路径、文件正文、凭证、原始 payload、thread/turn/tool id 或 stderr 原文；临时目录均已清理。

## 2. AC-1—AC-8 映射

| AC | 验收证据 | 结论 |
| --- | --- | --- |
| AC-1 · Grok replacement | 当前 Grok `1.0.3` 真实 Edit 修改成功；in-progress/terminal 共观察到 2 个含 `path/oldText/newText` 的 diff block。Grok tracker、mapper、history canonical 与 visible-chain 测试固定原位更新、替换片段文案和无 `diff: <path>` 占位。 | 通过 |
| AC-2 · Claude Edit/Write | 当前 Claude Code `2.1.227` 真实 Edit/Write 均成功；Edit input keys 为 `file_path/new_string/old_string/replace_all`，Write 为 `content/file_path`。tracker 与 Widget 测试覆盖成功、失败、终态保留和“尝试修改”语义。 | 通过 |
| AC-3 · Codex command-only | 当前 Codex `0.144.1` 真实编辑成功，但 item 只有 user/reasoning/commandExecution/agentMessage；command approval 6 次，fileChange approval、patchUpdated、turn diff、history fileChange 均为 0。负向 fixture 与 UI 测试固定只显示命令卡，不猜文件证据。 | 通过 |
| AC-4 · Codex structured fixture | 仓库 schema provenance fixture 覆盖 fileChange started/patchUpdated/completed、unified patch、typed turn fallback、tool 优先、aggregate 抑制与 empty clear；三 Provider visible-chain 展开通过。当前 CLI 未触发真实 structured path，继续标记“待观察”。 | 通过（真实路径待观察） |
| AC-5 · 完整 snapshot / coalescing | 三 Provider tracker 测试覆盖“证据 → status/text-only → terminal”；Provider 在 pipeline 前输出完整累计 snapshot。Buffer/Pipeline/Store 测试固定 latest-wins、barrier 顺序和机械替换。 | 通过 |
| AC-6 · live/history/replay | Grok、Claude Code、Codex App-Server structured tool 分别以独立 tracker/identity/reducer 重建，并逐位比较 owner、change id/order、动作、evidence、revision、replayability 与终态；步骤 15 另以当前 `0.144.1` 脱敏 fixture 固定本地 JSONL `patch_apply_end` replayable history。Codex turn aggregate 只验证 `liveOnly`。 | 通过 |
| AC-7 · 长内容与 resize | Widget 测试覆盖 639/640/641px、2x 文字、10,000 行末行可达、挂载行 `<80`；Windows Profile 在 101 turn 和 10,000 行下完成 1280→1000→1280，投影计算仅 1 次。 | 通过 |
| AC-8 · raw/persistence/log purity | presentation purity guard 禁止 Provider 分支、file-change raw/wire key 与 patch header identity；TimelineStore 不含文件 wire 推断；ignored logger 的内存和磁盘 sentinel 泄漏测试通过；文件 evidence 不进入任何 Zeta store。 | 通过 |

## 3. 固定门禁结果

按步骤 14 的顺序执行：

- `dart format .`：通过；临时 Profile 入口存在时共 529 个 Dart 文件，0 个变更；入口删除后的
  最终复核为 528 个文件，0 个变更。
- `flutter analyze`：通过，最终复核仍为 0 个问题。
- `flutter test`：步骤 15 后最终复核为 1,736 项通过，1 项按平台条件跳过，0 失败；跳过项是只适用于 macOS 的
  WindowFrame 菜单测试，Windows 对应测试已执行。
- G1/G2/G3 自查：共享五文件非注释 Provider code 命中 0；TimelineStore 文件 wire 推断命中 0；
  reducer async/scheduler 命中 0。
- G6 本任务变更集自查：24 个变更的 Agent production Dart 文件中，domain 的 Flutter/data
  import 与 application/presentation 的 data import 均为 0。
- G7/G8 与 presentation purity：4 个守卫/行为测试文件共 12 项通过，0 失败。
- `git diff --check`：通过；只有工作区 CRLF 转换提示，0 个空白错误。

### 全仓 G6 既有基线备注

额外扩大到整个 `features/agent/domain` 的扫描发现，未被本任务修改的
`agent_skill_models.dart` 既有 `package:flutter/foundation.dart` import。这不是本次文件变更
方案引入，也不影响上述 24 个变更文件的 G6 结果；本任务没有越权顺手修复。该既有例外应由
独立架构清理处理，不能在本报告中伪装成全仓 0 命中。

## 4. 三 Provider 当前环境 smoke

### Grok

- `python tool/smoke_grok_acp.py --timeout 120`：4 项通过，0 失败；两 session 并发和进程回收
  恢复均成功。
- 临时 Edit 采样：文件实际改变；观察到 2 个 diff block，字段包含
  `_meta/newText/oldText/path/type`；tool_call 与 tool_call_update 生命周期存在。
- fixture 仍保留原 `1.0.0` provenance；本次 `1.0.3` 结果是新增兼容证据，不改写旧来源标签。

### Claude Code

- `python tool/smoke_claude_code_stream_json.py --timeout 90`：
  `PASS (init+assistant+result)`。
- 临时 Edit/Write 采样：两个文件结果均符合合成目标；Edit/Write input keys 与既有 fixture
  一致，未出现 control request。

### Codex

- `python tool/smoke_codex_app_server.py --timeout 120`：17 项通过，0 失败。
- 临时编辑采样：合成文件实际改变且 turn 成功；只观察到 commandExecution，结构化
  fileChange、patchUpdated、turn diff 与 history fileChange 均为 0。
- 因当前真实路径无法触发 structured fileChange，验收只确认 command-only 诚实降级与 schema
  fixture 兼容；没有把 schema fixture 冒充真实运行证据。

## 5. Windows Profile

使用临时、不提交的 Profile 入口直接加载生产主题、生产 typed projection/cache 和生产 evidence
Widget；运行后入口已删除。慢帧定义为 UI build 或 Raster 任一超过 16.667ms。

| 指标 | 结果 |
| --- | ---: |
| 模式 / 平台 | Windows desktop Profile |
| 数据量 | 101 turn；10,000 行 unified patch evidence |
| 宽度序列 | 1280 → 1000 → 1280 |
| FrameTiming 样本 | 139 |
| UI p95 | 1.041ms |
| Raster p95 | 1.553ms |
| 慢帧 | 2（1.44%） |
| 文件正文投影次数 | 1 |
| 实际挂载 evidence 行 | 32 |
| 实际挂载 turn 行 | 24 |

结果证明 resize 不会按宽度重复解析 10,000 行正文，长 evidence 与长 timeline 都保持有界挂载。
本表只采用 Profile 数据，不用 Debug 帧耗时作结论。

## 6. 剩余观察项

1. 当前 Codex CLI 的实时 App-Server 编辑仍未产生 structured fileChange；等未来实际出现时，应追加真实
   provenance fixture，但不得替换或重标当前 schema fixture。
2. 全仓 G6 既有 `agent_skill_models.dart` Flutter import 不属于本任务，需独立评估清理。
3. 本任务没有新增 Provider port/capability，没有修改权限/Plan 语义，也没有持久化文件证据正文。

## 7. 步骤 15 · Codex 本地 JSONL 历史修复复验

- 当前 Codex `0.144.1` 脱敏形状确认：外层工具为 `custom_tool_call(name: exec)`；独立
  `patch_apply_end.changes` 的 update 带 `unified_diff`，add/delete 带 `content`，当前采样未观察到
  非空 `move_path`。fixture 不保存 prompt、回复、命令、真实路径、真实正文、id 或 stderr。
- Codex-local mapper 现在把 update 投影为 unified patch evidence、add 投影为 written content、
  delete 投影为无详情的删除摘要；非空 `move_path` 使用合成兼容测试固定 moved 语义。
- 外层 Exec 不解析 input、不生成文件 snapshot；call id 不同的 Apply patch 保持独立 owner。
  缺失 diff 仍可保留 Provider 明示的路径/动作摘要，损坏 `changes` 不覆盖 last-valid snapshot。
- `dart format .`：528 个文件，0 个待格式化；`flutter analyze`：0 个问题。
- Codex tracker、fixture 合同、完整 Provider 历史、历史 evidence Widget 与两项 purity guard 共
  6 个测试文件、136 项通过，0 失败。首次组合运行因测试把外层 `exec` 误断言为 `execute` 而
  失败；修正为实际 `AgentToolKind.other` 后生产实现未为测试改语义。fixture 隐私合同随后两次
  拦截未标记 `REDACTED` 的合成内容；统一脱敏后从 format → analyze → 相关测试完整重跑通过。
- 本步只修改 2 个 Codex data 文件，没有修改共享层、Provider port/capability 或 presentation。
