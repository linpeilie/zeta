# 左侧栏与 Agent 统计整合 · 任务拆解

- 日期：2026-08-11
- 输入：`01-需求分析.md`、`02-方案设计.md`
- 状态：实施中（T01～T08 完成）
- 档位：L

## 执行约束

- 按表中顺序执行；每一步从上一步的绿色提交开始，单独提交、可单独回滚。
- 实施时每完成一步，把本表对应状态改为“完成”并放进同一提交；若发现方案与代码不符、必须修改共享适配层或 Provider 端口，立即停止并记录到 `04-实现记录.md`。
- 在本任务的干净分支或独立 worktree 中执行，不把无关改动带入提交。
- 每一步先跑该行列出的专项命令，再跑通用门禁：修改过 Dart 时执行 `dart format .`；始终执行 `flutter analyze` 和 `flutter test`。完成结果必须分别为格式化成功、`No issues found!`、`All tests passed!`，并且 `git diff --check` 无输出。
- T06→T08 与 T09→T13 是两组“先加后删”迁移：typed Turn 终态路径和新左栏路径先并行跑通，再分别删除旧无参回调与首页 Activity Rail/旧统计卡片适配层。

## 步骤表

| 序号 | 做什么 | 涉及文件 | 完成判据 | 状态 |
|---|---|---|---|---|
| T01 | 先补现状安全网，只冻结本需求不能破坏的既有行为：真实 `IdeHome` 切换现有左区后不重挂 AgentPane、不丢当前 Thread/草稿/滚动状态；迟到或旧 generation 的完成 effect 不触发回调；旧 v1～v4 会话仍可宽容读取。不添加未来 UI 的失败断言。 | `test/src/app/ide_shell_widget_test.dart`；`test/src/features/agent/application/agent_conversation_effect_runner_test.dart`；`test/src/features/ide_session/domain/ide_session_state_test.dart` | `flutter test test/src/app/ide_shell_widget_test.dart test/src/features/agent/application/agent_conversation_effect_runner_test.dart test/src/features/ide_session/domain/ide_session_state_test.dart` 全绿；通用门禁全绿。该提交只有测试变化，生产行为不变。 | 完成 |
| T02 | 新增纯 Dart `IdeWorkbenchLayoutState`，定义左栏显隐、统计展开、栏宽、展开高度比例和统计 Provider 偏好；实现不可变更新与逐字段宽容 JSON 投影，非法/非有限数值回退默认。此时尚不接入会话快照和 UI。 | 新增 `lib/src/features/ide_session/domain/ide_workbench_layout_state.dart`；新增 `test/src/features/ide_session/domain/ide_workbench_layout_state_test.dart` | `flutter test test/src/features/ide_session/domain/ide_workbench_layout_state_test.dart` 覆盖默认值、往返、缺字段、未知字段、错误类型、NaN/Infinity/越界值并全绿；通用门禁全绿。 | 完成 |
| T03 | 把 Workbench 状态接入现有 v4 IDE 会话快照和 `IdeShellController`：恢复、清洗、构建、延迟保存与关闭前 `saveNow` 均携带该状态；新增窄 setter。UI 暂不消费，但读取到的新字段不能在下一次保存时被默认值覆盖。 | `lib/src/features/ide_session/domain/ide_session_state.dart`；`lib/src/features/ide_session/application/ide_session_state_builder.dart`；`lib/src/app/shell/ide_shell_controller.dart`；`test/src/features/ide_session/domain/ide_session_state_test.dart`；`test/src/features/ide_session/data/ide_session_store_test.dart`；`test/src/features/ide_session/application/ide_session_persistence_coordinator_test.dart`；`test/src/features/ide_session/presentation/ide_session_restore_widget_test.dart`；`test/src/app/ide_shell_controller_test.dart` | `flutter test test/src/features/ide_session/ test/src/app/ide_shell_controller_test.dart` 全绿；断言旧 v1～v4 缺少 `workbench` 时使用默认值、单字段损坏不丢项目/Thread、未知字段可忽略、五个字段恢复后再次保存不漂移；通用门禁全绿。 | 完成 |
| T04 | 在中立 Usage domain 增加 `compactQuotaWindow` 派生规则：仅付费套餐参与；选正数周期最短窗口；等周期保持原顺序；所有周期未知时取第一项；无窗口返回空。不改变 repository 数据形状。 | `lib/src/features/usage_statistics/domain/agent_usage_panel_models.dart`；新增 `test/src/features/usage_statistics/domain/agent_usage_panel_models_test.dart` | `flutter test test/src/features/usage_statistics/domain/agent_usage_panel_models_test.dart` 覆盖最短周期、稳定同序、部分/全部周期未知、无套餐和无窗口并全绿；测试及实现均无 Provider id/kind 分支；通用门禁全绿。 | 完成 |
| T05 | 扩展 `AgentUsagePanelController` 的选择状态：区分恢复偏好与当前有效选择，增加手动选择、Turn 终态选择和 selection changed 回调；目录到达前保留最新偏好，失效时按稳定目录回退第一项，空目录清空；保留现有 load token 竞态保护。新参数均可选，现有 UI 行为不变。 | `lib/src/features/usage_statistics/application/agent_usage_panel_controller.dart`；`test/src/features/usage_statistics/application/agent_usage_panel_controller_test.dart` | `flutter test test/src/features/usage_statistics/application/agent_usage_panel_controller_test.dart` 覆盖有效/失效恢复值、目录前终态、手动后自动覆盖、空目录、最后终态胜出、旧刷新结果丢弃及恢复不回写循环并全绿；通用门禁全绿。 | 完成 |
| T06 | 【先加】新增中立 `AgentTurnTerminalSignal`，从通过 scope/generation 校验的 `AgentTurnCompletedEffect.scope` 构造 Provider、可空 Thread 与 Turn 白名单身份；EffectRunner 新增 typed `onTurnTerminal` 回调，同时暂时保留旧无参 `onTurnCompleted`，生产调用方仍走旧路径。 | 新增 `lib/src/features/agent/domain/agent_turn_terminal_signal.dart`；`lib/src/features/agent/application/agent_conversation_effect_runner.dart`；`test/src/features/agent/application/agent_conversation_effect_runner_test.dart` | `flutter test test/src/features/agent/application/agent_conversation_effect_runner_test.dart` 全绿；断言 typed signal 精确取当前 effect scope、同一 effect 只投递一次、旧 scope/generation 不投递，且只配置旧回调的调用方行为不变；通用门禁全绿。 | 完成 |
| T07 | 【切换】把 typed 终态信号贯通 ViewModel→Workspace Controller→Shell→`IdeHome`；生产装配停止传旧回调。`IdeHome` 收到任意前后台终态后，先按 signal Provider 更新统计选择并写入 Workbench 状态，再请求现有刷新协调器静默刷新；统计选择不得写会话默认 Provider。 | `lib/src/features/agent/presentation/agent_conversation_view_model.dart`；`lib/src/features/agent/application/agent_thread_workspace_controller.dart`；`lib/src/app/shell/ide_shell_controller.dart`；`lib/src/ui/features/ide/views/ide_home.dart`；相关测试：`agent_conversation_view_model_test.dart`、`agent_thread_workspace_controller_test.dart`、`ide_shell_controller_test.dart`、`ide_shell_widget_test.dart` | `flutter test test/src/features/agent/presentation/agent_conversation_view_model_test.dart test/src/features/agent/application/agent_thread_workspace_controller_test.dart test/src/app/ide_shell_controller_test.dart test/src/app/ide_shell_widget_test.dart` 全绿；覆盖 completed/failed/interrupted/取消映射、Plan handoff、两个前后台 entry 各自 Provider、一次终态只刷新一次、统计选择持久化且 active Provider 不变；通用门禁全绿。 | 完成 |
| T08 | 【后删】删除旧无参 `onTurnCompleted` 契约和兼容调用，只保留 typed `onTurnTerminal`；同步生产与测试构造器，确保没有双通知路径。 | `lib/src/features/agent/application/agent_conversation_effect_runner.dart`；`lib/src/features/agent/presentation/agent_conversation_view_model.dart`；`lib/src/features/agent/application/agent_thread_workspace_controller.dart`；`lib/src/app/shell/ide_shell_controller.dart`；对应测试 | 对上述生产文件及对应测试执行 `rg -n "onTurnCompleted"` 无旧回调残留；T06、T07 的专项测试全绿；通用门禁全绿。 | 完成 |
| T09 | 【先加】扩展 `WindowFrame` 通用标题栏契约：新增默认空的左侧 actions 槽，并给 `WindowTitleBarAction` 增加可选 `FocusNode`；保证 macOS action 位于三色灯 gutter 之后，Windows/Linux 位于最左可交互区，旧页面不传参数时布局不变。暂不在首页启用。 | `lib/src/ui/core/window_frame.dart`；`test/src/ui/core/window_frame_menubar_test.dart` | `flutter test test/src/ui/core/window_frame_menubar_test.dart` 全绿；覆盖三平台位置、空列表兼容、tooltip/semantic label、焦点可恢复以及拖拽区/窗口按钮仍可用；通用门禁全绿。 | 待做 |
| T10 | 【先加】为 Agent 统计增加 cardless、mode-aware 内容组件，保留当前 `AgentUsagePanel` 卡片包装作为临时兼容入口。新增折叠摘要：套餐/Provider 首行、可选最短周期额度行、今日 Token 行，以及呼吸 Skeleton、错误、`-` 和长文本省略；展开态复用现有 Tabs 与完整内容。生产首页仍走旧包装。 | `lib/src/features/usage_statistics/presentation/agent_usage_panel.dart`；`test/src/features/usage_statistics/presentation/agent_usage_panel_test.dart`；必要时复用 `lib/src/features/agent/presentation/widgets/agent_provider_icon.dart`，不新增 Provider 分支 | `flutter test test/src/features/usage_statistics/presentation/agent_usage_panel_test.dart test/src/features/usage_statistics/domain/agent_usage_panel_models_test.dart test/src/features/agent/presentation/widgets/agent_provider_icon_test.dart` 全绿；覆盖有套餐三行、无套餐两行、最短窗口、Skeleton、错误、Token `-`、展开完整内容和紧凑高度无溢出；通用门禁全绿。 | 待做 |
| T11 | 【先加】新增未接线的 `ProjectAgentSidebar`：唯一 `PanelCard` 内由 Projects 占剩余空间、统计固定底部；展开时使用父约束、保存比例和双侧最小高度夹紧，折叠时隐藏 resize handle 并保留比例。新增响应式尺寸 token，不使用 post-frame 测高或设备类型分支。 | 新增 `lib/src/ui/features/ide/views/project_agent_sidebar.dart`；`lib/src/ui/core/ide_metrics.dart`；新增 `test/src/ui/features/ide/views/project_agent_sidebar_test.dart` | `flutter test test/src/ui/features/ide/views/project_agent_sidebar_test.dart` 全绿；断言只有一个 `PanelCard`、折叠无手柄、展开向上挤压 Projects、拖动结果夹紧、空间不足时正文内部滚动、不同高度/文字缩放下无 overflow；通用门禁全绿。 | 待做 |
| T12 | 【切换】在真实 `IdeHome` 启用新路径：标题栏左 action 控制整个合并栏，`ProjectAgentSidebar` 替换旧上下双卡片，五项状态从 Shell 恢复并在 toggle/Tab/drag end 后保存，Compact 下复用 Navigation Overlay 且 Esc/scrim 关闭并恢复焦点。Activity Rail 暂留一个提交作为冗余入口，但改为操作同一 Workbench 状态，不能再维护第二套本地状态。 | `lib/src/ui/features/ide/views/ide_home.dart`；`lib/src/ui/core/workbench/ide_workbench_scaffold.dart`（仅在现有 overlay/focus 契约确有缺口时修改）；`test/src/ui/core/ide_workbench_scaffold_test.dart`；`test/src/app/ide_shell_widget_test.dart`；`test/src/features/ide_session/presentation/ide_session_restore_widget_test.dart` | `flutter test test/src/ui/core/ide_workbench_scaffold_test.dart test/src/app/ide_shell_widget_test.dart test/src/features/ide_session/presentation/ide_session_restore_widget_test.dart` 全绿；断言新单卡片和标题栏入口已作为生产路径、五项状态重启恢复、Compact 为浮层、drag 只在结束时提交、AgentPane/Thread/草稿/滚动不重置；旧 Rail 与新入口操作同一状态且不重复挂载内容；通用门禁全绿。 | 待做 |
| T13 | 【后删】从 Agent 首页摘掉 Activity Rail 和所有左栏旧状态/切换方法，删除临时 `AgentUsagePanel` 独立卡片包装，只保留 cardless 内容；保留 `IdeActivityRail` 与 `IdeWorkbenchScaffold.leadingRailBuilder` 作为通用能力，也保留右侧 Files/Tools 使用的 `_ResizableColumn`。补最终架构守卫和真实首页验收。 | `lib/src/ui/features/ide/views/ide_home.dart`；`lib/src/features/usage_statistics/presentation/agent_usage_panel.dart`；`test/src/app/ide_shell_widget_test.dart`；`test/src/features/usage_statistics/presentation/agent_usage_panel_test.dart`；新增/修改 `test/src/features/agent/architecture/` 下的统计选择隔离守卫 | 对 `ide_home.dart` 执行 `rg -n -e "_buildLeadingRail" -e "_leftTopVisible" -e "_leftBottomVisible" -e "_leftTopRatio" -e "_toggleLeftPanel"` 无输出；`flutter test test/src/app/ide_shell_widget_test.dart test/src/features/usage_statistics/presentation/agent_usage_panel_test.dart test/src/features/agent/architecture/` 全绿，并逐项覆盖 `01-需求分析.md` 的 5 条验收标准、G1 文件未引入 usage/Provider 分支、统计选择未写 active Provider；通用门禁全绿。 | 待做 |
| T14 | 同步长期文档与用户可见记录：更新 Workbench slot/Activity Rail 描述、标题栏入口、单卡片折叠统计、Compact Overlay、应用级恢复及使用统计约束；产品文档仍明确不扩展账号/购买能力；写入 `[未发布]`。不修改 AGENTS 门禁数量或 Provider capability。 | `docs/architecture/design_document.md`；`docs/architecture/overview.md`；`docs/architecture/overview.en.md`；`docs/guides/developer_guide.md`；`docs/product/product_requirements.md`；`CHANGELOG.md` | 中英文架构描述一致，长期文档不再把首页 Activity Rail/双统计卡片描述为现状，`CHANGELOG.md` 的 `[未发布]` 有本功能条目；通用门禁全绿。完成后才进入 `05-验收报告.md` 阶段，执行 G1/G9 自查与 Windows Profile 1280→1000→1280 十秒采样，不在本步骤中推断验收通过。 | 待做 |

## 阶段门

- T01～T14 全部为“完成”，且每一步对应提交都通过通用门禁，才算实现阶段结束。
- 之后单独进入验收阶段：逐条映射 `01-需求分析.md` 的 5 条验收标准，运行 G1/G9 自查，执行 Windows Profile resize 采样并把真实结果写入 `05-验收报告.md`。
- 本任务不应修改 `AgentEvent`、G1 共享适配层、Provider adapter、`AgentProviderBundle` 或 capability；任一步发现必须修改这些边界时，停止实施并回到方案评审。
