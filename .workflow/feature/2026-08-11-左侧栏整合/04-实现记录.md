# 左侧栏与 Agent 统计整合 · 实现记录

- 日期：2026-08-12
- 输入：`01-需求分析.md`、`02-方案设计.md`、`03-任务拆解.md`
- 当前进度：T01～T13 完成，下一步 T14

## T01 · 现有行为安全网

- 真实 `IdeHome` 新增左侧 Projects / Agent 统计区域切换回归：依次覆盖双区可见、
  仅统计可见、左栏全关与重新打开 Projects；每个阶段都断言 WindowFrame、Workbench、
  AgentPane Element、当前 Thread、Composer 草稿/模式及时间线滚动控制器与偏移不变。
- EffectRunner 新增迟到 scope 已释放与 listener generation 已推进两条完成 effect 回归，
  均不得触发旧无参完成回调。
- 会话状态新增 v1～v4 参数化宽容读取回归，覆盖重复/错误类型字段和未知字段，确认有效
  项目、展开目录仍可恢复。未添加未来 Workbench UI 断言，未修改生产代码。
- 验证：专项测试共 33 项全绿；`dart format .`（508 files，3 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1677 项通过、1 项既有
  跳过；`git diff --check` 通过。

## T02 · Workbench 布局状态模型

- 新增纯 Dart `IdeWorkbenchLayoutState`，覆盖左栏显隐、统计展开、可空栏宽、可空展开
  高度比例和统计 Provider 偏好；`copyWith` 能区分“未传值”与“显式清空”。
- JSON 投影逐字段宽容读取：布尔错误回退各自默认，栏宽只接受正有限数，高度比例只接受
  0～1 之间的有限数，Provider id 去首尾空白并把空串归一为空；未知字段不影响恢复。
  具体 UI 最小/最大尺寸仍由后续 presentation token 夹紧，本步未接入会话或 UI。
- 验证：专项测试 7 项全绿；`dart format .`（510 files，2 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1684 项通过、1 项既有
  跳过；`git diff --check` 通过。

## T03 · 会话快照与 Shell 持久化接线

- v4 `IdeSessionState` 新增 `workbench` 嵌套投影但不提升版本；v1～v4 缺字段均回退
  `IdeWorkbenchLayoutState` 默认值，嵌套单字段损坏或未知字段不会丢项目与 Thread。
- `buildIdeSessionState`、`sanitizeIdeSessionState` 和 `IdeShellController` 的恢复/当前快照
  路径完整携带 Workbench 状态。Shell 新增五个窄 setter，通过同一个不可变状态真源触发
  变更通知和现有延迟保存；`saveNow` 会取消待保存旧快照并写入最新值。
- Widget 冷启动回归确认恢复后的五字段在启动清洗与自动回存后不漂移；本步 UI 尚未消费
  这些字段，也未修改 session 版本或创建新 store。
- 验证：专项测试 49 项全绿；`dart format .`（510 files，最终 1 changed）；
  `flutter analyze` 为 `No issues found!`；最终全量 `flutter test` 共 1689 项通过、1 项
  既有跳过；`git diff --check` 通过。

## T04 · 紧凑额度窗口派生规则

- `AgentUsagePanelEntry.compactQuotaWindow` 仅在现有 `hasSubscriptionPlan` 为真时参与，
  从中立窗口列表选择正周期最短项；等周期保持原顺序，部分未知周期不阻断已知项，全部
  未知/非正周期时回退第一项，无窗口返回空。
- repository 与 quota 数据形状不变；源码检查确认实现和测试均无 Provider id、kind 或
  显示名分支。
- 验证：专项测试 6 项全绿；`dart format .`（511 files，1 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1695 项通过、1 项既有
  跳过；Provider 分支检查无输出；`git diff --check` 通过。

## T05 · 统计 Provider 选择状态

- `AgentUsagePanelController` 现在分别维护可提前恢复的 Provider 偏好与目录确认后的有效
  选择；构造参数和回调均保持可选，现有未接线 UI 的默认行为不变。
- 手动选择、Turn 终态选择和恢复入口使用同一规范化规则。目录到达前会保留最后一个终态
  偏好；目录到达后，有效偏好直接生效，失效偏好稳定回退目录首项，空目录清空两类状态。
  恢复 seed 本身不触发回写，避免持久化循环；实际回退和用户/终态变更才通知调用方。
- 原有 load token 竞态保护保留，并新增旧刷新流不能覆盖后续恢复选择的回归测试。
- 验证：专项测试 10 项全绿；`dart format .`（511 files，最终 1 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1701 项通过、1 项既有
  跳过；`git diff --check` 通过。

## T06 · 中立 Turn 终态信号

- 新增仅含 `providerId`、可空 `threadId` 与 `turnId` 的
  `AgentTurnTerminalSignal`，不携带正文、错误、命令、路径或 Provider raw payload。
- EffectRunner 继续先执行既有 live scope、listener generation、runtime/epoch 与 thread
  校验；通过后从 effect scope 取得 Provider/Thread，并从完成 effect 取得 Turn，交付 typed
  回调。同一 effect 的 once 语义同时覆盖 typed、旧无参与 attention 回调。
- 按迁移顺序暂时保留旧 `onTurnCompleted`；现有生产装配尚未切换，旧回调单独配置时行为
  保持不变。迟到 scope 与旧 generation 均不会触发 typed 或旧回调。
- 验证：专项测试 6 项全绿；`dart format .`（512 files，2 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1702 项通过、1 项既有
  跳过；`git diff --check` 通过。

## T07 · typed 终态链路切换

- `AgentConversationViewModel`、`AgentThreadWorkspaceController` 与 `IdeShellController`
  改为原样传递 `AgentTurnTerminalSignal`；生产装配不再传旧无参完成回调。每个 Workspace
  entry 的固定 Binding 仍是 Provider 身份真源，后台完成不会读取当前选中 entry 猜身份。
- Shell 将恢复的统计 Provider 偏好交给 `AgentUsagePanelController` 解析，并把控制器选择
  变化回写同一 Workbench 快照。`IdeHome` 收到终态后先执行 Provider 自动选择，再请求
  现有协调器做一次合并静默刷新；未写入会话默认 Provider。
- 回归覆盖 completed、failed、interrupted（含取消规范化）和 Plan handoff；两个前后台
  entry 分别交付 Grok/Codex 身份；真实首页从 Grok 恢复偏好跟随 Codex 终态、只刷新一次
  并持久化为 Codex，同时会话 active Provider 保持不变。
- 验证：四组专项测试共 141 项全绿；`dart format .`（512 files，8 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1707 项通过、1 项既有
  跳过；四个生产接线文件的旧回调搜索无输出；`git diff --check` 通过。

## T08 · 删除旧 Turn 完成回调

- 从 `DefaultAgentConversationEffectRunner` 删除临时旧无参 `onTurnCompleted` 参数、字段与
  执行分支，只保留 typed `onTurnTerminal`；完成 effect 现在只有一条终态通知路径。
- EffectRunner 的 once、scope/generation 拒绝与 dispose 测试全部改为直接断言 typed
  signal，T07 已切换的 ViewModel、Workspace、Shell 和首页接线无需再次改动。
- 验证：T06+T07 五组专项测试共 147 项全绿；`dart format .`（512 files，2 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1707 项通过、1 项既有
  跳过；仓库 `lib`/`test` 范围搜索旧回调无输出；`git diff --check` 通过。

## T09 · 标题栏左侧操作槽

- `WindowFrame` 新增默认空的 `titleBarLeadingActions`，macOS 在 traffic-light gutter
  之后渲染，Windows/Linux 在 Logo 与菜单之前渲染；未传参数与显式空列表的 Logo、菜单
  和拖拽区几何位置保持一致。本步未在首页启用。
- `WindowTitleBarAction` 新增可选 `FocusNode` 并原样传给 `PaneInteractiveSurface`；左右
  actions 共用按钮组实现，既有右侧间距、tooltip、语义标签与 active 视觉契约不变。
- 标题栏平台布局改用 Flutter `defaultTargetPlatform`，生产默认仍映射当前操作系统，同时
  允许单个 Widget 测试覆盖 macOS、Windows、Linux。测试还通过 mock 平台通道实际触发
  拖拽、最小化、最大化和关闭动作。
- 验证：专项测试 9 项全绿；`dart format .`（512 files，0 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1714 项通过、1 项既有
  跳过；`git diff --check` 通过。

## T10 · Cardless Agent 统计内容

- 新增公开 `AgentUsagePanelContent` 与 `AgentUsagePanelMode`，把业务内容从卡片外框拆出；
  旧 `AgentUsagePanel` 继续以 `PanelCard` 包装 expanded 内容，因此生产首页本步仍走旧路径。
- 折叠态按中立数据渲染：Provider 图标；付费套餐名或无套餐 Provider 名；T04 派生的最短
  周期额度与剩余进度；今日 Token 总量或 `-`。所有长文本限定单行省略，无 Provider
  id/kind/显示名分支。
- 冷加载使用紧凑三行呼吸 Skeleton；目录与单 Provider 错误均保留重试入口；展开态直接
  复用既有 Pane、Tabs、刷新、全量套餐窗口与 Token 明细，并提供可选 mode change action。
- 验证：三组专项测试共 25 项全绿；`dart format .`（512 files，2 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1720 项通过、1 项既有
  跳过；Provider 特判搜索与 `git diff --check` 均无输出。

## T11 · 合并左栏响应式容器

- 新增未接线的 `ProjectAgentSidebar`：单一 `PanelCard` 内 Projects 用 `Expanded` 占剩余
  空间，Agent 统计固定在底部；组件只接收两个业务 Widget，不读取 Provider 数据。
- 展开高度完全由一个 `LayoutBuilder` 的父约束、保存比例和 token 计算。空间充足时统计
  最低 220、Projects 最低 160；空间不足时优先保留 Projects，统计降到可用高度并由正文
  自身滚动。拖动预览只更新组件本地比例，结束时才回调一次，取消则恢复外部保存值。
- 增加 `agentUsageExpandedMinHeight`、`projectsPaneMinHeight` 与
  `agentUsageDefaultFraction` 三个尺寸 token。折叠态不创建 resize handle，也不改写保存比例。
- `flutter-build-responsive-layout` 技能促使实现严格依赖可用约束与 flex 分配，并补了不同
  父高度、1.4～2.0 倍文字缩放的无溢出回归；源码检查确认无 post-frame 测高、GlobalKey、
  orientation、设备或平台分支。
- 验证：专项测试 8 项全绿；`dart format .`（514 files，0 changed）；
  `flutter analyze` 为 `No issues found!`；全量 `flutter test` 共 1728 项通过、1 项既有
  跳过；禁用模式搜索与 `git diff --check` 均无输出。

## T12 · 真实首页切换到合并左栏

- `IdeHome` 的生产路径已使用标题栏左侧 action 与 `ProjectAgentSidebar`；Projects 和
  cardless Agent 统计只保留一个 `PanelCard`。标题栏入口按 Shell 的
  `leftSidebarVisible` 显示 active/tooltip/语义，旧 Activity Rail 暂留一提交，但 Projects
  与 Context action 分别操作同一左栏显隐和统计展开状态，不再维护第二套本地显隐/比例。
- 左栏显隐、统计展开、左栏宽度、统计高度比例和统计 Provider 选择均从 Shell 的同一
  Workbench 快照恢复。宽度与高度拖动逐像素仅本地预览，drag end 才提交；折叠不清空高度
  比例。重启回归从真实交互写入五项偏好，再验证隐藏状态、宽高、展开态和 Provider Tab
  完整恢复。
- Compact 继续复用 Navigation Overlay，scrim 与 Esc 都会关闭并持久化左栏隐藏，同时把
  焦点恢复到标题栏触发按钮。专项测试确认标题栏位于 Workbench 快捷键作用域外时，原有
  `CallbackShortcuts` 无法接收 Esc；因此只在真实 Overlay 挂载期间增加键盘监听，未扩展
  Scaffold API，未改变无 Overlay 页面快捷键。
- AgentPane、当前 Thread、Composer 草稿/模式、时间线滚动控制器与偏移在标题栏、旧 Rail、
  展开/折叠切换中保持同一 Element/状态；右侧 Files/Tools 与 `_ResizableColumn` 行为保留。
- 窄视口 Agent conversation 回归原先恢复“左栏可见”后直接点击被 Overlay 遮挡的 Project
  Home 会话；测试会话现显式恢复“左栏隐藏”，与该用例关注的 Plan/提问卡窄屏行为解耦，
  对应单测复验通过。
- 验证：三组专项测试共 39 项全绿，额外窄视口回归 1 项全绿；`dart format .`
  （514 files，0 changed）；`flutter analyze` 为 `No issues found!`；最终全量
  `flutter test` 共 1730 项通过、1 项既有跳过；`git diff --check` 通过。

## T13 · 删除首页旧左栏入口与兼容包装

- Agent 首页不再向 `IdeWorkbenchScaffold` 提供 `leadingRailBuilder`，并删除旧 Projects /
  Context Rail、对应 FocusNode 与 `_toggleLeftPanel`。`IdeActivityRail`、Workbench 的通用
  leading slot 及右侧 Files/Tools `_ResizableColumn` 保留，未缩减其它页面或未来 Inspector
  的通用能力。
- 删除临时 `AgentUsagePanel` 独立 `PanelCard` 包装，统计测试与生产首页统一直接使用
  `AgentUsagePanelContent`；最终首页只有 `ProjectAgentSidebar` 提供的一个卡片外壳。
- 新增最终架构守卫：禁止首页旧符号/旧 action 回流；确认 G1 application 与共享 ACP mapper
  不引入 usage 标识或具体 Provider 代码分支；截取 Turn 终态处理与 Shell 统计选择 setter，
  断言只更新统计控制器/Workbench 快照，不调用 active Provider 控制器。
- 五条验收标准分别由真实首页默认单卡片/无 Rail、紧凑统计状态矩阵、五项重启恢复、前后台
  Turn Provider 跟随隔离、标题栏与 Compact Overlay/AgentPane 保活回归覆盖。
- 验证：专项测试共 70 项、会话恢复测试 7 项全绿；`dart format .`
  （515 files，0 changed）；`flutter analyze` 为 `No issues found!`；全量
  `flutter test` 通过（1 项既有跳过）；旧符号搜索无输出；`git diff --check` 通过。
