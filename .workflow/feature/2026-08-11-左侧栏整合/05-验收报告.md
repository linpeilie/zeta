# 左侧栏与 Agent 统计整合 · 验收报告

- 日期：2026-08-12
- 验收范围：`dev...36a387e9`（T01～T14，共 14 个独立提交）
- 总结：**功能、架构与自动化回归通过；Windows Profile 性能门禁未通过，因此总体验收未通过。**
- 停线审计：实现期间未出现方案与实际代码不符、必须修改共享适配层 / Provider 端口、
  或单步改动量明显超出预估的情况；没有触发用户指定的三类停线条件。

## 1. 可观察验收标准

| 标准 | 结果 | 证据 |
|---|---|---|
| AC1 · 首页单一合并左栏 | 通过 | 真实 `IdeHome` 默认只有 `ProjectAgentSidebar` 的一个 `PanelCard`；Activity Rail 与独立统计卡片均不存在；Agent Canvas、Files 及 AgentPane 保活回归通过。 |
| AC2 · 折叠摘要状态完整 | 通过 | 覆盖付费套餐三行、无套餐两行、最短正周期窗口、稳定同序、Skeleton、错误、Token `-`、长文本省略和展开完整内容；不同高度与 1.4～2.0 倍文字缩放无 overflow。 |
| AC3 · 五项应用级恢复 | 通过 | 从真实交互写入并重启恢复统计 Provider、左栏显隐、统计展开态、栏宽和展开高度比例；失效 Provider 稳定回退，旧 v1～v4 与损坏字段宽容读取。 |
| AC4 · 任意 Turn 终态自动跟随 | 通过 | completed / failed / interrupted / cancel、Plan handoff、前后台两个 entry 均携带实际 Provider；最后终态覆盖手动统计选择，一次终态只刷新一次，active Provider 与 Binding 不变。 |
| AC5 · 标题栏唯一入口与 Compact Overlay | 通过 | 三平台标题栏位置、tooltip / semantics、隐藏后可操作、scrim / Esc 关闭与焦点恢复均通过；窄窗使用 Navigation Overlay，项目、Thread、草稿、滚动和持久化左栏状态不丢失。 |

## 2. 架构与范围门禁

- G1：`dev...HEAD` 未修改 Pipeline、CoalescingPolicy / Buffer、Dispatcher、TimelineStore
  或共享 ACP mapper；对现有 G1 代码去除注释后搜索，无具体 Provider 标识。
- Provider 边界：未修改 `AgentProviderBundle`、capability、Provider adapter 或 datasource；
  统计选择隔离架构守卫通过。
- G9：新增 Dart diff 中无裸色值、手写阴影、临时圆角、Material seed 色或旧 Shad API；
  `shadcn_flutter` 新增 import 均使用 `as sf`。新响应式组件无 post-frame 测高、
  `GlobalKey` 查高、设备 / 平台分支或 layout 后反馈环。
- 产品范围：套餐与账号 metadata 继续只读；没有登录、账号、购买、续费或支付能力扩张。

## 3. 自动化验证

- `dart format .`：515 files，0 changed。
- `flutter analyze`：`No issues found!`。
- 最终验收专项：110 项通过，覆盖真实 Shell、架构守卫、标题栏、合并栏、统计、会话恢复、
  retained page、响应式断点、投影 / diff / highlight 缓存与时间线虚拟化。
- 完整 `flutter test`：通过，保留 1 项既有跳过。
- `git diff --check`：通过。

结构计数与边界回归：

| 不变量 | 结果 |
|---|---|
| 隐藏 retained page build / layout 增量 | `0 / 0` |
| 同 revision 投影 / grouping 解析增量 | `0`（首次缓存后 compute count 保持不变） |
| resize 时 diff / highlight 可观察替换增量 | `0`（保持同一 `HighlightView` identity） |
| 100 个时间线 item 首帧构建 / 滚动后挂载上界 | `<30 / <40` |
| 2000 个动态高度 item 首帧构建上界 | `<100` |
| 外窗断点 | `1197 / 1196 / 1195px` 全绿 |
| Agent Canvas 断点 | `641 / 640 / 639px` 全绿 |

## 4. Windows Profile 采样

环境：Windows 11 企业版 LTSC 10.0.26100 x64，Intel Core i7-12700，47.7 GiB；
活动显示设备为 Intel UHD Graphics 770（驱动 32.0.101.7082，3840×2160），系统另安装
Oray 虚拟显示驱动。Flutter 3.44.4 stable、Dart 3.12.2。

场景：真实 Windows Profile 应用壳层，内存会话、空临时 workspace、一个合成 Provider
与只读统计快照；Agent 首页统计展开。启动稳定 3 秒后，在 10 秒内以约 25Hz 执行
`1280×800 → 1000×800 → 1280×800`，结束后等待 0.5 秒。未读取或写入用户
`~/.zeta`、CLI 配置或 Agent 历史；临时入口与目录均已删除。

| 轮次 | 帧数 | UI p95 | Raster p95 | 慢帧 | 慢帧率 | transient callback |
|---|---:|---:|---:|---:|---:|---:|
| 1 | 298 | 5.322ms | 26.927ms | 34 | 11.409% | `0 → 0` |
| 2 | 298 | 5.483ms | 26.200ms | 28 | 9.396% | `0 → 0` |

门槛为 UI / Raster p95 均不高于 16.7ms、慢帧率不高于 5%。两轮 UI 和 transient
callback 达标，但 Raster p95 与慢帧率稳定超标，故性能门禁判定为**未通过**。

本任务没有同构的改动前 Profile 基线，因此不能声称此次改动造成性能回归，也不能用相对
改善替代绝对门槛。虚拟显示驱动是环境事实，但不作为豁免或通过依据。

## 5. 结论与后续

五条用户验收标准、G1/G9、持久化兼容、Provider 隔离和自动化回归均通过，T01～T14
实现阶段完整结束。由于 Windows Profile 的 Raster 指标未达标，本分支当前**不满足最终
合入门禁**。

建议另开性能专项：在相同机器、显示路径和合成数据上补 `dev` 同构基线，再用 DevTools
GPU timeline 定位 Raster 开销；若调整显示驱动或采样环境，必须重新建立两边基线，不能
与本报告数值直接对比。
