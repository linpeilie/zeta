# 多语言支持 · 实现记录

只记录方案偏离、临时决策或阻塞。

## 2026-08-16 · 步骤 2 · ARB 简体中文文件名

`02-方案设计.md` 指定 `app_zh_Hans.arb`，且完成判据写「仅声明 en/zh-Hans」。

当前 Flutter `gen-l10n` 在只有 `zh_Hans`、没有基础 `zh` 时直接失败：要求先有 `{fileName}_zh.arb` 作为 fallback。若同时提交 `app_zh.arb` 与 `app_zh_Hans.arb`，生成的 `supportedLocales` 会变成 `en` / `zh` / `zh_Hans` 三个条目。

**临时决策**：步骤 2 使用 `app_en.arb` + `app_zh.arb`（`@@locale: zh`）两份资源。产品语义仍是简体中文；domain 持久化码 `zh-Hans` 按步骤 4 的解析函数处理，不把 `zh` 当成第三种界面语言。未新增共享适配层或 Provider 端口。

若后续必须在资源层出现 `zh_Hans` 标识，需要另评：接受 Flutter 的 `zh` fallback 文件，或改 Flutter 工具链配置。

步骤 2 的 Widget 测试宿主默认使用 `en`。`sf.ShadcnApp` 自带的 `ShadcnLocalizations` 只支持英语，在步骤 3 补齐适配器之前不能把宿主 Locale 设为 `zh`，否则 Flutter 会报 delegate 不支持。中文 getter 用 `AppLocalizations.delegate.load` 单独验证。

## 2026-08-16 · 步骤 3 · Calendar 月份/星期保持英文 token

`formatDateTime` 是 `ShadcnLocalizations` 的 extension，会拼接 `monthJanuary` 等 getter。若把月份译成中文，D6 要求的日期格式快照会随语言变化。

**临时决策**：星期缩写、月份全称/缩写、AM/PM、YYYY/MM/DD 类 token 在 en/zh 使用相同英文值；Command、表单、菜单、DatePicker 占位等静态句翻译。生产根仍未接入。

## 2026-08-16 · 步骤 6 · 等待设置仅在文件持久化时启用

方案要求 `MainApp` 在常规设置加载完成前不挂有文字 UI。Widget 测试普遍在 `pumpWidget` 后立即点图标，若内存宿主也等一帧会大面积改测试。

**临时决策**：`waitForGeneralSettings` 默认 false，由生产 `main` 显式传 true。带 `dataPaths` 的 Widget 测试若默认等待，会在第一帧找不到 IdeHome。显示语言生产固定简体中文，测试用 `displayLanguageOverride`。生成的 `AppLocalizations.delegate` 是异步的，首帧会空白；app 组合层用 `SynchronousFuture` + `lookupAppLocalizations` 同步加载。

## 2026-08-16 · 步骤 7 · 中文 ARB 对齐当前混语

为保持「中间提交不改变当前用户语言行为」，zh 资源逐字沿用现有界面（含 `Open folder`、`Usage statistics` 等英文 chrome）。相对时间（刚刚 / N 分钟前）按 D6 未迁入 ARB。`ProjectThreadsController` / `IdeShellController` 的 application 错误句仍是固定语言，下一步再接文本目录。

## 2026-08-16 · 步骤 8 · fallback 目录与格式器

使用统计 application/data 需要一份无 Flutter 的目录实现。生产 Locale 仍固定简体中文，因此 `FallbackUsageStatisticsTextCatalog` 与 zh ARB 逐字对齐，供未注入 `AppUsageStatisticsTextCatalog` 的测试与 shell 构造。扫描工具跳过该文件，避免把已迁入 ARB 的副本再登记 allowlist。

日期、数字、百分比、相对时间 formatter 保持语言无关；ARB placeholder 一律 `String`，不使用 int/plural/date formatter。

## 2026-08-16 · 步骤 9 · fallback 目录与剩余 chrome

Agent 管理 application/data 注入 `AgentManagementTextCatalog`，默认 `FallbackAgentManagementTextCatalog` 与 zh ARB 对齐；扫描跳过该 fallback 文件。页面/编辑器/日志主体已走 `context.l10n`。部分诊断细节、接入指引长文、相对时间和少数状态 chip 仍保留当前中文，待步骤 14 清扫。未改 G1 共享层或 Provider 端口。

## 2026-08-16 · 步骤 10 · presentation 直出文案

AgentPane 部件与文件证据走 `context.l10n` + `agent_presentation_l10n` extension。ViewModel 中无 context 的状态胶囊/刷新错误、导航 tooltip 仍是固定中文，留给步骤 11–12 的 `AgentUiTextCatalog`。未改 TimelineStore 等 G1 文件。Composer 的 Send/Cancel 与审批卡的「取消」拆成不同 ARB key，避免混语 chrome 互相覆盖。

## 2026-08-16 · 步骤 11 停线：缺架构 Issue，且注入路径与代码不符

步骤 10 已完成。步骤 11 要求修改 G1 的 `AgentConversationTimelineStore`，并贯穿 `AgentUiTextCatalog`。按 `03-任务拆解.md` 执行规则 4 与 `02-方案设计.md` §4.1：未取得经确认的架构 Issue 时停在步骤 10。

核验：

- `.workflow/feature/2026-08-12-多语言支持/` 内无 Issue 编号或批准链接。
- 本机无 `gh`；GitHub API 对 `linpeilie/zeta` 返回 Not Found（私有仓或未授权），无法确认远端是否已有审查 Issue。
- 未改生产代码、测试或步骤表状态。

同时发现方案与实际构造链对不上，不能按字面开写：

| 方案步骤 11 | 当前代码 |
|---|---|
| catalog 作为 required 依赖贯穿 Provider factory、binding、view model、live/history/replay reducer 与 TimelineStore | `DefaultAgentProviderFactory` 只 `createBundle`；Binding 不持有 TimelineStore/reducer。Store 与 `AgentConversationReducerContexts` 都在 `AgentConversationViewModel` 构造；ViewModel 由 `AgentThreadWorkspaceController` 创建。 |
| 只把「思考」fallback 改走目录 | 共享层唯一用户可见 canary 是 `AgentConversationTimelineStore.appendReasoningDelta` 的 `existing?.title ?? '思考'`。reducer 另有「模型已改道」「适配层弃用提示」，属步骤 12。 |
| 不得用全局 singleton；required 注入 | 步骤 8–9 的 usage/management 目录是可选参数 + `Fallback*`（与 zh ARB 对齐）。`ZetaTextCatalogs` 已有 app adapter，但生产装配仍走 fallback。 |

若按方案字面把 catalog 塞进 factory/binding，会扩大到 Bundle 生命周期，而 canary 并不在那条链上。若只按实际构造注入 ViewModel → Store（reducer 本步可先接线不用），则与步骤表「贯穿 factory/binding」不完全一致。

未改 G1 文件、未改 Provider 端口、未改 `AgentEvent`。

### 恢复条件

需要你拍板下面两件事后才能开始步骤 11：

1. **G1/G2 Issue**：给出已审查通过的 Issue 链接，或在本对话明确批准 §4.1 的中立性论证（固定中文 fallback → 构造函数注入不可变 `AgentUiTextCatalog`；不读 raw/extra；不增加 Provider 分支；不改 identity/merge）。
2. **注入路径**：按实际构造只贯穿 ViewModel → TimelineStore，reducer 本步只加 required 参数、暂不消费；还是坚持字面贯穿 factory/binding（即使它们当前不创建 Store）。

步骤 11 在 `03-任务拆解.md` 保持「待做」。

## 2026-08-16 · 步骤 11 恢复并完成：批准 §4.1，接线用 A

用户在对话中明确批准 `02-方案设计.md` §4.1，并选择接线路径 A。本记录作为本次 G1 改动的批准凭据。

实施边界：

- 不改 `DefaultAgentProviderFactory`、Binding、`AgentEvent`、bundle/capability。
- catalog 从 `MainApp`（冻结 Locale → `ZetaTextCatalogs.agentUi`）经 `IdeHome` → `IdeShellController` → `AgentThreadWorkspaceController` → `AgentConversationViewModel` 注入 `AgentConversationTimelineStore`。
- live/history/replay reducer 接收同一不可变目录对象，本步不消费；仅 TimelineStore 的思考卡 fallback 改走 `thinkingToolTitle`。
- 构造函数保留 `FallbackAgentUiTextCatalog` 默认值（与步骤 8–9 相同，对齐 zh ARB），避免机械改写全部测试构造；生产路径由 MainApp 注入 App adapter。
- 刷新 T18 中 TimelineStore 内容基线。

## 2026-08-17 · 步骤 12 · catalog 扩面与装配

接线沿步骤 11 的 A 路径，并补上 Provider-local 装配，不改 bundle 端口或 `AgentEvent`：

- `AgentUiTextCatalog` 覆盖 tool/activity、Plan 交接、改道/弃用、error guidance，以及 Codex/Grok/Claude 的 Zeta fallback。
- `displayTitle` 放到 catalog extension，避免 `agent_tool_models` 与 catalog 循环依赖；已删除 `agentToolKindLabel` / `agentActivitySegmentLabel`。
- factory / `create*Bundle` 增加可选 `textCatalog`（默认 Fallback）。`MainApp` 先冻结 Locale 再创建 factory，把同一目录注入 ViewModel 链与 Provider adapter。
- model selection / permission / mode controller 与额度窗口时长 formatter（「N 周」）仍是固定中文，留给步骤 14。
- 刷新 T18 中 TimelineStore 内容基线。

## 2026-08-17 · 步骤 13 · 通知目录与 macOS 菜单 configure

- Windows/Linux Flutter 标题栏菜单在步骤 7 已走 `context.l10n`，本步不再改文案。
- macOS 自有 File 菜单改为收到 `zeta/menu` `configure` 后才安装。生产 Locale 仍冻结简体中文，因此该菜单从启动即英语变为 configure 后的中文标签，与 Windows/Linux 对齐。未知版本/缺字段返回 `FlutterError`，不安装也不更新。
- 通知 payload 仍是 version/providerId/threadId/identity，不含 title/body。
- Linux `defaultActionName` 在 service 构造时从目录读取；测试未注入时默认仍是「打开 Zeta」。
- Windows 上 `Process.run('dart'|'flutter')` 找不到 `.bat`。护栏/契约测试改为 `runInShell: true`，不改变断言。

## 2026-08-17 · 步骤 14 停线：剩余库存过大，无法一次保持全绿

步骤 14 要求把 allowlist 收到只剩品牌/术语/raw/协议 key/日志，并补架构护栏 + 真实 `IdeHome` 双语言覆盖。

启动时扫描仍有 **446 条 zeta_copy**（约 **301 条不重复中文**，73 个文件）。按步骤 12 已迁完的 Agent context-free 之后，剩余主要是：

- Agent presentation 直出（ViewModel、卡片、mode selector、文件证据、导航）
- Agent 管理页诊断/状态 chip 与部分 data 建议
- Provider 运行时状态句（Starting/ready/连接关闭）
- Usage 窗口标签与「暂无数据」
- application 错误包装（权限/模型/模式/Shell）

本步已做、且 `flutter analyze` 通过：

- scanner 跳过 `*_preview.dart`、FlutterError/ErrorSummary/ErrorHint/ArgumentError/assert
- 去掉 ui/core 的 `l10nOrNull ?? 中文` 兜底；`IdeDialogAction` 取消/确认改为必传 label
- 相对时间抽到 `formatLocalizedRelativeTime`，首页/项目首页/管理页改走 ARB token
- 上下文面板角色/事件标签、管理页模型 chip 与 Claude 接入指引改走 ARB
- 已补一批 ARB key，但 application catalog 与大量 presentation 直出尚未接线

未改 G1 共享层、未改 Provider 端口、未改 `AgentEvent`。步骤 14 在任务表保持「待做」。

### 需要你拍板

1. **继续本步扫完**（预估还要改 50+ 文件、上百 ARB key，才能把 allowlist 收到目标形态）
2. **把步骤 14 拆成两段**：本提交先落地 scanner/架构测试/已接线的 ui/core 与管理页/相对时间；下一段再清 presentation/data 剩余字面量并重建 allowlist

## 2026-08-17 · 步骤 14 拆成 14a / 14b

用户选择方案 2。原步骤 14 拆成：

- **14a**（本提交）：scanner 跳过规则、ui/core 去中文兜底、相对时间 helper、首页/管理页已接线文案、import layering / 持久化不含 UI copy / 真实 `IdeHome` 已迁 chrome 双语覆盖。allowlist 仍保留未迁债务。生产 Locale 仍冻结简体中文。
- **14b**（下一步）：清扫 Agent presentation、application 错误包装、Usage 窗口、Provider 运行时状态句等剩余字面量，并把 allowlist 收到品牌/术语/raw/协议 key/日志。

未改 G1 共享层、未改 Provider 端口、未改 `AgentEvent`。原步骤 14 不标完成。

## 2026-08-17 · 步骤 14b 停线：剩余库存仍超出单步可独立全绿范围

启动 14b 时扫描仍有 **365 条 / 64 文件**。本段已做且定向测试通过：

- scanner 把 `StateError` / `UnsupportedError` 与纯格式碎片（`${}`、`%`、`·`）排除出 zeta_copy
- 管理页 / 日志页剩余 chrome 改走已有 ARB（含少量新 key）
- Usage 空值、相对时间、按量套餐标签、统计摘要/卡片计数改走 l10n
- `IdeTabs` 加载语义去掉中文兜底；虚拟滚动条语义改走 ARB

收口后仍剩 **265 条 / 51 文件**，主要是：

- Agent presentation（context / ViewModel / messages / cards / mode / evidence / composer）
- application 错误包装（权限 / 模型 / 模式 / Shell / 外观字体）
- Provider data 运行时状态句与 management repository 建议
- domain 窗口标签、Cursor 退役文案、默认会话标题

未改 G1 共享层、未改 Provider 端口、未改 `AgentEvent`。步骤 14b 保持「待做」，allowlist 尚未收到目标形态。

### 需要你拍板

1. **继续在本步扫完**（预估还要改 50 个文件、上百 ARB/catalog 接线）
2. **把 14b 再拆两段**：下一段先清 Agent presentation；再下一段清 application/data/domain 并重建 allowlist
3. **把 14b 拆三段**：presentation → application catalogs → data/domain + allowlist

## 2026-08-17 · 步骤 14b 续：Agent presentation 主体已接线

用户说「继续」。本段把 Agent presentation 主 chrome 接到已有/新增 ARB，ViewModel 用户可见状态改走 `AgentUiTextCatalog`。

已接线：context panel、messages、plan panel、navigation/rail、mode selector、model config 触发器、cards 提问/执行权限、composer Plan badge、skill/slash 空态、Cursor 不可用卡、ViewModel 胶囊/模式/模型刷新/权限切换/ready/working/history/branch。

文件证据明细（evidence views 长语义）和少数 composer/model 语义拼接仍留。application/data/domain 未动。

未改 G1 共享层、未改 Provider 端口、未改 `AgentEvent`。步骤 14b 仍不标完成。
