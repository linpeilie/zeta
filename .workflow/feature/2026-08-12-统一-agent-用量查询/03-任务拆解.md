# 统一 Agent 用量查询 · 任务拆解

- 日期：2026-08-12
- 输入：`01-需求分析.md`、`02-方案设计.md`
- 状态：已确认，T00–T11 已完成，待执行 T12
- 档位：L

## 0. 执行约束

- 按表中顺序执行；每一步从上一步的绿色状态开始，单独可提交、可回滚。
- 每完成一步，执行 `dart format .` → `flutter analyze` → 该步列出的相关测试，并把该步状态改为“完成”。
- 实施期发现方案与实际代码不符、必须改共享适配层或 Provider 端口、或某步改动量明显超出表中估计时，**立即停止并请用户确认**，先将原因写入 `04-实现记录.md`。
- 用户已确认总体方案，并明确要求删除原 G7 私有数据读取禁令；T00 按“删除门禁 + 原 G8/G9 顺延为新 G7/G8”执行，不采用精确路径白名单。
- T01–T10 是架构收敛，要求 Codex/Grok 可见行为不变；T11–T12 才新增 Claude Code Token 统计。
- T05 命中持久化格式演进；开始前必须建立 `.workflow/migration/2026-08-12-usage-statistics-index-v4/` 并按专项 D 补齐兼容策略，不跳过迁移阶段门。
- 不修改 `dart_test.yaml` 并发度，不引入新依赖。

## 1. 先加后删的迁移对

| 先加 | 切换 | 后删 |
|---|---|---|
| T02 新 token source 契约和旧 Codex/Grok adapter | T08 生产 query service 切换 | T10 删旧 composite/panel/provider loader 路径 |
| T05 v4 partition store 并支持 v2/v3 读取 | T06/T07 Codex/Grok source 切到 v4 store | T10 删 provider-typed v3 store API |
| T11 Claude source 自有测试路径 | T12 app registry 注册 Claude | 无；旧 Claude 历史读取继续作为 source 依赖 |

## 2. 步骤表

| 序号 | 做什么 | 涉及文件 | 完成判据 | 状态 |
|---|---|---|---|---|
| T00 | 【前置规则】删除原 G7“不得读取其他 CLI 私有数据”门禁，允许 Provider 自有 data adapter 按功能读取 CLI 私有数据；原 G8/G9 顺延为新 G7/G8，同步权威规则、长期文档、贡献指南、提示词和代码注释。读取权限不自动授权迁移、改写或删除。此步不改业务查询和 UI。 | `AGENTS.md`；`CLAUDE.md`；`CONTRIBUTING*.md`；`docs/architecture/engineering_standards.md`、`overview*.md`；`docs/guides/developer_guide.md`；`docs/prompts/`；受门禁编号影响的协议文档、注释和测试名称 | 非历史长期文档只声明 8 条门禁且无旧 G7 禁读表述；Provider 私有读取归属 data 层，新 G7 仍保护敏感持久化，新 G8 仍保护主题；`dart format .`、`flutter analyze` 和相关架构/安全测试全绿 | 完成 |
| T01 | 先补行为安全网，不改生产代码：固定 quota 失败不隐藏本地记录、单历史 source 失败不丢其他 Provider、面板目录顺序/逐项完成/静默刷新、Controller 旧请求丢弃、v2/v3 损坏降级和并行分区 merge。已知将被修正的“侧栏/完整页路径漂移”和“侧栏不写回共享索引”不作当前行为断言。 | `test/src/features/usage_statistics/data/provider_agent_usage_panel_repository_test.dart`；`codex_usage_statistics_repository_test.dart`；`grok_usage_statistics_repository_test.dart`；`composite_usage_statistics_repository_test.dart`；`usage_statistics_index_store_test.dart`；`application/agent_usage_panel_controller_test.dart` | characterization tests 在当前代码上全绿；只增强对已存在可见行为的覆盖；`flutter test test/src/features/usage_statistics/` 全绿 | 完成 |
| T02 | 【先加】新增中立 `AgentUsageQuery`、`AgentTokenUsageSource`、source snapshot 和 supported/unavailable 状态；建立 Codex/Grok legacy adapter，先委托旧 repository，不切生产路径。 | 新增 `lib/src/features/usage_statistics/domain/agent_usage_query_models.dart`、`agent_token_usage_source.dart`；新增 `data/providers/codex/codex_token_usage_source.dart`、`data/providers/grok/grok_token_usage_source.dart`；对应新测试 | domain 无 Flutter/IO/Provider raw import；adapter 与旧 repository 对同 fixture 产出 canonical records 一致；`flutter test test/src/features/usage_statistics/domain/ test/src/features/usage_statistics/data/` 全绿 | 完成 |
| T03 | 新增中立 `AgentUsageQuotaSource` 契约与 `AgentUsageQueryService`；对单 Provider 并行读 quota/history，区分 unsupported/unavailable，局部失败互不阻断，支持目录顺序渐进发布和同 key in-flight 合并。此时只用 fake quota/token source，不切生产 UI；global-runtime data 实现留到 T08 组合时接入。 | 新增 `lib/src/features/usage_statistics/domain/agent_usage_quota_source.dart`、`application/agent_usage_query_service.dart`；新增 application 测试 | 测试覆盖 quota 失败/history 成功、history 失败/quota 成功、两者 unsupported、多 Provider 乱序完成但目录稳定、并发合并、force refresh 不误复用；application 不 import `AgentProviderGlobalRuntime`；相关测试全绿 | 完成 |
| T04 | 将“当日 Token 求和”从面板具体仓储提取为 application 级中立函数；用 query service 新增 `AgentUsagePanelRepository` 和 `UsageStatisticsRepository` 适配器，保留现有 Controller 契约，仍不切生产装配。 | 新增/modify `usage_statistics/application` 下 query adapter/聚合函数；新增对应测试 | 同一 fake snapshot 通过新 panel adapter 产出当前 `AgentUsagePanelLoadEvent` 序列；通过新 statistics adapter 产出当前 `UsageStatisticsSourceSnapshot`；Controller 现有测试无需改可见断言并全绿 | 完成 |
| T05 | 【持久化先加】按专项 D 建立 v4 迁移产物，实现 Provider 不透明 `UsageStatisticsPartitionStore`；支持 v2/v3 读取、v4 往返、未知分区保留、损坏降级、串行分区 merge。保留旧 typed store API 作为临时迁移路径。 | 新增 `lib/src/features/usage_statistics/data/usage_statistics_partition_store.dart`；调整 `usage_statistics_index_store.dart`；`test/src/features/usage_statistics/data/usage_statistics_index_store_test.dart`；新增 `.workflow/migration/2026-08-12-usage-statistics-index-v4/01-04` 阶段文档 | 五类持久化测试全绿：v4 往返、真实 v2/v3 形状迁移、损坏输入、缺/未知字段、重复迁移幂等；并行不丢分区；旧测试仍绿 | 完成 |
| T06 | 将 Codex source 从 legacy repository 委托改为直接使用 partition store 和 Codex codec；移除 source 中 quota/raw Provider loader 职责；将仅为统计服务的 Codex usage scanner 搬至 `usage_statistics/data/providers/codex/`。 | `data/providers/codex/` 下 source、scanner、partition codec；移动对应测试 | 同一 fixture 与旧路径 canonical records 逐条一致；累计 Token 非负差分、重复 rollout、损坏行、增量 cache 和 forceRefresh 测试全绿；source 无 `AgentProvider`/`AgentUsageQuotaProvider` 依赖，raw/路径不上浮 | 完成 |
| T07 | 将 Grok source 从 legacy repository 委托改为直接使用 partition store 和 Grok codec；移除 source 中 quota/raw Provider loader 职责；将仅为统计服务的 Grok usage scanner 搬至 `usage_statistics/data/providers/grok/`。 | `data/providers/grok/` 下 source、scanner、partition codec；移动对应测试 | 同一 fixture 与旧路径 canonical records 逐条一致；cached/reasoning Token、损坏行、增量 cache 和 forceRefresh 测试全绿；source 无 `AgentProvider`/`AgentUsageQuotaProvider` 依赖，raw/路径不上浮 | 完成 |
| T08 | 【切换】`app.dart` 继续构建并注入 partition store；`IdeShellController` 在建立 global runtime 和 Provider 设置控制器后，构建 `GlobalRuntimeAgentUsageQuotaSource`、Codex/Grok source factory registry、query service 和两个 UI controllers。`IdeHome` 改为只消费 shell controller，删除对 usage data 实现的构造。 | `lib/src/app/app.dart`；`lib/src/app/shell/ide_shell_controller.dart`；`lib/src/ui/features/ide/views/ide_home.dart`；新增 `data/global_runtime_agent_usage_quota_source.dart` 和必要的 app 组合工厂；`test/src/app/ide_shell_controller_test.dart`；`ide_shell_widget_test.dart` | 真实 `IdeHome` 下侧栏和统计页均走新 query service；Codex/Grok 可见结果、静默刷新、页面保活均不变；quota data source 只在 global-runtime callback 中读 bundle；`rg -n "usage_statistics/data" lib/src/ui/features/ide/views/ide_home.dart` 无输出；相关 widget/app 测试全绿 | 完成 |
| T09 | 加固架构守卫：UI 不 import usage data；query service/partition store 不 import Codex/Grok/Claude 自有类、不按 Provider kind/id 分支；Provider source codec 不写敏感字段；专用 legacy decoder 之外不出现旧 Provider 分区分支；G1 共享文件无改动。 | 新增 `test/src/features/usage_statistics/architecture/usage_statistics_layering_test.dart`；必要时扩展既有 agent architecture guard | architecture tests 全绿；G1 自查无非注释 Provider 残留；`git diff` 确认 G1 共享文件未改 | 完成 |
| T10 | 【后删】删除无生产调用方的 `ProviderAgentUsagePanelRepository`、`CompositeUsageStatisticsRepository`、旧 Codex/Grok repository quota/provider-loader 路径以及 provider-typed store API；将仍有价值的测试转移到 source/query service，不修改可见行为断言。 | 删除/收敛 `lib/src/features/usage_statistics/data/` 旧文件；对应测试搬迁；`app.dart` / `ide_home.dart` import 收口 | `rg` 确认旧类无生产调用和死代码；`flutter test test/src/features/usage_statistics/ test/src/app/ide_shell_widget_test.dart` 全绿；索引只有 v4 生产写路径 | 完成 |
| T11 | 【Claude 先加】实现未注册的 `ClaudeCodeTokenUsageSource` 和自有 partition codec；复用独立 history reader，按 `(providerId, threadId, turnId)` 去重，本回合 usage 按绝对值处理，不切生产 UI。 | 新增 `lib/src/features/usage_statistics/data/providers/claude_code/claude_code_token_usage_source.dart`、codec 和测试；仅在确有必要时给 `ClaudeCodeSessionHistoryReader` 增加中立只读窄方法 | 用脱敏 fixture 覆盖完成/失败/中断回合、cache create/read Token、重复记录、损坏行、时间窗和增量索引；与现有 history/live canonical usage 逐位置一致；raw/路径/凭据不进入统计快照；AgentEvent/G1 文件无改 | 完成 |
| T12 | 【Claude 切换】在 app source registry 注册 Claude Code，面板和完整统计页自然获得当日/历史 Token；保留已有 quota 端口、60s 节流和账号数据增强开关语义。 | `lib/src/app/shell/ide_shell_controller.dart` 或 app 组合工厂；`agent_usage_panel_test.dart`；`usage_statistics_page_test.dart`；`ide_shell_widget_test.dart` | 已启用 Claude 有历史时展示今日 Token 且可在统计页筛选；无历史时不伪造 0；quota 失败不影响 Token，Token 失败不影响 quota；三 Provider 并行加载顺序稳定；相关测试全绿 | 完成 |
| T13 | 文档收口：同步使用统计 query/source 接入规则、app-only composition、v4 分区、Claude Code Token 统计和索引白名单；纯重构不写 changelog，只记录 Claude 新能力。 | `AGENTS.md`；`docs/architecture/engineering_standards.md`、`design_document.md`、`overview.md`、`overview.en.md`；`docs/guides/developer_guide.md`；`docs/product/troubleshooting.md`、`.en.md`；`CHANGELOG.md` | 长期文档不再把生产索引限定为 `providers.codex/grok`；新 Provider 接入步骤不要求修改共享 Store/query；中英文概览一致；`CHANGELOG` 只有用户可见项 | 待做 |
| T14 | 最终验收：运行全量格式化、静态分析、测试、G1/G6/G7/G8 自查和旧路径搜索；逐条对照 `01-需求分析.md` 的 8 条验收标准，结果写入 `05-验收报告.md`。 | 新增 `05-验收报告.md`；无业务代码预期变更 | `dart format .`、`flutter analyze`、`flutter test` 全绿；G1 自查无输出；Provider 私有读取不穿透 data 层且索引无敏感字段；旧 repository/store API 无生产调用；报告已脱敏并对每条验收给出测试证据 | 待做 |

## 3. 每步固定验证命令

每步先执行：

```sh
dart format .
flutter analyze
```

然后执行步骤中列出的相关测试。T08、T10、T12、T13 是生产切换/收口点，除相关测试外再执行：

```sh
flutter test
```

所有步骤均执行 `git diff --check`。

## 4. 预估与回滚点

| 阶段 | 预估 | 回滚点 |
|---|---|---|
| T00 规则对齐 | 中 | 只修改规则、长期文档、提示词和编号引用；若仍有非历史禁读表述或门禁编号漂移则停止，不进入 T01 |
| T01–T04 契约/安全网 | 中 | 新路径未接入生产，可整体回退新 source/service，旧行为不受影响 |
| T05–T07 索引与 Provider source 演进 | 大 | 旧 typed store API 和生产 repository 仍保留；T08 前可不切换 app 装配 |
| T08–T10 生产切换 | 大 | T10 前旧 repository 仍存在；如新 query 结果不对齐，回切 app 装配即可 |
| T11–T12 Claude 能力 | 中 | T12 前 Claude source 未注册；如实际语义不符，不切换即可 |
| T13–T14 收口 | 小 | 只是文档与验收，不改运行时语义 |

## 5. 阶段门

- 用户已确认；T00 完成前不进入 T01，不修改业务查询或 UI。
- T00 必须先完成；权威规则、长期文档和当前设计仍冲突时不得进入 T01。
- T01–T10 全部完成且 Codex/Grok 行为对齐后，才允许实施 Claude Code Token 历史。
- 任意步骤命中 `02-方案设计.md` §11.2 的止损线时必须停止，不以临时 compatibility 分支绕过。
- T14 全量验收完成后才能标记整个任务完成。
