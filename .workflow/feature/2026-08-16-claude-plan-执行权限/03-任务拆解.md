# Claude Plan 执行权限 · 任务拆解

- 日期：2026-08-16
- 输入：`02-方案设计.md`
- 状态：T01–T07 完成；通用门禁已过（format / analyze / flutter test 2000 绿）；待验收
- 档位：L

## 执行约束

- 按表中顺序执行；每一步从上一步的绿色提交开始，单独提交、可单独回滚。
- 实施时每完成一步，把本表对应状态改为「完成」并放进同一提交；若必须改 G1 共享适配层、新增 bundle 端口、或按 `providerId`/kind 分支，立即停止并记入 `04-实现记录.md`，回到方案评审。
- 在本任务的干净分支中执行，不夹带无关改动。
- 每一步先跑该行列出的专项命令，再跑通用门禁：改过 Dart 则 `dart format .`；始终 `flutter analyze` 与 `flutter test`。完成结果必须是格式化成功、`No issues found!`、`All tests passed!`，且 `git diff --check` 无输出。
- 「先加后删」：T01～T03 只加字段与新 API，生产调用仍走旧路径；T04～T05 切到新路径；T06 只改文案。本任务 **不删除** `selectionBeforeCurrentUserSelection`（其它语义仍用），只停止交接路径依赖它。

## 步骤表

| 序号 | 做什么 | 涉及文件 | 完成判据 | 状态 |
|---|---|---|---|---|
| T01 | 【先加】`AgentPermissionOption` 增加 `planningOnly`，默认 `false`，纳入 `==`/`hashCode`。构造处不传该参数则行为与现在完全一致。不改 catalog、不改交接。 | `lib/src/features/agent/domain/agent_permission_policy_models.dart`；`test/src/features/agent/domain/agent_permission_policy_models_test.dart`（无则新建并补默认/相等性） | `flutter test test/src/features/agent/domain/agent_permission_policy_models_test.dart`：默认 false、显式 true 不相等、拷贝构造兼容；`rg -n "planningOnly" lib/src/features/agent/domain/agent_permission_policy_models.dart` 有字段；生产其它层无引用亦可。通用门禁全绿。 | 完成 |
| T02 | 【先加】Claude 权限目录给 Plan 项打 `planningOnly: true`，其余三档 false。只改 data catalog，application 仍不读该标记。 | `lib/src/features/agent/data/mappers/claude_code_permission_mode_codec.dart`；`test/src/features/agent/data/mappers/claude_code_permission_mode_codec_test.dart` | `flutter test test/src/features/agent/data/mappers/claude_code_permission_mode_codec_test.dart`：断言 `catalog()` 中 `:plan` 为 `planningOnly == true`，`:ask` / `:accept-edits` / `:bypass` 为 false；既有编解码用例仍绿。通用门禁全绿。 | 完成 |
| T03 | 【先加】权限控制器新增 `selectionBeforePlanningOnly`（进 planning 项时记下非 planning 选择；有效条件为同 Binding/thread，**不**绑 runtime generation）和 `adoptSessionPermission`（`syncPort: true`、`persistDefault: false`）。生产调用方暂不改，旧 `selectionBeforeCurrentUserSelection` / `selectOption` 行为与测试保持。 | `lib/src/features/agent/application/agent_conversation_permission_selection_controller.dart`；`test/src/features/agent/application/agent_conversation_permission_selection_controller_test.dart` | `flutter test test/src/features/agent/application/agent_conversation_permission_selection_controller_test.dart`：覆盖选到 planningOnly 后 generation+1 仍能读到进规划前、切 thread/Provider 清空、adopt 不调用 persist、旧 `selectionBeforeCurrentUserSelection` 用例仍过。通用门禁全绿。 | 完成 |
| T04 | 【先加】`reconcileExecutionPermission` / `selectExecutionPermission` 丢弃 `planningOnly` 种子与卡上选择；**有权限端口时**不得再产出无 optionId 的 `providerFallback`（无可用非 planning 项则 `executionPermission == null`）。无端口路径保持 providerFallback。ViewModel 仍用旧暂存 API。 | `lib/src/features/agent/application/agent_plan_execution_handoff_controller.dart`；`test/src/features/agent/application/agent_plan_execution_handoff_controller_test.dart` | `flutter test test/src/features/agent/application/agent_plan_execution_handoff_controller_test.dart`：planningOnly 种子回落到非 planning 的 catalog 默认；catalog 默认也是 planning 则取第一项非 planning；都没有则 permission 为 null；卡上选 planning 被拒；`supportsPermissionSelection: false` 仍 fallback。既有 beforePlan / generation fallback / override 用例仍绿。通用门禁全绿。 | 完成 |
| T05 | 【切换】交接与执行走新路径：`stageProviderApprovedPlan` / `offerCompletedPlan` 改传 `selectionBeforePlanningOnly`（不再传当前 `effectiveSelection`）；`startPlanExecution` 在校验非 planning 快照后，有对话模式则 `selectMode(Default)`，有权限端口则 `adoptSessionPermission`，再 `sendMessage` + resolved 覆盖快照。adopt/校验失败不得发送。不按 provider 名分支。 | `lib/src/features/agent/presentation/agent_conversation_view_model.dart`；`test/src/features/agent/presentation/agent_conversation_view_model_test.dart`；必要时 `test/src/features/agent/presentation/agent_pane_composer_toolbar_test.dart` / `agent_plan_document_card_test.dart` | `flutter test test/src/features/agent/presentation/agent_conversation_view_model_test.dart`：① 仅权限 Plan 的 fake（无 conversationModes，catalog 含 planningOnly）点执行后 `permissionSnapshot.selection` 非 planning，且 `selectedOptionId` 离开 Plan；② 仅对话 Plan 的既有用例仍 Default mode + 新回合、不 steer。`rg -n "selectionBeforeCurrentUserSelection" lib/src/features/agent/presentation/agent_conversation_view_model.dart` 无输出。`rg -nE "(claude|codex|grok)" lib/src/features/agent/presentation/agent_conversation_view_model.dart lib/src/features/agent/application/agent_plan_execution_handoff_controller.dart` 无非注释命中。既有 `turn permission snapshot resumes … plan → ask`：`flutter test test/src/features/agent/data/datasources/claude_code/claude_code_agent_provider_test.dart` 仍绿。通用门禁全绿。 | 完成 |
| T06 | 文案：审批卡「执行」改为「接受计划」（或同等「只确认方案」用语），交接卡保持「执行」。权限下拉对 `planningOnly` 项补只读说明（用 catalog description 即可，缺则 UI 按标记加一句）。不改协议、不合并两张卡。 | `lib/src/features/agent/presentation/widgets/agent_pane_sections.dart`；`lib/src/features/agent/presentation/widgets/agent_pane_cards.dart`；相关 widget 测试（`agent_pane_pending_interactions_test.dart` / `agent_plan_document_card_test.dart` / composer toolbar） | `flutter test` 上述改动对应的 widget 测试全绿；审批卡测例找新文案、不再把该按钮当「开始执行」；交接卡仍有「执行」。通用门禁全绿。 | 完成 |
| T07 | 文档与用户可见记录：`developer_guide` Plan 段写明两种能力与「点交接执行会会话内离开只读、不 persist 默认」；G5 不改编号，只补「离开只读 ≠ 预授权」。`CHANGELOG.md` `[未发布]` 写修复条。不改 AGENTS 门禁条数、不改 bundle 端口表。 | `docs/guides/developer_guide.md`；必要时 `docs/guides/glossary.md`；`CHANGELOG.md` | 文档与 `02-方案设计.md` 结论一致；无「执行零 apply」与实现矛盾的旧句未改干净（`rg -n "零 apply|不调用 permission apply" docs/guides/developer_guide.md` 核对后只保留「卡上改选」语境）。通用门禁全绿。 | 完成 |

## 阶段门

- T01～T07 全部为「完成」，且每步对应提交都通过通用门禁，才算实现阶段结束。
- 之后如需验收：对照 `02-方案设计.md` §6 测试矩阵与用户感知一句话，有设备再补 Claude 真机冒烟（Plan → 交接执行 → 写工具可被询问）；无设备在 `05-验收报告.md` 标阻塞，不得推断通过。
- 本任务不应修改 G1 共享适配层、`AgentEvent`、`AgentProviderBundle` 端口列表。任一步发现必须改这些边界，停止并回到方案评审。
