# 文档整理与自查

日期：2026-09-07。基线：`693dcf7a`，开始时工作区干净。

## 改动

- 重建 docs 中英文入口和工作记录索引，用户操作、开发细则、协议证据各有入口。
- 重写中英文各 10 篇用户指南，按操作、结果和限制组织；界面名称、文件位置和必要诊断命令附用途说明。去除实现术语、口号和无条件保证。
- AGENTS 从 412 行精简至 96 行，保留 G1–G8、阅读路由和检查要求；CLAUDE 从 68 行精简至 5 行，仅指向规则。长期细则归入工程规范。
- 删除 27 份过时或重复文档：8 份旧历史页、旧架构正文、Claude 初期提案、截图占位页、事件重构草稿，以及 15 份已被实现替代的阶段草稿。原文仍可从 Git 查阅。
- 保留原验收、断言审计、跳过事项和未完成验证；不把历史测试结果改写成本次结果。
- 修正数据目录、Claude 登录续期、权限确认、草稿恢复、Skill 支持、状态所有权、协议路径及发布示例等不一致说明。
- 同步 Issue/PR 模板入口、两份 schema 脚本的文档提示和一处测试注释链接，没有修改应用逻辑或测试断言。

## 自查

| 检查 | 结果 |
| --- | --- |
| 本地 Markdown 链接、图片、标题锚点、代码围栏 | 通过；docs 与工作记录均有索引入口 |
| 表格列数 | 通过 |
| 中英文用户指南 | 10 对逐段检查；功能矩阵和版本数字一致 |
| 用户视角 | 按“念给不会编程的人听”检查操作、结果与必要解释；内部实现名不进入用户步骤 |
| 当前源码引用 | 内联源码路径存在；重点事实对照现有代码与协议记录 |
| 历史审计 JSON | 可解析，原始证据未删除 |
| `dart format .` | 通过，1141 个 Dart 文件，0 个格式变化 |
| `flutter analyze --no-pub` | 通过，No issues found |
| `git diff --check` | 通过 |
| 依赖 | Flutter 首次分析产生的锁文件变化已撤回；用原锁文件执行 `flutter pub get --enforce-lockfile` 后重新分析 |

没有改动应用行为，未运行行为测试或全量测试。未执行真实 CLI、平台手工验收、发布或外部链接联网检查；历史待验证项见[未完成事项](../../pending-validation.md)。本次检查依据本地代码和已有证据，不声称验证了新的助手版本。

维护方法见[文档维护](../../../docs/zh/development/documentation.md)。
