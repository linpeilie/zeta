# usage_statistics_index.json v4 · 收口

- 本次根版本从 v3 升至 v4；v2 顶层 sessions 与 v3 typed providers 均由专用 legacy
  decoder 投影为 schemaVersion 1 分区，首次成功写回后成为 v4。
- 未使用 marker；写失败不标完成、不阻断查询，下次继续从现有文件重试。
- 未知分区在其它 source 写回时保留；损坏分区不影响合法分区。
- v4 只落 JSON-safe 派生白名单，不包含正文、工具内容、原始错误、环境值、凭据或
  session 文件路径。
- 主任务 T08 已切换用户可见生产路径，T10 已删除旧 typed Store 与旧 repositories；
  T11 已证明新增 Claude Code 自有分区无需修改共享 Store；troubleshooting/CHANGELOG 和
  长期架构说明由 T13 统一同步。

收口状态：v4 生产切换与旧写路径删除均完成；长期文档收口待主任务 T13。
