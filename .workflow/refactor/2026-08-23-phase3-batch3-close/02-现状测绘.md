# 第 3 批关批：现状测绘

## 关批前结构

- 3a：`IdeShellController` 按 flag 在 `ProjectThreadsViewModel` 与
  `ProjectThreadsSliceStore` 间二选一；`ProjectThreadsController` 已是无列表状态的
  effect/query runner。
- 3b：MainApp 在 flag 开启时创建 `UsageStatisticsSliceComposition`；Shell 在 flag
  关闭时自行创建 QueryService、两个 repository 和两个 ChangeNotifier controller。
- presentation 通过 operations 端口消费，但仍保留 Listenable/null 分支；Riverpod
  store provider 以 null 表示 legacy 路径。

## 调用方清单

生产调用方：

- `lib/main.dart`、`lib/src/app/app.dart`；
- `lib/src/app/shell/ide_shell_controller.dart`；
- `lib/src/ui/features/ide/views/ide_home.dart`；
- Project Threads 与 Usage Statistics 的三个 Riverpod adapter/Widget。

测试调用方：

- `ide_shell_controller_test.dart`、`ide_shell_widget_test.dart`、
  `main_app_localization_test.dart`；
- `project_threads_controller_test.dart`、`project_threads_widget_test.dart`；
- `usage_statistics_page_test.dart`、`agent_usage_panel_test.dart`；
- 两个 usage legacy controller 测试与两个 app slice runner 测试；
- `feature_layering_guard_test.dart`。

长期文档调用方：Phase 3 总文档、目标架构与第 3 批执行文档。历史设计/工作流记录
保留当时事实，不改写历史。

## 测试覆盖

- 根 `MainApp → IdeHome → Shell → Riverpod` 已对 3a/3b 两条路径做同断言对照；
- Projects Pane 11 个真实交互场景与 Project Home 保活场景已覆盖两条路径；
- usage store/runner 覆盖分页窗口、竞态、取消、Provider 单飞与尾随刷新；
- 既有页面/侧栏 Widget 覆盖渲染、交互、错误与空状态；
- 完整门禁在结构删除前为根包 2347 项、内部 Package 70 项全绿，最近一次翻旗后
  167 项受影响测试全绿。

已知无新增裸奔路径；关批只移除已被永久切片覆盖的实现测试。

## Zeta 专项检查

- G6：状态仍在 feature application；Riverpod 只在 presentation/app，删除两个
  ChangeNotifier 后 application Flutter 依赖减少两项。
- G7：不改 codec、JSON key、schema 或文件位置。
- 跨页保活：`IdeHome`、`IdeRetainedPageView` 与 AgentPane key 不移动，只收敛数据源。
- G3：不改 Agent reducer，也不触碰 live/history/replay 实例边界。
- G1：不改共享适配层，不新增 Provider 分支。
