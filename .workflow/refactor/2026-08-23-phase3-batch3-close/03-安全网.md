# 第 3 批关批：安全网

## 已有安全网

- Project Threads store/reducer 与 runner 单测钉住列表、选择、运行态、分页、防抖、
  lifecycle 写操作、OperationId 和迟到结果；
- Usage Statistics store/runner 单测钉住页面报表等价、目录/Provider 查询竞态、关闭
  结算与选择持久化；
- 真实 IdeHome Widget 测试钉住 Projects Pane、Project Home、Usage 页面、侧栏弹层及
  跨页保活；
- 分层守卫钉住 application 不新增 Flutter 依赖。

## 结构改动前证据

- `dart format .`：无格式变化；
- `flutter analyze`：零诊断；
- `tool/test_full.sh`：根包 2347 项、内部 Package 70 项通过；
- 生产翻旗后的 `tool/test_affected.sh`：167 项通过，包含 3a/3b 根组合切片路径。

## 本次策略

现有用户行为断言不修改。测试只允许更换 owner fixture、移除 false-path 参数化，以及
删除仅验证已退役实现内部细节的测试文件；永久路径上的原断言全部保留。
