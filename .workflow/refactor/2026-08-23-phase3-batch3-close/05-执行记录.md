# 第 3 批关批：执行记录

## 已执行改动

1. `IdeShellController` 永久创建 `ProjectThreadsSliceComposition`，删除
   `ProjectThreadsViewModel`、3a flag、nullable getter 与旧 listener/UI 分支。
2. `MainApp` 永久创建 `UsageStatisticsSliceComposition`，并注入已启用 Provider
   loader；删除两个 usage controller、3b flag，以及 Shell 中 QueryService、
   repository、quota source、source registry 的构造与 dispose。
3. `IdeHome` 绑定 usage 选择持久化与 Provider 设置目录同步；Riverpod store provider
   改为非空、缺少组合根 override 时 fail-closed。
4. Project Threads、Usage 页面和 Agent Usage Panel 固定消费 store 只读镜像；测试
   fixture 从旧 ChangeNotifier owner 改为纯 Dart store。
5. 分层守卫从 application Flutter 依赖燃尽清单删除两个 usage controller 条目，
   当前基线由 12 降到 10；长期架构文档标记第 3 批已关批。

## 与计划的偏差

原计划把旧 `UsageStatisticsController` 与新 store 的报表做运行时对照。旧 controller
删除后，该测试无法继续把退役实现作为 oracle，因此改为使用同一个稳定
`buildUsageStatisticsReport`，显式传入既有筛选、`UsageTrendMetric.totalTokens` 与
排序，和 store 结果逐字段比较。

这是测试 oracle 的结构调整，不是产品语义变更：页面、筛选、报表、失效选项清理与
Widget 断言均保持原预期。其余用户行为断言没有修改。

## 止损线检查

- 未修改 Provider wire、capability 或 runtime/Binding；
- 未修改持久化 schema、key 或路径；
- 未创建第二个 Provider settings owner；
- `IdeHome`、`IdeRetainedPageView`、Agent pane 与 workspace entry 生命周期未移动；
- 定向核心回归 161 项通过，未命中止损线。
