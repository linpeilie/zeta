# 第 3 批关批：等价性验收

## 结论

第 3 批可以关门。Project Threads 与 Usage Statistics 已成为单一路径；行为、分层、
持久化与生命周期证据均通过，未命中止损线。

## 门禁结果

| 门禁 | 结果 |
| --- | --- |
| `dart format .` | 775 个 Dart 文件检查完成，0 个需改格式 |
| `flutter analyze` | 根 Package 零诊断 |
| 定向核心回归 | 161 项通过 |
| `bash tool/test_affected.sh` | 删除/改名触发自动全量；根包 2333 项、内部 Package 71 项通过 |
| `bash tool/test_full.sh` | 根包 2333 项通过（258.88 秒），内部 Package 71 项及各自静态分析通过 |
| 架构守卫 | application Flutter 燃尽基线由 12 降到 10；分层、Package DAG、G1/G7、测试分片守卫通过 |

## 等价性判定

- Project Threads 的聚合、分页、防抖、选择、运行态、session 恢复和 capability
  fail-closed 断言保持通过；
- Usage Statistics 的时间窗口、筛选、报表、增量索引、额度、目录 coalescing、
  Provider single-flight、选择恢复和迟到结果断言保持通过；
- `MainApp → IdeHome → Shell → ProviderScope` 只解析一个 Project Threads owner 与
  两个 usage owner，漏 override 立即失败；
- 跨页保活、AgentPane、当前 Thread、草稿、滚动与 Pane 布局测试保持通过；
- Provider wire、runtime/Binding、审批语义、JSON schema 与本地数据路径未改动。

## 测试调整说明

旧 owner fixture 替换为对应 store；false-path 参数化被删除。完整统计页的等价性 oracle
由已删除的 controller 改为稳定 report builder 的显式输入，产品断言没有改变。旧
controller 的纯内部实现测试随实现删除；其用户行为覆盖已由 store、runner、Widget 与
根组合测试承接。
