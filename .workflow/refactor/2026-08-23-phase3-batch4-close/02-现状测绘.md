# 第 4 批关批：现状测绘

## 关批前结构

- `IdeShellController` 已始终创建 `WorkspaceSliceComposition`，但 flag 关闭时仍读取八个
  Workspace 字段并直接访问目录、构建文件树。
- `MainApp` 已创建 `IdeSessionSliceComposition`，但 Shell 仍接收 `IdeSessionStore`，
  并在 flag 关闭时自行创建 `IdeSessionPersistenceCoordinator`。
- `IdeHome` 的文件树与恢复 selector 仍按 flag 选择 Shell legacy getter 或 Riverpod
  只读镜像。
- 生产入口两个 flag 已为 true；false-path 只剩测试与回退用途，没有生产流量。

## 删除对象

- Workspace：`_workspaceTree`、`_expandedDirectoryPaths`、`_projects`、
  `_projectLastOpenedAtByPath`、`_projectPath`、`_currentFilePath`、
  `_selectedTreePath`、`_isLoadingProject`，以及 Shell 的目录读取/树构造；
- IDE Session：Shell 的 coordinator、旧 restore/save 分支、`_workbenchLayout`、
  `_initialRestoreCompleted`、`_initialRestoreCompleter`；
- `workspaceSliceEnabled`、`ideSessionSliceEnabled` 及所有调用点；
- 仅验证上述 false-path 的测试参数和守卫断言。

## 保留对象

- `ide_session.json` v4 DTO、codec、store 与原子写语义；
- Workspace 惰性树 runner、文件索引及 corpus 缓存；
- `_agentThreadIdsByProject` 与 `_projectHomeActive`，它们属于第 5 批跨 conversation
  workflow，不在本次误删；
- `IdeHome`、`IdeRetainedPageView`、Agent Pane 与 Workbench 三槽结构。
