# 第 4 批关批：安全网

## 已有证据

- Workspace store/runner 覆盖项目加载、惰性展开、恢复、移除、竞态和 dispose；
- IDE Session store/runner 覆盖恢复、保存防抖、取消、布局、迟到结果和 v4 宽容解码；
- 真实 `MainApp → IdeHome → Shell → ProviderScope` Widget 测试覆盖恢复、文件树、
  项目首页、左右栏布局与跨页保活；
- 生产翻旗后的根包 2363 项、内部 Package 70 项完整门禁已通过。

## 本次补强

- 增加 `ZetaStateSnapshot` 单元测试，验证只读聚合、集合冻结和会话正文不进入投影；
- 增加真实 `IdeHome` 根快照测试，验证 Workspace、Session、Project Threads 与
  Conversation 身份关系可按需重建；
- 架构守卫钉住双 flag、Shell 旧字段/IO 构造和生产 Widget 根快照订阅均不可回归。

既有用户行为断言不修改；测试只删除 false-path 参数化并改为永久切片 fixture。
