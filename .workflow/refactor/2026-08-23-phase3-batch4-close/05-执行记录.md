# 第 4 批关批：执行记录

## 已完成改动

1. `IdeShellController` 删除八个 Workspace 旧字段、`dart:io`/tree builder 直连、
   Session coordinator 与 restore/save/layout false-path；Workspace store 和
   `IdeSessionSliceOperations` 变为永久必选路径。
2. `MainApp`、`IdeHome` 与生产入口删除 `workspaceSliceEnabled`、
   `ideSessionSliceEnabled`；文件树与 Session UI 永久订阅对应 feature selector。
3. 新增 `ZetaStateSnapshot`：MainApp 按需聚合 app 生命周期切片，无监听 relay 从唯一
   Workbench 组合边界读取 Shell 侧投影。Conversation 与 Agent Management 使用无正文
   白名单摘要。
4. Shell controller 测试改为显式注入 app session composition；false-path 测试参数删除，
   既有行为断言不变。
5. 新增根快照单测、真实 IdeHome 身份关系 Widget 测试和三组架构守卫；长期 Phase 3
   文档同步标记第 4 批已关批。

## 止损线检查

- Provider wire、capability、runtime/Binding 与审批语义未改；
- `ide_session.json` v4、codec、data store 和路径未改；
- 根快照无 Provider/Notifier/listener/持久化 API，生产 Widget 不 watch；
- Conversation 标题/正文、工具/diff、Agent 配置、日志与 raw payload 不进入根投影；
- Shell 可变字段净减少，未新增 feature owner。

## 定向证据

- 根快照单测、Workspace/Session/root snapshot 架构守卫：通过；
- `ide_shell_controller_test.dart`：17 项通过（与其余定向文件合计 27 项）；
- 真实 IdeHome 根快照场景：通过；
- Workspace 与 IDE Session Widget 回归：13 项通过；
- `flutter analyze`：零诊断。

最终受影响测试和完整重构门禁见 `06-等价性验收.md`。
