# 第 4 批关批：等价性验收

## 结构验收

- 生产代码已删除 `workspaceSliceEnabled`、`ideSessionSliceEnabled` 及其 false-path。
- `IdeShellController` 不再持有 Workspace 树、IDE Session layout 或 persistence
  coordinator 的旧状态；Workspace/Session 切片成为唯一 owner。
- `IdeHome` 永久通过 feature selector 消费 Workspace 与 IDE Session 状态。
- 根状态通过 `ZetaStateSnapshot` 按需投影；它不订阅、不缓存，不包含会话正文、
  配置值、日志、错误原文或 Provider raw payload。
- 未修改 Provider 协议、审批语义、持久化 schema 或页面视觉。

## 门禁结果

| 门禁 | 结果 |
| --- | --- |
| `dart format .` | 803 个文件检查完成，0 个文件变化 |
| `flutter analyze` | 通过，0 issue |
| `bash tool/test_affected.sh` | 选择 22/296 个根测试文件，187 条测试通过 |
| `bash tool/test_full.sh`（根 Package） | 2366 条通过，0 失败、0 跳过，264.54 秒 |
| `bash tool/test_full.sh`（内部 Packages） | 5 个 Package analyze 通过，70 条测试通过 |

## 等价性结论

原 Workspace/IDE Session 行为断言未改语义并保持全绿；新增根快照单元测试、
真实 `IdeHome` Widget 验收和三组架构守卫共同固定单 owner、关系可重建与敏感正文
不进入根状态的边界。第 4 批满足提前关批条件，可以进入下一批。
