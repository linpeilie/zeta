# 执行记录

## 与步骤表的偏差

无。4b 按“纯 Dart store/reducer → app runner/composition → Shell 双路径 → Riverpod
只读镜像”的顺序完成；生产翻旗、旧路径删除和 root `ZetaStateSnapshot` 未提前混入。

## 已执行改动

1. 新增 `IdeSessionSliceState`、typed intent/effect、纯同步 reducer 与纯 Dart store，运行态
   只保存恢复 lifecycle 和 Workbench layout，不复制 v4 持久化 DTO。
2. app 组合层继续复用既有 `IdeSessionPersistenceCoordinator`，保留 restore token、防抖、
   restore 期间排队保存、取消与关闭前 `saveNow` 语义。
3. `MainApp` 稳定创建一个 composition 和一个 root Provider override；flag false 时 store
   dormant，flag true 时 Shell 只经 `IdeSessionSliceOperations` 写入，不发生双写。
4. `IdeHome` 仅选择性监听 layout 与初始恢复完成状态；Shell 不拥有也不释放 app-owned
   store，Riverpod 镜像不拥有业务状态。
5. 新增 reducer/store、runner/composition、架构守卫及真实 `IdeHome` 双路径测试，覆盖慢恢复
   取消、保存排队、同一 `MainApp` rebuild 与跨页保活。

## 断言与止损线

既有测试断言零修改，只新增 true-path 和生命周期用例。未修改 `ide_session.json` v4
schema、codec、store、state builder、Provider 协议、Binding/runtime、文件树算法或视觉
布局；两个生产 flag 继续显式为 `false`，未命中止损线。
