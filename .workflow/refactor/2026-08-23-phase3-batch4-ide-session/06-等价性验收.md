# 等价性验收

## 结论

第 4 批 4b IDE Session 默认关闭路径验收通过。生产入口仍显式传
`ideSessionSliceEnabled: false`；旧 Shell/coordinator 路径继续是生产唯一 owner，新切片
只在测试显式开启。4a `workspaceSliceEnabled` 同样保持 `false`。

## 行为证据

- reducer/store 单测固定恢复状态、取消、初始恢复等待、layout、typed save future 和
  dispose 结算；
- runner/composition 单测证明 restored/failed 路由、restore 期间只保存最新快照，以及
  cancel/`saveNow` 仍透传既有 coordinator；
- 真实 `MainApp → IdeHome → Settings → Agent` Widget 测试证明新路径恢复并重存 layout，
  rebuild 不改变 Riverpod override 数量，慢恢复可取消；
- 真实工作台跨页测试证明 Agent pane Element、草稿、滚动与 pane 布局不被重置；
- 架构守卫证明 application slice 无 Flutter/Riverpod/`dart:io`，v4 DTO/store 不依赖
  slice，两个生产 flag 均保持关闭。

## 收尾门禁

```text
dart format .
  800 files checked, 0 changed

flutter analyze
  No issues found

bash tool/test_affected.sh
  selected 21/294 root test files
  188 tests passed

bash tool/test_full.sh
  root: 2363 tests passed, 0 failed, 0 skipped (260.58 seconds)
  zeta_agent_core: analyze + 3 tests passed
  zeta_agent_providers: analyze + 11 tests passed
  zeta_foundation: analyze + 29 tests passed
  zeta_plugin_kernel: analyze + 23 tests passed
  zeta_ui: analyze + 4 tests passed
```

## 下一接缝

4a/4b 生产翻旗、观察、旧路径删除、关批和 root `ZetaStateSnapshot` 均尚未授权。下一次
显式推进应先决定是否同时翻两个独立 flag；未获得确认前继续保留默认关闭双路径。
