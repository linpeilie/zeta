# 生产翻旗与观察

## 授权与范围

默认关闭双路径及完整重构门禁通过后，用户以“继续”确认进入第 4 批下一接缝。本步只
执行四步节奏第 3 步：同时翻转 4a/4b 生产入口，但保留两个独立回滚点；不删除旧路径，
不建立 root `ZetaStateSnapshot`，不关批。

## 执行记录

- `workspaceSliceEnabled` 与 `ideSessionSliceEnabled` 在 `main.dart` 中显式改为 `true`；
- 架构守卫同步固定生产入口必须保持启用，避免后续误拨回而没有记录；
- 两个 context 的 store、runner、Riverpod 装配、持久化 schema 与业务断言均未修改；
- 第 4 批按中高风险取至少 7 天观察期，起点为 2026-08-23，最早于 2026-08-30 关批；
- 任一路径出现问题时只把自身 flag 拨回 `false`，修复复测后重新起算自身观察窗口。

## 门禁

```text
定向生产路径回归
  55 tests passed

dart format .
  800 files checked, 0 changed

flutter analyze
  No issues found

bash tool/test_affected.sh
  selected 16/294 root test files
  153 tests passed

bash tool/test_full.sh
  root: 2363 tests passed, 0 failed, 0 skipped (272.44 seconds)
  zeta_agent_core: analyze + 3 tests passed
  zeta_agent_providers: analyze + 11 tests passed
  zeta_foundation: analyze + 29 tests passed
  zeta_plugin_kernel: analyze + 23 tests passed
  zeta_ui: analyze + 4 tests passed
```

业务行为断言零修改；只把两条“生产入口保持关闭”的架构守卫期望改为“生产入口保持
启用”，与本步授权完全对应。旧路径删除和关批必须再次获得显式授权。
