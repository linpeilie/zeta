# 等价性验收

## 结论

第 4 批 4a Workspace 默认关闭路径验收通过。生产入口仍显式传
`workspaceSliceEnabled: false`；旧路径继续是生产唯一 owner，新切片只在测试显式开启。
未修改 `ide_session.json` v4、Provider 协议、Binding、文件树算法或视觉布局。

## 行为证据

- 新切片单测固定 project load、迟到结果、惰性展开、选择、移除、恢复、不可变集合和
  dispose 语义；
- runner 测试证明只读取请求层级，缺失目录不提交状态，完整文件索引仍异步启动；
- 真实 `MainApp → IdeHome → FileTreePane` 测试覆盖打开项目、展开目录、选择文件、保存
  v4 session、销毁应用与重启恢复；
- 既有 Workspace、IDE Session、Shell、Agent workspace 与 Composer mention 断言未改；
- 架构守卫证明 application slice 无 Flutter/Riverpod/`dart:io`，文件索引已脱离
  `ChangeNotifier`，生产 flag 保持关闭。

## 收尾门禁

```text
dart format .
  788 files checked

flutter analyze
  No issues found

bash tool/test_affected.sh
  selected 49/291 root test files
  476 tests passed

bash tool/test_full.sh
  root: 2347 tests passed, 0 failed, 0 skipped
  zeta_agent_core: analyze + 3 tests passed
  zeta_agent_providers: analyze + 11 tests passed
  zeta_foundation: analyze + 29 tests passed
  zeta_plugin_kernel: analyze + 23 tests passed
  zeta_ui: analyze + 4 tests passed
```

## 下一接缝

4a 翻旗、观察与关批均尚未授权。下一次显式推进应先启动 4b IDE Session 切片，保持既有
coordinator 的 restore token、防抖保存、关闭前 `saveNow` 与 v4 codec 不变；不得把 4a
生产翻旗混入 4b 的首次实现。
