# 第 5 批直接迁移：安全网

结构改动前已运行以下既有行为测试：

- desktop attention controller；
- Agent thread workspace Binding；
- conversation mode/model/skills controller；
- conversation slice store；
- IDE Shell controller。

合计 70 条测试通过。它们固定可见性抑制、通知幂等、两 conversation 隔离、Binding
懒启动、目录与保存竞态、G5 四链路、fork/恢复/持久化及 Shell 事件风暴边界。

本批会新增纯 reducer/store、Riverpod selector、旧入口零残留及真实 `IdeHome` 保活守卫；
不修改既有行为断言。最终按 R4 执行完整 `tool/test_full.sh`。
