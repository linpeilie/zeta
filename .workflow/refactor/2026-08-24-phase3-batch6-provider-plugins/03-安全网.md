# 安全网

## 改造前通过的契约

- plugin catalog / compatibility lifecycle；
- core Provider catalog freeze；
- bundle architecture；
- Provider config V1/V2 宽容解码与 display-name 归一化；
- legacy permission migration；
- 三类 native bundle 端口与 Claude loader 注入。

命令：

```sh
flutter test --no-pub test/src/app/plugins/zeta_plugin_catalog_test.dart \
  test/src/features/agent/architecture/agent_provider_catalog_freeze_test.dart \
  test/src/features/agent/architecture/agent_provider_bundle_contract_test.dart \
  test/src/features/agent/data/agent_provider_config_store_test.dart \
  test/src/features/agent/data/agent_provider_permission_migration_test.dart \
  test/src/features/agent/data/native_agent_provider_bundle_test.dart
```

结果：39 个既有测试通过。

## 新增 characterization test

旧 `default_agent_provider_factory_test.dart` 新增“custom config id 按 provider protocol
domain 路由”，旧实现上 3/3 通过；目标态将该断言迁入
`agent_provider_plugins_test.dart`。它防止聚合器错误地按固定 providerId 查 factory。

## 明知不补的路径

不新增真实 CLI 冒烟：本批不改协议或 wire，自动化也不能替代最终三平台验收。Phase 4
准入审计会把真实 smoke 作为独立未完成门禁记录。
