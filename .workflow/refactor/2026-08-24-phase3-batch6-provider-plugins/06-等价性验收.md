# 等价性验收

日期：2026-08-24
结论：第 6 批通过；Phase 3 阶段尚未结束。

## 行为与持久化

| 验收项 | 结果 |
| --- | --- |
| Provider 默认顺序与默认 active | Codex、Grok、Claude Code；默认 Codex，保持不变 |
| Provider JSON | version 2、`kind` key 与 `codexAppServer` / `acp` / `claudeCode` 保持不变 |
| 自定义配置 ID | 与插件 type 分离，按开放 type 路由 |
| 未知/重复/冒充身份 | fail-closed，不回落 Codex |
| capability 与可选端口 | 三类 native bundle 契约测试保持原矩阵 |
| runtime ownership | runtime registry 仍是唯一 bundle/runtime owner；插件只贡献 factory |
| 关闭顺序 | runtime registry 先于 plugin catalog；插件内部反序关闭 |

## 自动化证据

- `dart format .`：825 个文件检查，0 个改动。
- `flutter analyze`：0 个问题。
- 模型目录启动边界定向测试：8 条通过。
- `bash tool/test_affected.sh`：根测试 2383 条通过；全部内部 Package analyze/test 通过。
- `bash tool/test_full.sh`：根测试 2383 条通过；全部内部 Package analyze/test 通过。
- 静态清零：`AgentProviderKind`、`DefaultAgentProviderFactory`、
  `CompatibilityAgentProviderPlugin` 在生产 Dart 中无引用。
- 独立复审：无 P1/P2。

## 阶段准入

Phase 3 仍被第 1、2 批观察/关批、连续 14 天真实使用证据、5 个 application Flutter
燃尽项、root snapshot 可空节点、`zeta_agent_core` 的 11 个 Flutter import，以及 Phase 4
所需的迁移窗口、可构建回退锚、三平台真实 Provider smoke/性能证据阻塞。当前不能进入
Phase 4；完整矩阵见 `docs/architecture/phase3_batch6_provider_plugins.md` §10。
