# 动机与止损线

## 动机

Phase 3 第 6 批关批后的准入审计仍有五类阻塞。本次按明确要求只修复 1、3、4：
提前关闭第 1/2 批、清零 application Flutter 例外并收紧 root snapshot、将
`zeta_agent_core` 纯 Dart 化；不把时间或平台证据伪装成代码可修复项。

## 止损线

- 不改变 Provider wire、CLI 参数、capability、配置 JSON schema 或迁移语义；
- 不恢复 compatibility/default factory 或任何运行时双轨；
- 不修改 G5 四种审批语义、entry identity、coalescing 或 reducer 边界；
- 旧 controller/flag 必须同次物理删除，失败时整体 revert，不加 shim。

## 范围外

Phase 2 连续 14 天真实使用证据，以及 Phase 4 所需迁移窗口、可构建回退锚、三平台
构建、真实 Provider smoke 与 Profile，均保留为独立门禁。
