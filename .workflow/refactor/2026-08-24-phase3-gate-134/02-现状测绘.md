# 现状测绘

## 阻塞 1

第 1/2 批生产虽已翻旗，但根 app、IdeHome 与 Shell 仍保留两个 flag、五个旧
controller/Flutter port、ingress 和 false-path；settings/provider/management 的 root
snapshot 因此可空。

## 阻塞 3

`feature_layering_guard_test` 仍容许 5 个 application Flutter import；management
application 还读取 Provider 私有配置 key，身份投影没有完全回到 app/data 组合层。

## 阻塞 4

`zeta_agent_core` 有 11 个生产 Flutter import，manifest 依赖 Flutter SDK 与
`flutter_test`；根守卫仍只冻结历史上限 17，没有强制目标值 0。

## 既有安全网

settings/provider/management slice、配置 codec、三 Provider bundle/runtime、root snapshot、
conversation history/live UI 与 Package DAG 均已有契约或 Widget 测试。本次保留这些行为
断言，只删除双路径对照断言。
