# 执行记录

日期：2026-08-24

## 第 1/2 批直接关批

- 删除 appearance/general/provider settings/management 五个旧 controller、settings ingress
  与 controller notification bridge；
- 删除两个批内 flag、MainApp/IdeHome/Shell false-path 与双路径测试；
- settings 测试改从 data store 注入，Provider/Shell 测试统一安装 slice composition；
- Provider settings port 改为 `subscribe`/取消订阅的纯 Dart 契约。

## 状态树与分层

- root snapshot 的 appearance、general、provider settings、agent management 改为必选；
- application Flutter 燃尽清单清零；Provider 私有 extra-key 与指标标签解析改由 app
  组合注入，新增 application/domain 禁止依赖 providers 包的守卫。

## 纯 Dart core

- 用 `AgentListenable`、`AgentValueListenable`、`AgentChangeNotifier` 与
  `AgentValueNotifier` 替代 Flutter foundation 状态类型；
- presentation 用显式 adapter 投影为 Flutter `Listenable`/`ValueListenable`；
- core manifest 删除 Flutter SDK/flutter_test，包测试改用 `package:test`。

## 未执行

没有把连续 14 天证据、迁移窗口、回退 tag/分支、三平台真实 Provider smoke 或 Profile
标记为通过。
