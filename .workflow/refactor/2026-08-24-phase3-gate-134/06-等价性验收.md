# 等价性验收

日期：2026-08-24

## 自动化门禁

- `dart format .`：819 个文件扫描，0 个文件变化；
- `flutter analyze`：0 issue；
- `bash tool/test_affected.sh`：因删除旧 Dart 文件自动升级为完整根包测试，
  根包 2371/2371；随后 5 个内部 Package 共 74/74，且各包分析通过；
- `bash tool/test_full.sh`：重构强制完整门禁再次通过，根包 2371/2371，
  5 个内部 Package 共 74/74，且各包分析通过；
- `git diff --check`：无 whitespace error，仅工作区既有的 LF/CRLF 提示。

## 静态边界

- 旧 settings/provider management controller、ingress 与两个 rollout flag 在生产代码零命中；
- `zeta_agent_core` 的源码和 manifest 对 Flutter 零命中；
- feature application/domain 对 `zeta_agent_providers` 零依赖；
- root snapshot 的 settings/provider management 节点均为必选。

## Review 修复

- 修复 appearance/general 首次 load 与写入交错、前次持久化失败被后续写入夹带的竞态；
- Provider 私有账号字段映射改为组合层必注入，删除默认放行路径；
- Provider settings 与模型目录 Riverpod 根接线改为 non-null、缺失即 fail-closed，
  删除 flag-era 的默认状态与 `sourceUnavailable` 静默降级；
- keep-alive Shell 回归按真实生命周期验收：离屏页不重建，重新激活后同一 Element
  消费最新 slice；
- `AgentListenable` 到 Flutter listenable 的转换仅留在 presentation adapter；单个坏
  listener 不再截断后续通知，异常交给当前 Zone；runtime registry 关闭后释放订阅。

## Phase 4 准入结论

第 1、3、4 项代码阻塞已经关闭，但 Phase 3 阶段准入仍为红色，不能进入 Phase 4：

1. 本轮修复了 slice 路径竞态，连续 14 天真实使用证据从目标态提交后重计，
   最早约 2026-09-07；
2. 旧格式迁移窗口、当前目标态可构建回退 tag/分支、Windows/macOS/Linux 三平台
   真实 Codex/Grok/Claude Code smoke，以及 Windows Profile 证据尚未完成。

最近一份已跟踪的 Windows Profile（2026-08-11）Raster p95 为 26.927/26.200ms，
慢帧率为 11.409%/9.396%，明确未过 16.7ms/5% 门槛；它不能证明当前目标态仍失败，
但在最终目标态同构复测关闭前，性能门禁必须保持红色。

用户要求“不需要等到那个时间”只用于提前删除第 1/2 批旧路径，不视为对上述阶段证据的豁免。
