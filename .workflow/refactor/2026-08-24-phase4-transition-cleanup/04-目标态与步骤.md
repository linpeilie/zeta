# 04 · 目标态与步骤编排（P4-1：零调用与机械过渡 API 清理）

前置：`02-现状测绘.md` 冻结清单 · `03-安全网.md` 基线（根 2371 + Package 74，全绿）
批次定义与关批标准：[计划书 §5 P4-1](../../../docs/architecture/phase4_transition_cleanup.md)

---

## 1. 目标结构

P4-1 不改变任何结构，只**缩小后续批次的改动面**：把「同一语义有两个名字」和
「定义了但没人用」的符号删干净，让 P4-2～P4-5 面对的是单一路径。

| 语义 | 现在 | P4-1 之后 |
|---|---|---|
| turn 正文修订号 | `renderRevision` + `contentRevision` 两个名字，projection 里还有 `!= 0` 兜底 | 只有 `contentRevision` |
| 辅助前景色 | `mutedText` + `textSecondary` 两个 token，`copyWith` 互相同步 | 只有 `textSecondary` |
| 跨层类型可见性 | 3 条过渡 re-export 让下游可以不 import 真源 | 每个下游显式 import 真源 |
| 零调用符号 | 8 个符号 + 3 个死 provider 仍可被新代码引用 | 物理删除，不可再引用 |

**不新增任何抽象、flag 或适配层。** 每步都是删除或就地改名。

## 2. 步骤序列（先切调用点，再删旧符号）

| # | 做什么 | 涉及文件 | 完成判据 | 回滚点 | 状态 |
|---|---|---|---|---|---|
| 1 | 删 8 个零调用符号 | `ide_shell_controller.dart`(agentViewModel) · `agent_provider_config_store.dart`(CallbackAgentProviderConfigStore) · `appearance_settings_store.dart`(CallbackAppearanceSettingsStore)+其测试 · `codex_usage_log_scanner.dart`(codexUsageSourceId) · `agent_thread_models.dart`(displayTitle) · `agent_conversation_model_selection_controller.dart`+`agent_conversation_view_model.dart`(selectServiceTier) · `ide_virtual_list_controller.dart`(kIdeUseAnchoredDynamicSliver) · `ide_dynamic_sliver_list.dart`(buildIdeVirtualSliver)+其测试 | 8 个符号 grep 全库为 0；analyze 绿 | 步骤 1 提交前 | 完成 |
| 2 | 删 3 条过渡 re-export，给下游补显式 import | `agent_conversation_view_model.dart:33` · `agent_provider_config_store.dart:9` · `appearance_settings_slice_state.dart:7` + analyze 报错的每个下游 | `lib/src` 只剩 `app_localizations_x.dart` 一条 `export`；analyze 绿 | 步骤 1 完成态 | 完成 |
| 3 | `renderRevision` 收敛到 `contentRevision` | `agent_conversation_timeline_store.dart`(ctor 字段 `:1776`、final `:1796`、getter `:1850`、快照 `:1984`) · `agent_timeline_projection_cache.dart`(getter `:21`、`resolve` 兜底 `:61`) · `agent_conversation_region_state.dart:840` · `agent_timeline_extent_descriptor.dart:206` 注释 · 15 处测试引用 | `renderRevision` grep 全库为 0；projection 缓存命中/失效行为测试全绿 | 步骤 2 完成态 | 完成 |
| 4 | 删 `mutedText` 别名 | `ide_colors.dart`(ctor `:27`、暗色 `:99`、亮色 `:138`、field `:222`、copyWith `:397/:420/:421/:431`、lerp `:466`、doc `:386`) · `agent_pane_header.dart:145/163/171` 改 `textSecondary` | `mutedText` grep 全库为 0；主题 token 测试全绿 | 步骤 3 完成态 | 完成 |
| 5 | 删 3 个零读取者 app provider | `agent_conversation_workspace_providers.dart` 的 `…EntriesProvider` / `…SelectedEntryIdProvider` / `…ProjectHomeProvider` | 三符号 grep 全库为 0；analyze 绿 | 步骤 4 完成态 | 完成 |
| 6 | 关批：零容忍守卫 + mutation check + 全量 | 新增/扩展 `test/src/architecture/` 守卫；`bash tool/test_full.sh` | 守卫无 allowlist 且 mutation check 变红；全量 exit 0 | 步骤 5 完成态 | 完成 |

每步之间仓库必须可编译、`flutter analyze` 绿。步骤 1–5 是同一批次（P4-1）的内部增量，
最终合成**一个** `refactor` 提交。

## 3. 等价性论证（动手前先证明，不靠跑完再说）

### 3.1 `mutedText` → `textSecondary` 是视觉等价的

已实测两个调色板中两者取值**完全相同**：

- 暗色 `ide_colors.dart:99/101`：`mutedText: neutral[400]` == `textSecondary: neutral[400]`
- 亮色 `ide_colors.dart:138/140`：`mutedText: zinc[600]` == `textSecondary: zinc[600]`

且 `mutedText` **从未被单独覆盖**：全库 4 处 `mutedText:` 赋值全部在 `ide_colors.dart`
内部（两个调色板 + `copyWith` + `lerp`），`IdeColors(` 的 5 个构造点也全在该文件内。
因此 `agent_pane_header.dart` 的 3 处替换不改变任何一个像素。

### 3.2 `renderRevision` → `contentRevision` 是语义等价的

两处别名都已是纯转发：`agent_conversation_timeline_store.dart:1850`
的 `int get renderRevision => _contentRevision;`、`agent_timeline_projection_cache.dart:21`
的 `int get renderRevision => contentRevision;`，快照构造 `:1984` 也是
`renderRevision: _contentRevision`。

唯一有实际分支的是 projection 的兜底：

```dart
final contentRevision = turn.contentRevision != 0
    ? turn.contentRevision
    : turn.renderRevision;
```

由于 `AgentConversationTurnGroup` 的两个字段在唯一构造点被赋同一个 `_contentRevision`，
`contentRevision == 0` 时 `renderRevision` 必然也是 0，兜底分支恒等于
`turn.contentRevision`。**删除兜底不改变结果。**

> ⚠️ 步骤 3 必须先删 ctor 字段再删兜底：只要 `AgentConversationTurnGroup` 还允许
> 外部传入不一致的两个值，上面这条论证就不成立。

### 3.3 re-export 删除由编译器兜底

删掉 `export` 行后，任何依赖传递导出的下游都会**编译失败**，analyze 会逐个点名。
补显式 import 是纯机械操作，不存在"漏改但能跑"的中间态。

## 4. 测试改动的边界（止损线 S1 的具体化）

P4-1 会碰到测试文件，但**只允许**这三类改动，其余一律触发停批：

| 允许 | 例子 |
|---|---|
| 机械改名 | 测试里的 `turn.renderRevision` → `turn.contentRevision` |
| 补显式 import | re-export 删除后下游测试加 `import 'package:zeta_agent_core/...'` |
| 删除只证明被删符号存在的用例 | `appearance_settings_store_test.dart` 里构造 `CallbackAppearanceSettingsStore` 的那条；`app_dependencies_test` 里断言 provider `name` 的那条（若涉及） |

**不允许**：改断言的期望值、放宽 matcher、删除任何证明**行为**（而非符号存在）的用例。
每一条删除的用例都要在 `05-执行记录.md` 里点名，并说明它证明的是符号存在而非行为。

## 5. 本批明确不做的（附理由）

| 候选 | 为什么不做 |
|---|---|
| `zetaClockProvider` / `zetaMetricsPortProvider` / `requiredDependency` | 生产读取者为 0（`zetaMetricsPortProvider` 只有 `app.dart:551` 一处**覆盖**、无读取），但它们是 `app_dependencies.dart` 文档化的"组合根 + 覆盖点"契约，且有 `app_dependencies_test.dart` 专测。**P4-2 要重建 app composition，这三个的去留应由 P4-2 一次决定**，在 P4-1 删掉再在 P4-2 重建是churn。 |
| `rawPayload` 空参数改 logger 摘要 API | P4-0 实测全库 0 命中，无待办（计划书已标注） |
| `ProjectThreadsSliceRunnerAdapter` | 有 3 处生产调用，属 P4-3b |
| `phase3_batch5_architecture_guard_test.dart` 改名 | 属 P4-6，且改名要连带 `tool/test_shards.dart` 归片确认 |

## 6. 步骤 6 的守卫设计（按 `03-安全网.md` §4 的每批自证法）

新增一条零容忍守卫，断言 P4-1 删除的符号不能重新出现在 `lib` / `packages`：

- 覆盖面：`renderRevision`、`mutedText`、`agentViewModel`、`codexUsageSourceId`、
  `selectServiceTier`、`kIdeUseAnchoredDynamicSliver`、`buildIdeVirtualSliver`、
  `CallbackAgentProviderConfigStore`、`CallbackAppearanceSettingsStore`，
  以及 `lib/src` 内除 `app_localizations_x.dart` 外的 `export` 语句；
- **无 allowlist**，直接断言计数为 0；
- mutation check：临时把 `mutedText` 的 field 加回 `ide_colors.dart`，确认守卫变红，
  再撤销。结论记进 `05-执行记录.md`。

`AgentThreadSummary.displayTitle` **不进守卫的名字黑名单**——`displayTitle` 是
`AgentToolCallUiText` 的活 API，按名字断言会误伤（见 `02-现状测绘.md` §1.1）。
