# 05 · 执行记录

只记三类事：**测试断言被迫变动**、**偏离步骤表的改动**、**触碰止损线的时刻**。
顺利的步骤不写。

---

## P4-1：零调用与机械过渡 API 清理（2026-08-24）

步骤表见 `04-目标态与步骤.md` §2，六步全部完成。
**触发过一次停线（S1），已按流程取得批准后继续**——见 §5。

### 1. 测试改动清单（逐条定性）

按 `04` §4 的边界，本批共动了 7 个测试文件。**没有任何一条断言的期望值被修改。**

#### 1.1 删除的用例（3 条，都是"证明被删符号存在"而非证明行为）

| 用例 | 文件 | 为什么可以删 |
|---|---|---|
| `callback store falls back to the legacy theme mode` | `settings/data/appearance_settings_store_test.dart` | 见下 §1.2，**这条查得最细** |
| `feature flag 可回退到普通 SliverList` | `ui/core/virtualization/ide_dynamic_sliver_list_test.dart` | 唯一断言是 `buildIdeVirtualSliver(useAnchoredDynamic: false)` 会渲染出 `SliverList`。flag `kIdeUseAnchoredDynamicSliver` 是 `const bool = true`，该回退分支生产从未走到；helper 与 flag 一并删除后，这条用例测的是不存在的代码 |
| `expect(state.renderRevision, 2, reason: 'renderRevision 别名 content')` | `agent/presentation/agent_timeline_projection_cache_test.dart:112` | 断言文本自陈是在验证"别名等于 content"。同一个 `test()` 内 `contentRevision`、`metaRevision` 的行为断言全部原样保留 |

#### 1.2 `CallbackAppearanceSettingsStore` 那条为什么不是删掉了 backward-read

删除前专门核查过——**这是本批最接近计划书 §4 红线的一处**：

- 该用例证明的是 `AppearanceSettings.fromLegacyThemeMode`（旧 themeMode 回退），
  属于 G7 / §4 明确要求保留的向后读；
- 但生产的 `FileAppearanceSettingsStore` **没有** `loadLegacyThemeMode` 分支，
  这条回退只能通过 `CallbackAppearanceSettingsStore` 触达，而它零生产调用者；
- 真正的旧 themeMode 迁移在 `lib/src/app/zeta_storage_migrator.dart:172-179`，
  由 `test/src/app/zeta_storage_migrator_test.dart:112,121` 覆盖，**本批完全没碰**；
- `AppearanceSettings.fromLegacyThemeMode` 本身保留（仍被 migrator 调用）。

结论：删掉的是一条**生产从未走过的并行旧路径**及其测试，向后读能力与其测试都在。

#### 1.3 机械改名（4 个文件）

`renderRevision` → `contentRevision`，值不变、断言不变：

| 文件 | 改法 |
|---|---|
| `agent_timeline_describe_reuse_test.dart:51,58` | fixture 参数改名 |
| `agent_timeline_entry_layout_revision_test.dart:53,61` | fixture 参数改名 |
| `agent_timeline_extent_descriptor_test.dart:29,39,48` | fixture 参数改名 |
| `agent_timeline_truncated_estimate_test.dart:38` | 与 `contentRevision: 1` 重复，删冗余行 |
| `agent_timeline_projection_cache_test.dart:35,155,156,167,168,181` | 断言与 fixture 改名 |

> **改名等价性**：这些 fixture 原先只传 `renderRevision`，`contentRevision` 取默认 0，
> 于是 projection 的 `contentRevision != 0 ? … : renderRevision` 兜底恰好取到
> `renderRevision`。改名后直接传 `contentRevision`，解析结果与原先逐位相同。
> 换句话说，那条兜底分支存在的唯一原因就是这批 fixture——删兜底与改 fixture 必须同批完成。

### 2. 偏离步骤表的改动

#### 2.1 步骤 2 实际零下游改动（比预期小）

`04` §2 预计删 3 条 re-export 后要"给 analyze 报错的每个下游补显式 import"。
实际删完后 `flutter analyze` **0 error**——21 / 26 / 14 个 importer 全都已经直接
import 了真源，没有一个依赖传递导出。步骤 2 因此只删了 3 行，没有补任何 import。

#### 2.2 发现并修正：grep 的 `\b` 在 CJK 邻接处漏匹配

`02-现状测绘.md` 用 `grep -rEn '\brenderRevision\b'` 统计，`lib` 得到 4 处。
新守卫（Dart `RegExp`）跑起来立刻抓到**第 5 处**：
`agent_timeline_extent_descriptor.dart` 的文档注释
`（不含 turn 级 renderRevision）` —— 符号紧邻全角括号 `）`，GNU grep 的 `\b`
在该处不匹配，Dart 的 `RegExp` 匹配。

**影响**：`02` 里凡是用 `\b…\b` 统计、且符号可能紧邻中文标点的计数**可能偏低**。
已用不带词边界的 `grep -rn` 对本批 9 个符号全量复查，确认全部为 0。

**对后续批次的要求**：P4-2～P4-6 的销账**以 Dart 守卫为准，不以 grep 计数为准**；
需要 grep 时不要用 `\b`，改用不带词边界的匹配再人工排歧义。

#### 2.3 顺带修复测试基础设施（P4-0 遗留项）

`tool/test_full.sh` 与 `tool/test_shard.sh` 的 `--file-reporter` 路径缺陷已修
（详见 `03-安全网.md` §5）。这是 P4-0 关批时留下的待决策项，用户确认后在本批一并处理。

### 3. 本批明确保留、未删的候选

`zetaClockProvider` / `zetaMetricsPortProvider` / `requiredDependency` 生产读取者为 0
（`zetaMetricsPortProvider` 只有 `app.dart:551` 一处覆盖、无读取），但按 `04` §5 留给
**P4-2** 与 app composition 重建一起决定，避免删了再建的 churn。

### 4. 守卫与 mutation check

新增 `test/src/architecture/deleted_transition_api_guard_test.dart`：

- 两条零容忍断言，**无 allowlist**：9 个已删符号不得出现在 `lib/` 与 `packages/*/lib/`；
  `lib/src` 除 `app_localizations_x.dart` 外不得有 `export`；
- `displayTitle` **故意不入名单**——它同时是 `AgentToolCallUiText` 的活 API；
- 文件不带阶段命名（`phase4_*`），直接按 P4-6 要求写成永久保护面。

**Mutation check 结论**：在 `packages/zeta_ui/lib/src/ide_colors.dart` 临时加回
`Color get mutedText => textSecondary;`，守卫立即变红并准确点名该文件
（`mutedText 已删除，不得重新引入：[packages\zeta_ui\lib\src\ide_colors.dart]`）；
撤销后恢复绿。守卫确实在保护，不是空跑。

此外守卫在**首次运行**时就抓出了 §2.2 那处 grep 漏掉的残留——它在真实清理面上命中过，
不是事后才写的摆设。

### 5. 停线事件：T18 共享层冻结基线（止损线 S1）

**触发**：首次跑完整门禁，`2370 passed / 1 failed`，唯一失败是
`claude_code_shared_layer_purity_test.dart` 的
`G1 five application files keep frozen content baseline (T18)`：

```
Expected: <2022>  Actual: <2012>
packages/.../agent_conversation_timeline_store.dart lineCount drift
```

**为什么算停线**：删 `renderRevision` 动到了 G1 冻结的 TimelineStore。让守卫恢复绿
必须改断言的期望值（lineCount / byteLength / fingerprint），符合 S1 的定义；
T18 自己的文档也写明「若实现不得不改共享层，必须先停线取得明确批准并记录边界，
批准后才能更新本基线」。**没有自行刷新基线，停下来把 diff 摆给用户定夺。**

**处置**：用户批准刷新基线（2026-08-24）。理由是本次改动是**纯删除**，
diff 全部 4 处都在删一个纯转发的兼容别名，没有新增或修改任何一行可执行语句：

| 位置 | 删除内容 |
|---|---|
| `AgentConversationTurnGroup` ctor | `this.renderRevision = 0,` |
| `AgentConversationTurnGroup` 字段 | `final int renderRevision;` + 3 行文档 |
| `AgentConversationTurnState` | `int get renderRevision => _contentRevision;` + 1 行文档 |
| `AgentConversationTurnState.snapshot()` | `renderRevision: _contentRevision,` |

合并逻辑、entryId 身份、dumb merge、Provider 分支、typed metadata 全部未触碰
（G1 / G2 保护面无变化）。

基线由守卫自己的算法重算（LF 规范化 + FNV-1a 64bit）：
`lineCount 2022 → 2012`、`byteLength 68261 → 67887`、
`fingerprint c5dae1dc034f5873 → bd6bd5a988733b38`。
批准边界已按前五次授权刷新的先例，记进 `claude_code_shared_layer_purity_test.dart`
的基线文档注释。

**这次停线是守卫起作用的正面证据**：G1 冻结面确实拦住了对共享层的无声改动，
流程也确实把决定权交回给了人，而不是让重构顺手把基线改掉。
