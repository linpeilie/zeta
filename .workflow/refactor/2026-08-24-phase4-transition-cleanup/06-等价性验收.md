# 06 · 等价性验收（P4-1）

验收时间：2026-08-24 · 基线对照：`03-安全网.md` §1（根 2371 + Package 74）

---

## 1. 全量测试

`bash tool/test_full.sh`（**不是** `test_affected.sh`）：

| 项 | 基线（改动前） | P4-1 之后 |
|---|---|---|
| 根测试 | 2371 passed / 0 failed | **2371 passed / 0 failed** |
| `zeta_agent_core` | analyze 绿 · 7 passed | analyze 绿 · 7 passed |
| `zeta_agent_providers` | analyze 绿 · 11 passed | analyze 绿 · 11 passed |
| `zeta_foundation` | analyze 绿 · 29 passed | analyze 绿 · 29 passed |
| `zeta_plugin_kernel` | analyze 绿 · 23 passed | analyze 绿 · 23 passed |
| `zeta_ui` | analyze 绿 · 4 passed | analyze 绿 · 4 passed |
| 退出码 | 0 | **0** |
| 墙钟 | 214s | 196s |

**用例数账目可核**：2371 − 2（删除的 2 条"只证明被删符号存在"的用例）
+ 2（新增守卫的 2 条断言）= 2371。第三处删除是某条 test 内部的一行别名断言，
不减少用例数。逐条定性见 `05-执行记录.md` §1.1。

`dart format .` 820 文件 0 改写；`flutter analyze` 根 + `zeta_agent_core` +
`zeta_ui` 均 `No issues found!`。

## 2. 门禁自查（AGENTS.md §1，本次命中 G1 / G8）

| 门禁 | 命令 | 结果 |
|---|---|---|
| G1 共享适配层纯度 | AGENTS.md §1 G1 的 `grep -rnE "(codex\|grok\|claude\|cursor)" …` | **无输出** ✅ |
| G1 内容冻结 | `claude_code_shared_layer_purity_test.dart`（T18） | ✅ 绿（基线已按批准刷新，见 §5） |
| G8 主题 token | `grep -rnE "Color\(0x\|BoxShadow\(\|BorderRadius\.circular\|ColorScheme\.fromSeed\|Shad[A-Z]" lib/src/features` | **无输出** ✅ |
| G8 sf 控件存量 | `grep -rnE "sf\.(IconButton\|TextField\|Button)\." lib/src/features \| wc -l` | 10，与 AGENTS.md 记录的基线一致，本批未新增 ✅ |

其余门禁（G2–G7）本批未触及：没有改身份/合并、没有改 reducer 或 EffectRunner、
没有改 capability 或审批语义、没有跨层移动文件、没有改任何持久化字段或 codec key。

## 3. 旧路径已删干净

不带词边界的全库复查（`grep -rn`，排除守卫文件自身）：

| 符号 | lib | packages | test |
|---|---|---|---|
| `renderRevision` | 0 | 0 | 0 |
| `mutedText` | 0 | 0 | 0 |
| `agentViewModel` | 0 | 0 | 0 |
| `codexUsageSourceId` | 0 | 0 | 0 |
| `selectServiceTier` | 0 | 0 | 0 |
| `kIdeUseAnchoredDynamicSliver` | 0 | 0 | 0 |
| `buildIdeVirtualSliver` | 0 | 0 | 0 |
| `CallbackAgentProviderConfigStore` | 0 | 0 | 0 |
| `CallbackAppearanceSettingsStore` | 0 | 0 | 0 |

`AgentThreadSummary.displayTitle` 已删；同名的 `AgentToolCallUiText.displayTitle`
与 Grok history reader 的私有 getter 按 `02` §1.1 保留，未误伤。

`lib/src` 的 `export` 语句只剩 `ui/localization/app_localizations_x.dart:5`（generated l10n）。

**没有留下注释掉的旧实现、"以防万一"的死代码或 `@Deprecated` 桩。** 删除即物理删除。

## 4. 行为等价：逐个走 `02-现状测绘.md` 的调用方清单

| 原调用方 | 原走哪条路径 | 现在走哪条 |
|---|---|---|
| `ide_shell_controller.dart:208` | `agentViewModel` getter | 无调用方，直接删；`selectedAgentViewModel` 原样保留 |
| `agent_pane_header.dart:145/163/171` | `colors.mutedText` | `colors.textSecondary`。两个调色板中两者取值逐位相同（暗 `neutral[400]`、亮 `zinc[600]`），且 `mutedText` 从未被单独覆盖 → 像素级等价 |
| `agent_timeline_projection_cache.resolve()` | `contentRevision != 0 ? contentRevision : renderRevision` | 直接 `turn.contentRevision`。唯一构造点给两字段赋同一值，兜底恒等 → 结果等价 |
| `agent_conversation_region_state.dart:840` | 快照同时传两个 revision | 只传 `contentRevision`，值不变 |
| `AgentConversationTurnState.snapshot()` | 同上 | 同上 |
| `agent_conversation_view_model.dart:1402` | `selectServiceTier` → controller | 整条链零外部调用，删；`selectFastEnabled` / `resolveCompatibilityConflict` 原样保留 |
| `app.dart` / `zeta_startup_bootstrap.dart` / `zeta_storage_migrator.dart` 等 26 个 `agent_provider_config_store.dart` importer | 可经 re-export 拿到 `AgentProviderConfigStore` | 全部**本来就**直接 import 了 `zeta_agent_core`，删 export 后 analyze 0 error |
| 21 个 `agent_conversation_view_model.dart` importer | 同上 | 同上 |
| 14 个 `appearance_settings_slice_state.dart` importer | 可经 re-export 拿到 `ZetaThemeModePreference` | 同上 |
| `ide_dynamic_sliver_list_test.dart:294` | `buildIdeVirtualSliver(useAnchoredDynamic: false)` | 该 fallback 分支生产从未走到（flag 恒 `true`），helper 与用例一并删 |
| `appearance_settings_store_test.dart:134` | `CallbackAppearanceSettingsStore` 的旧 themeMode 回退 | 生产从未走这条；真迁移在 `zeta_storage_migrator.dart:172-179`，其测试未动 |

**向后读能力全部保留**：`AppearanceSettings.fromLegacyThemeMode`、
`legacyThemeModeStorageKey`、`ZetaStorageMigrator` 及其测试一行未改。

## 5. 停线与批准

本批触发一次 S1 停线（G1 T18 内容冻结基线），已停线取得明确批准后刷新，
边界记录在守卫文档注释与 `05-执行记录.md` §5。**没有自行改基线绕过守卫。**

## 6. 文档同步（AGENTS.md §6）

P4-1 **没有**改动分层、Provider 契约、事件管线、能力协商或持久化格式，
因此不触发 §6 的联动修改义务。已核实：`AGENTS.md`、`engineering_standards.md`、
`design_document.md`、`overview(.en).md`、`developer_guide.md`、`glossary(.en).md`、
`CONTRIBUTING(.en).md`、`CLAUDE.md` **均未引用**本批删除的任何符号。

已更新：`docs/architecture/phase4_transition_cleanup.md`（P4-1 关批结论 + 状态行）。

## 7. CHANGELOG

纯重构，**不写** `CHANGELOG.md`（AGENTS.md §4）。

## 8. 脱敏检查

本任务文件夹（`01`–`06`）已通读：只含符号名、文件路径（仓库相对）、行号、计数与
测试用例名。**无** prompt 正文、模型回复、工具输出、凭证、环境变量值、
`~/.zeta/logs/` 原始日志或用户项目绝对路径。

## 9. 未做到的事（如实记录）

- `03-安全网.md` §3 的四项 §0 `WAIVED` 证据（真实平台 smoke / 14 天使用 / 迁移窗口
  tag / Windows Profile）**本批仍未取得**，按计划书 §0 保持 `WAIVED`，未伪写成 `PASS`；
- 本批改变了 `IdeColors` 的字段集合，理论上属于设计 token 面的变化，但因两个 token
  取值逐位相同且无独立覆盖点，**未做 Windows Profile 或视觉回归采样**——依据是
  §4 的等价性论证，不是实测截图对比。
