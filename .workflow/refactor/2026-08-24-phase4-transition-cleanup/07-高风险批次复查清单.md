# 07 · 高风险批次独立复查清单

计划书 §6 要求 **P4-3b / P4-4 / P4-5** 由**独立 reviewer** 复查，并重点
mutation-check 对应架构守卫。

**这三批的 mutation check 都是执行者自证的，独立复查尚未进行。**
本文把复查做成可直接执行的清单，让人工复查只需要照着跑，不必重新理解上下文。

> 我（执行者）不能同时充当独立 reviewer。本文只是**准备材料**，
> 不构成复查已完成的证据。

---

## 复查前置

```sh
git log --oneline -8        # 确认三批各自的提交
bash tool/test_full.sh      # 基线：应 exit 0
```

每条 mutation 的做法一致：**改一行 → 跑指定测试 → 确认变红 → `git checkout --` 还原**。
还原后务必 `git status` 确认工作树干净。

---

## 一、P4-3b：Project Threads controller + adapter 合并

**提交**：`refactor(project_threads): P4-3b 合并 controller 与 adapter 为单一 runner`

### 1.1 这批到底改了什么

`ProjectThreadsController`(1146 行) 与 `ProjectThreadsSliceRunnerAdapter`(160 行)
合并成 `ProjectThreadsSliceRunner`。执行者的说法是「两者原为一比一转发，业务分支一行未改」。

### 1.2 **复查重点：验证"一比一转发"这个前提**

这是全批的立论基础。请对照合并前的 adapter：

```sh
git show <P4-3b父提交>:lib/src/app/project_threads_slice/project_threads_slice_runner.dart
```

逐个 `case` 确认：每个分支是否**只**做「effect → controller 同名方法 → 用
`operationId` 结算回执」，有没有任何 adapter 自己的判断（如提前 return、参数改写、
错误吞掉）。**只要有一处不是纯转发，合并就可能改变了行为。**

### 1.3 复查重点：分页 / 防抖 / stale token 分支

```sh
git diff <P4-3b父提交>..<P4-3b提交> -- lib/src/features/project_threads/ lib/src/app/project_threads_slice/
```

确认 diff 里**没有**对以下内容的实质修改（只应有类名、方法名、`dispose`→`close`）：
`_loadTokens` · `_searchDebounceTimers` · `projectThreadSearchDebounce` ·
`projectThreadPerProviderFetchCap` · `_aggregateCursorPrefix` 相关逻辑。

### 1.4 复查重点：回执方法上移到端口

执行者把 `operationSucceeded` / `forkSucceeded` / `operationFailed` 从具体 store
提升进 `ProjectThreadsStateOwner` 端口。请确认：

- 端口新增的三个方法签名与 store 上的实现**完全一致**；
- store 侧只加了 `@override`，方法体未改。

### 1.5 Mutation

```sh
# 在 project_threads_slice_composition.dart 里加一行引用
#   Object? _mut; void _probe() { _mut = ProjectThreadsSliceRunnerAdapter; }
flutter test test/src/architecture/app_composition_boundary_guard_test.dart
# 预期：'已收口的 callback seam 不得在生产代码里复活' 变红并点名该文件
git checkout -- lib/src/app/project_threads_slice/project_threads_slice_composition.dart
```

### 1.6 行为面

```sh
flutter test test/src/features/project_threads/ test/src/app/project_threads_slice/
```
预期 53 条全绿。断言应与合并前**逐字相同**（该批声明零断言修改）：

```sh
git diff <P4-3b父提交>..<P4-3b提交> -- test/src/features/project_threads/application/
# 预期只有 import / 类名 / dispose→close 三类改动
```

---

## 二、P4-4：Conversation 切片组合去 ViewModel 耦合

**提交**：`refactor(agent): P4-4 Conversation 切片组合去 ViewModel 耦合`

### 2.1 **复查重点：三处承重逻辑必须逐字未改**

执行者声明这层**不是**一比一转发，含三处承重逻辑且一行未改。请逐条比对：

| 承重逻辑 | 在哪 | 怎么验 |
|---|---|---|
| 五 region 同帧合并成一次 publish | `_flush()` / `_markDirty()` / `flushCount` | diff 里这段应只有 `_viewModel.` → `_regions.` 的接收者改名 |
| 两阶段作用域校验 | `_awaitCommand()` 的 `matchesForExecution` / `matchesForCommit` | `flutter test test/src/features/agent/architecture/agent_conversation_slice_scope_guard_test.dart` |
| fork 的 `null` → failed 翻译 | `_invoke()` 的 `AgentConversationThreadMutationKind.fork` 分支 | diff 里应完全未改 |

### 2.2 复查重点：ViewModel 真的没写实现代码

执行者声明 `AgentConversationViewModel implements` 两个新端口时"一行实现代码都没写"。

```sh
git diff <P4-4父提交>..<P4-4提交> -- lib/src/features/agent/presentation/agent_conversation_view_model.dart
```

预期只有：`implements` 子句、`@override` 注解、以及后来为纯 Dart 订阅补的
`_listenableForRegion` + `addRegionListener` / `removeRegionListener`。
**任何业务方法体的改动都应视为红旗。**

### 2.3 复查重点：端口没有给 UI 开第二条 region 直连

这是执行者两次改错后才收敛的设计（先放 application 违反 G6，再放 presentation
被 region 守卫拦下）。请确认最终形态：

- `agent_conversation_slice_ports.dart` 在 **application** 层且**不 import Flutter**；
- 端口用 `addRegionListener(AgentConversationSliceRegion, …)`，**不暴露 `ValueListenable`**。

```sh
flutter test test/src/architecture/feature_layering_guard_test.dart \
             test/src/features/agent/architecture/agent_region_subscription_guard_test.dart
```

### 2.4 Mutation

```sh
# 在 agent_conversation_slice_composition.dart 里加：
#   Object? _mut; void _probe() { _mut = 'AgentConversationSliceBinding'; }
flutter test test/src/architecture/app_composition_boundary_guard_test.dart
git checkout -- lib/src/app/conversation_slice/agent_conversation_slice_composition.dart
```

### 2.5 行为面

```sh
flutter test test/src/features/agent/presentation/agent_pane_slice_acceptance_test.dart \
             test/src/features/agent/presentation/agent_pane_slice_wiring_test.dart \
             test/src/features/agent/presentation/agent_phase0_behavior_baseline_test.dart
```

---

## 三、P4-5：Shell ChangeNotifier 清零 + IdeHome 定向订阅

**提交**：P4-5a / P4-5b（两个提交）

### 3.1 **复查重点：IdeHome 定向订阅的完备性**

这是全 Phase 4 **最需要人看**的一处，因为它的失败模式是"界面在某些时刻不刷新"，
而自动化测试大概率抓不到。

执行者的论证是：IdeHome 从 Shell 读的每一项都是三个 slice 的投影。请**独立复核**这份对照表：

| IdeHome 读的 | 执行者认定的来源 | 复核 |
|---|---|---|
| `workbenchLayout` | IDE Session slice | ☐ |
| `initialRestoreCompleted` | IDE Session slice | ☐ |
| `projects` / `activeProjectPath` | Workspace slice | ☐ |
| `isProjectHomeActive` | Conversation Workspace + Workspace | ☐ |
| `selectedAgentWorkspaceEntryId` | Conversation Workspace | ☐ |

```sh
grep -oE '_shellController\.[a-zA-Z]+' lib/src/ui/features/ide/views/ide_home.dart | sort -u
```

**逐个确认**：每个是命令调用（不需订阅）还是状态读取（必须有订阅覆盖）。
**只要有一项状态读取不在三个 slice 里，这批就不该关批。**

> 执行者曾在 `05-执行记录.md` §38 得出相反结论（"有 19 处不在 slice 里"），
> 后在 §40 自我更正。**请独立判断哪一版是对的**，不要直接采信 §40。

### 3.2 复查重点：三个 handler 的职责划分完整

`_handleIdeSessionChanged` / `_handleWorkspaceChanged` /
`_handleConversationWorkspaceChanged` 三个 handler 是否覆盖了原
`_handleShellChanged` 的全部三件事：

- 面板宽度同步（依赖 workbenchLayout → 只在 IDE Session handler）☐
- 首页预热（依赖 initialRestoreCompleted + activeProjectPath → **两个** handler 都要调）☐
- Attention 可见性（依赖 selectedEntry + projectHomeActive + activeProjectPath →
  **两个** handler 都要调）☐

### 3.3 Mutation

```sh
# M1：Shell 加回 extends ChangeNotifier
# M2：IdeHome 加回 _shellController.addListener(_rebuild);
flutter test test/src/architecture/app_composition_boundary_guard_test.dart
# 预期分别命中 'Shell 不得重新变成 Flutter 通知源' 与 'IdeHome 不得监听整个 Shell'
```

### 3.4 **无法用自动化覆盖的部分**

`03-安全网.md` §3 已记明：本批改变 `IdeHome` 的 rebuild 边界，
**自动化基线挡得住量级回归，挡不住帧级抖动**；而 Windows Profile 属 §0 `WAIVED`。

人工验收建议至少覆盖：

1. 打开项目 → 观察加载中状态是否正常出现与消失；
2. 冷启动恢复会话 → 观察首页/工作台切换是否卡在中间态；
3. 拖动左侧面板宽度 → 松手后宽度是否被正确持久化与恢复；
4. 切换到设置页再切回 → 当前 Thread / 草稿 / 滚动位置是否保留；
5. 窗口失焦/聚焦 → 桌面通知（Attention）是否按预期开关。

---

## 四、复查结论（由 reviewer 填写）

| 批次 | 结论 | reviewer | 日期 |
|---|---|---|---|
| P4-3b | ☐ 通过 ☐ 有问题 | | |
| P4-4 | ☐ 通过 ☐ 有问题 | | |
| P4-5 | ☐ 通过 ☐ 有问题 | | |

有问题的项请写明**哪个断言/哪个场景**，不要只写"感觉不对"。
