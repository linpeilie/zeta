# 更新日志

本文件记录 Zeta 面向用户的变化。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

条目类型：`新增` / `变更` / `弃用` / `移除` / `修复` / `安全`。

面向开发者的详细改动记录见 [`docs/history/development_log.md`](docs/history/development_log.md)。

## [未发布]

首个版本尚未发布。以下是当前主干已具备的能力。

### 移除

- Agent 管理页去掉「超时时间」配置；检测与连接测试仍使用固定 60 秒上限，
  不再写入或展示可调超时
- Agent 管理页去掉可执行文件「选择文件」；路径仅通过自动检测发现
- Agent 管理页去掉顶部「已安装 / 已启用 / 运行中 / 需要处理」统计条
- Agent 管理页去掉状态筛选（全部状态 / 已启用 / 需要处理 / 运行中 / 可更新）
- Agent 首页去掉最右侧 Activity Rail 和右下 Tools 占位面板；Files 右栏改由标题栏
  右侧按钮控制折叠

### 变更

- Agent 首页移除最左侧 Activity Rail，将 Projects / Threads 与只读 Agent 统计合并为
  一个左侧卡片；统计默认折叠并展示套餐/Provider、最短周期余量和今日 Token。标题栏
  左侧按钮控制整个左栏，窄窗口改用浮层，重启后恢复显隐、宽高、展开态和统计 Provider
- Plan 执行确认默认恢复进入 Plan 前仍有效的权限；上下文或选项失效时回落到
  Provider 的保守默认，也可在执行卡上只为本回合改选，不会提前应用或保存权限
- 统一全局圆角：第三方组件与自绘组件此前是两套相差 1~2px 的平行体系，
  现在按钮、输入框、标签、列表行的圆角完全对齐
- 面板、侧栏、画布与浮层改用平滑圆角（squircle）
- 会话区改为文档流：Agent 正文、最终答复、工具调用、命令组、文件编辑不再套卡片，
  文字直接落在画布上；回合之间用大留白加一条全宽发丝线分隔
- 长会话里更容易找到自己的提问：用户消息的左侧竖线、角色前缀与正文字重都加重
- 连续的操作记录读起来是一整块「过程」：行间距收紧，折叠箭头移到行首，
  不再有横贯画布的空档
- 使用统计的趋势图改为单色发丝线 + 点阵网格，去掉面积填充与常态数据点；
  概览数字与图标不再着色，额度条改为无底槽的发丝指示线
- 深色主题改为极深炭黑单色系：表面按 frame → 画布 → 面板 → 控件 → 浮层
  逐档提亮，不再混用两种灰
- 全局移除阴影，层级改由表面明暗与 1px 半透明细边框表达；仅菜单、Popover、
  Toast 等浮层保留一层极淡投影兜底
- 鼠标悬浮改为只把背景提亮约 5%，不再改变元素自身颜色
- 圆角收敛为四档（标签 4 / 按钮输入框代码块 6 / 卡片 8 / 面板浮层 12），
  嵌套时内层始终小于外层
- 会话中的用户消息不再使用右对齐气泡：所有问答统一左对齐，改为终端日志式的
  左侧竖线加等宽角色前缀
- 主色降低饱和度，只在发送按钮、选中指示线等核心操作点出现
- 工作台整体收紧留白，面板标题条高度与窗口标题栏对齐，提高单屏信息密度
- 会话列表的最近活跃时间改用更弱的元信息层级，与状态文字拉开差距

### 新增

- Agent 时间线新增中立文件变更证据：Grok 与 Claude Edit 展示替换片段，Claude Write
  展示写入内容，Codex 结构化 fileChange 展示 unified patch；仅有命令时继续显示命令卡，
  不猜测文件 diff，实时 turn fallback 也会明确标注不可从历史恢复
- 对话消息与 Composer 草稿中的本地图片支持点击弹层查看大图（可缩放、点遮罩/Esc 关闭）

### 变更

- Claude Code Provider 的展示名简化为 Claude，并改用保留品牌原色的 Claude 图标
- 使用统计派生索引升级为多 Provider 分区：Codex 与 Grok 共用 fingerprint 缓存，
  打开统计页时未变更的本地历史可跳过重解析
- 界面字体改为内置 Geist，不再依赖各平台系统字体；中文仍由平台回退字体承接，
  在外观设置里可以改回系统字体
- 模型 ID、Provider 名、Token 统计和表格数字列统一改用等宽字体；数字启用等宽
  数位后，使用统计的各列可以按位对齐
- 内置 JetBrains Mono 补齐 Regular 与 Bold 字重，代码块和行内代码不再使用
  合成字重
- 对话导航轨 hover 动效只拉伸短横线宽度，高度保持稳定

### 修复

- 新建 Agent 会话首次发送后会保留并重新校验权限选项目录，Codex、Grok 与 Claude
  不再因 global/session Provider 切换显示原始权限 ID 或丢失权限选项
- Codex 本地历史中的 `Apply patch` 现在会从结构化 `patch_apply_end` 恢复文件路径、
  动作与可用的差异/写入内容，不再只显示工具名称
- Claude Code 历史读取会在 Provider 未覆盖用户目录时继承系统 HOME/USERPROFILE；
  重启后可凭项目路径与会话 ID 恢复历史，不再只显示居中的输入框
- Claude Code 仅返回最终完整文本时，回复会正确创建并显示，不再只显示耗时与 Token
- Windows 启动 Agent CLI 时统一解析 `.exe`、`.cmd`、`.bat` 与 `.ps1`；
  Claude Code 会跳过 npm 生成的无扩展名 POSIX shim，不再报 Win32 错误 193
- Plan 计划文档展示时同步隐藏 live 活动条，实时步骤进度卡不再误隐藏该状态
- Agent 统计套餐额度恢复可见进度条，并以已用比例展示用量
- 会话时间线的命令集、文件编辑组与工具调用图标在折叠标题行中上下居中
- 分叉会话后创建并选中独立 thread；编辑后重试和后续操作不再误用源会话
- 模型配置、Composer 与工具卡中若干文案不跟随「界面字号」设置缩放
- Grok 实时会话适配传输层 `retry_state`：重试中显示自动重试提示，耗尽时结束
  本回合为失败，不再静默忽略

### 新增

**工作台**

- 三栏桌面工作台：Projects（项目与会话）、Agent（对话时间线）、Files（项目文件树）
- 面板宽度可拖拽；窄窗口下左右栏自动改为浮层
- 跨页面保活：切到设置或使用统计再切回来，草稿、滚动位置和面板宽度不重置
- 深色 Graphite Night 与浅色 Graphite Day 双主题，可调字体与字号
- macOS 原生「文件 → 打开项目」菜单
- Grok 会话列表在正式标题下方展示 `last_turn_summary` 旁文案

**Agent 对话**

- 连续时间线展示用户消息、Agent 回复、推理过程、工具调用与 typed 文件变更证据，带语法高亮
- 权限审批、用户提问、Plan 审批三类交互卡片固定在输入框上方，响应后不在时间线重复出现
- Plan 模式：先出计划再执行，执行时新建独立回合且不预授权任何操作
- 回合终态展示耗时、Token 用量与本回合模型配置
- Composer 支持粘贴或选择图片、`$` 插入 Skill、`/` 唤出命令菜单、`@` 引用项目文件

**Provider**

- Codex CLI app-server 接入（默认 Provider），支持会话列表、历史读取、恢复、分支、模型切换、Skills 与 Plan 模式
- Grok ACP 接入，能力按握手结果自动降级
- Claude Code stream-json 接入，支持连续对话、思考与工具时间线、权限审批、Plan 审批和取消回合
- Claude Code Provider 支持只读展示本地历史、从 Zeta 列表隐藏记录，并按原 session 恢复对话
- Claude Code 自动检测只检查本地 CLI 版本、登录元数据和日志路径，不连接模型服务；
  手动“测试连接”前会明示提醒可能产生少量用量
- Claude Code 对话可在 Composer 中选择 Opus、Sonnet 或 Haiku；模型目录优先读取
  当前账号的动态列表，失败时回退内置目录；对话进行中的切换从下一回合开始生效
- Claude Code 支持展示五小时与周套餐用量；详情页可关闭「账号数据增强」，关闭后
  不再读取登录凭据或发起模型目录/套餐用量 REST 查询，并回退内置模型目录
- Claude Code 对话可从 Composer 的 `/` 菜单选择 `Compact context` 压缩上下文；
  仅在当前会话空闲且可写时显示
- 会话级 Provider Binding：打开草稿或历史不启动 CLI；首次提交时按会话创建实例，空闲 10 分钟自动回收并在下次提交时恢复
- 会话权限状态收敛到各自 Binding，runtime 权限变化不再影响其他会话
- 能力协商机制：Provider 不支持的功能在界面上不出现，而非点了无反应
- Agent 管理页：CLI 身份、版本、账号与连接诊断；自动检测不调用模型，Claude Code 的手动连接测试会先提示可能产生少量用量

**会话与持久化**

- 重启后恢复项目列表、当前项目、文件树展开状态、选中文件与会话缓存
- 自有数据存于 `~/.zeta/`，版本化 JSON，宽容解码，损坏不阻断启动
- 旧 SharedPreferences 数据一次性迁移

**桌面通知**

- 回合结束、需要审批、Agent 提问时发送系统通知，仅在目标会话不可见时触发
- Windows 任务栏闪烁、macOS Dock 角标、Linux urgency 提醒
- 点击通知恢复窗口并定位到对应会话
- 通知正文只含类别与项目目录名，不含 prompt、回复、命令或完整路径
- 「任务结束」与「需要确认」两类通知可独立开关

**使用统计**

- 按时间、项目、Agent 与模型筛选，展示调用次数、成功率、Token 消耗与响应速度
- 展示 Provider 实际返回的套餐用量窗口与重置时间
- 派生索引只保存白名单字段，不落盘 prompt、回复正文与工具输出

**发布**

- macOS（通用）、Windows x64、Linux x64 桌面安装包与免安装包，附 `.sha256` 校验

### 移除

- Cursor Provider 已退役：不出现在 catalog、设置与运行时组合中，不启动 Cursor 进程，也不读取或改写 `~/.cursor` 与项目 `.cursor`。遗留的 `cursor_sessions.json` 仅作为受保护用户数据原样保留

### 已知限制

- 安装包未做代码签名与公证，首次启动会触发 macOS Gatekeeper 或 Windows SmartScreen 提示
- 不含内置代码编辑器；Agent 上下文只传递项目路径与选中文件路径，不自动读取文件内容
- 文件树忽略列表固定，暂不支持自定义
- 日志不会自动清理，需手动删除 `~/.zeta/logs/` 下的旧文件

---

## 维护说明

发布新版本时：

1. 把 `[未发布]` 下已完成的条目移到新的版本小节，标注版本号与日期，例如 `## [0.1.0] - 2026-08-15`；
2. 在 `[未发布]` 留空的类型小节可以删掉；
3. 同步更新 `pubspec.yaml` 的 `version`，再按[发版指南](docs/release/release_guide.md)打 Tag。

只记录**用户能感知**的变化。纯重构、测试补充和内部依赖调整不必写进来。
