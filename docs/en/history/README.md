# History (archive)

[中文](../../zh/history/) ｜ English

Archived documents are kept as historical evidence only and **do not describe currently supported
capabilities**. By convention they keep their original language and are not translated, so this
directory holds pointers rather than copies.

| Document | Language | Why it is still worth reading |
| --- | --- | --- |
| [Claude Code provider adapter (historical proposal)](../../zh/history/claude_code_provider_adapter.md) | Chinese | §2 integration contract, §4 data layer design notes, §6 plan/permission/history semantic mapping and §7 CLI detection are still design input for `packages/claude_code_client`. The directory layout, class names and `lib/src/features/agent/...` paths in it belong to the old repository — **do not copy them**; see the [package API contracts](../architecture/package_api_contracts.md) for the new topology. |
