name: agent_history_client
description: Provider-neutral agent history input client for Zeta.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  agent_provider_contracts:
      path: ../agent_provider_contracts
  zeta_logging:
      path: ../zeta_logging
  zeta_storage:
      path: ../zeta_storage

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.3.0
