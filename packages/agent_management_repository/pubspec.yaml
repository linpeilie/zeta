name: agent_management_repository
description: Agent management domain repository for Zeta.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  agent_config_client:
      path: ../agent_config_client
  agent_management_client:
      path: ../agent_management_client
  agent_provider_contracts:
      path: ../agent_provider_contracts
  equatable: ^2.1.0

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.3.0
