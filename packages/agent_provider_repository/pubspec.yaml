name: agent_provider_repository
description: Agent provider domain repository for Zeta.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  agent_config_client:
      path: ../agent_config_client
  agent_provider_contracts:
      path: ../agent_provider_contracts
  claude_code_client:
      path: ../claude_code_client
  codex_app_server_client:
      path: ../codex_app_server_client
  equatable: ^2.1.0
  grok_acp_client:
      path: ../grok_acp_client
  zeta_logging:
      path: ../zeta_logging

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.3.0
