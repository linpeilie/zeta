name: grok_acp_client
description: Grok ACP data client for Zeta.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  agent_history_client:
      path: ../agent_history_client
  agent_provider_contracts:
      path: ../agent_provider_contracts
  json_rpc_transport:
      path: ../json_rpc_transport
  zeta_logging:
      path: ../zeta_logging
  zeta_storage:
      path: ../zeta_storage

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.3.0
