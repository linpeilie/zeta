name: json_rpc_transport
description: JSON-RPC stdio transport and lifecycle primitives for Zeta.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  clock: ^1.1.2
  zeta_logging:
      path: ../zeta_logging

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.3.0
