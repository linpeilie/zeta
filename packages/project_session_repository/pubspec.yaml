name: project_session_repository
description: Project session domain repository for Zeta.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  agent_provider_contracts:
      path: ../agent_provider_contracts
  equatable: ^2.1.0
  project_session_client:
      path: ../project_session_client

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.31.1
  very_good_analysis: ^10.3.0
