name: zeta_agent_core
description: Zeta 的中立 Agent 内核：领域模型与端口、Binding/runtime 契约、事件管线、纯 reducer、TimelineStore 与 Effect 描述。
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.18.0
  zeta_foundation: ^0.1.0

dev_dependencies:
  test: ^1.25.0
