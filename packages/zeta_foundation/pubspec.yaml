name: zeta_foundation
description: Zeta 的公共契约与宿主平台工具：时钟、操作身份、MVI transition、路径与脱敏指标端口。
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  path_provider_platform_interface: ^2.1.3
  test: ^1.25.0
