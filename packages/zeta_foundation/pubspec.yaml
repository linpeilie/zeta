name: zeta_foundation
description: Zeta 的纯 Dart 最小公共契约：时钟、操作身份、失败分类、MVI transition 与脱敏指标端口。
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.0
